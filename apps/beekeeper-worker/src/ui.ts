// (removed) The single-file HTML shift-editor UI that used to live here was
// ported to apps/web at app/schedule/* — a real Next.js page that inherits the
// shared Splash design system + navy header and renders a day-column week-grid
// calendar. This worker is now API-only (see src/index.ts). File kept empty to
// avoid a dangling import; safe to delete from the repo.
export {};
