# Brief index

Mirrors the prioritized work list in BUILD_STATE.md. Source of truth for
status is BUILD_STATE.md and the individual brief files; this is a
navigation aid.

| # | Brief | Status | Blocks | File |
|---|---|---|---|---|
| 161 | Promo announce — inline materials payload contract guard. Brief 160 was empirically reported as producing `outbound_emails.attachments = []` for sends with inline-flagged image materials, leaving recipients with broken-image placeholders where embedded images should render. Source audit found Brief 160's source code (commit `bd2f0d6`) already implements the contract correctly — `handleSendAnnouncement` maps over all `resolvedMaterials` (not just `attachmentMaterials`) and tags each inline entry with `is_inline: true` + `content_id: "material-{id}"`. The reported empty-attachments behavior most likely came from testing against a stale CF Workers deployment before the Brief 160 push landed. Rather than rewriting code that's already correct, this brief adds a regression-guard layer so the bug class cannot silently recur: (a) extracts the inline attachments-building loop into a named exported helper `buildOutboundEmailAttachmentsForAnnouncement(resolvedMaterials, inlineMaterialMap)` next to `partitionMaterialsForRender` in `apps/promo-worker/src/handlers/announce.ts` (both exported for testability), (b) adds an IMPORTANT block-comment above the helper explaining the silent-failure mode (no console error / no PA error / no queue error — just a missing image in the recipient's inbox) and the cross-side contract (HTML emits `cid:material-{id}` reference; queue attachment carries the bytes; BOTH are required), (c) adds FIXTURE_4 in `apps/promo-worker/test/render-html.snap.ts` asserting the four invariants — attachments.length === resolvedMaterials.length, image entry has is_inline:true, image entry's content_id matches the cid:material-{id} in the rendered body HTML, pdf entry has neither flag set — throwing on contract violation so `tsx test/render-html.snap.ts` exits non-zero. Decisions: (1) `process.exitCode = 1` swapped for `throw new Error(...)` so the test file typechecks under the worker tsconfig's `@cloudflare/workers-types` (no `@types/node`). (2) Helper takes `Map<string, {contentId: string}>` rather than the full inline-partition shape — smaller surface, easier to use from the test fixture. Validation: pnpm typecheck 21/21 green; promo-worker wrangler dry-run 881.58 KiB raw / 167.77 KiB gzip (+0.27 KiB raw / +0.04 KiB gzip vs Brief 160 — the helper extraction is a wash). No worker / Supabase / R2 / wrangler.toml / secret changes. No apps/web changes. PA flow unchanged (PA_FLOWS_BRIEF_160.md was already correct). | Completed (2026-06-06) | none — the regression guard is preventive; the operator's prior failed send should resend cleanly once the bd2f0d6 deploy is live | [brief-161-promo-announce-inline-materials-payload-fix.md](brief-161-promo-announce-inline-materials-payload-fix.md) |
| 160 | Promo announcement — preview + branded HTML + inline materials: extracts the Brief 134 `workflow-email-shell.ts` into a new shared workspace package `@splash/email-shell` (exports `wrapInEmailShell`, `EmailShellOptions`, `escapeHtml`, `escapeAttr`) so promo-worker can render the same Splash-branded HTML envelope as forms-worker. Forms-worker swaps its local import for the shared package; behavior identical. `OutboundEmailAttachment` in `@splash/db-supabase` widened (additively) with two optional fields — `is_inline?: boolean` and `content_id?: string` — that PA's Send Email V2 connector reads to flip `IsInline` + `ContentId` per attachment, embedding images inline in the body via CID reference (`<img src="cid:material-{id}" />`). Forms-worker's queue claim handler passes both fields through verbatim. New module `apps/promo-worker/src/announce/render-html.ts` exports `renderAnnouncement(input)` returning `{html, plainText}`: paragraphs from operator's plain-text body (escaped, blank-line → `<p>`, single `\n` → `<br />`), then inline `<img>` tags for image-MIME materials, then optional PTP three-block section, then a trailing "Attachments: ..." line for non-image materials. Wrapped in the shared Splash shell with `title=subject` + first-100-chars preheader. Plain text mirrors the same structure for client fallback. `handleSendAnnouncement` rewired to partition resolved materials into inline (image MIME → CID-referenced + `is_inline:true`) and attachment (everything else), build attachments accordingly, call `renderAnnouncement` once before fan-out, and enqueue with both `body_text` + `body_html`. New endpoint `POST /promo/api/promos/{id}/announce/preview` (same role gate + CSRF as `/announce`; `recipientEmails` accepted but optional; no snapshot insert, no fan-out, no activity log) returns `{ok, html, plain_text, attachment_summary: {inline_count, attachment_count, total_size_bytes}}`. Apps/web `AnnouncementComposeModal` grows a Preview button next to Send + a sub-modal that renders the HTML in `<iframe srcdoc sandbox="allow-same-origin">` (no JS execution; styles render). Per-material inline-vs-attachment radio for image materials (defaults to inline; operator can demote to attachment-only); non-image materials show static "Attachment (non-image)" label. Hidden FormData entries `materialMode[{materialId}]` ride along to both Send + Preview actions. New `previewAnnouncementAction` mirrors `sendAnnouncementAction` minus the recipient-required gate. `<ActionForm>` gains optional `id` prop so the Preview button (sibling, not child) can read the form's FormData via `getElementById`. Worker also accepts optional `materialModes` body field — when present, overrides the image-MIME = inline default per material. Three renderer fixtures captured in `apps/promo-worker/test/render-html.snap.ts` for human read-review. PA flow widening is documented (not yet executed) — until PA reads the new attachment fields, inline-flagged images fall back to regular attachments and the body HTML still ships, just without the images embedded. Typecheck 21/21; promo-worker dry-run 881.31 KiB raw / 167.73 KiB gzip; forms-worker dry-run 2024.58 KiB raw / 445.49 KiB gzip; apps/web build green with `/admin/promotions/[id]` at 6.84 kB / 114 kB (well under the 150 kB First-Load budget). | Completed (2026-06-06) | PA flow update (operator-side; not code) to set `IsInline` + `ContentId` per attachment from the new fields; until then inline images degrade to attachments | [brief-160-promo-announcement-preview-branded-html-inline-materials.md](brief-160-promo-announcement-preview-branded-html-inline-materials.md) |
| 159 | Sysadmin "Set Promo Role" tool — write `promo_user_roles`. Closes the gap Briefs 153–158b left: `promo_role` was settable only via direct SQL INSERT into `promo_user_roles`. New helper `setPromoRole(client, {userId, role, createdBy?})` in `@splash/db-supabase` upserts (or deletes when role=null) the single-scalar row + returns before/after snapshots for the audit log. New worker endpoint `POST /sysadmin/api/users/{userId}/promo-role` on sysadmin-worker — super_admin-only; second dynamic-path endpoint via new `PROMO_ROLE_PATH_RE` sibling to `DC_ROLE_PATH_RE`; role allow-list = `super_admin | it | marketing | ops | null`. Audit action `set_promo_role`; target_type `promo_user_roles`. New `<SetPromoRoleCard>` mounted in `_sections/UserOperations.tsx` after `<SetDcRoleCard>` — 7th Manage Users card; no companion locations sub-picker (promo_role is a scalar); per-role hint copy uses an inline event-delegated script (mirrors PASSWORD_MATCH_SCRIPT in ResetPasswordCard). New `setPromoRoleAction` mirrors `setDcRoleAction`; success copy notes the cookie-refresh constraint ("The user must sign out and back in to see the change take effect"). AuditLogPanel + worker allow-lists extended (`set_promo_role` action + `promo_user_roles` target_type + `USER_TARGET_TYPES_CSV`). user_permissions.role, dc_role, and promo_role are three independent permission domains. No schema changes; no new env vars / bindings. Typecheck 19/19; apps/web build succeeded; sysadmin-worker wrangler dry-run bundled at 763.39 KiB raw / 143.66 KiB gzip. | Completed (2026-06-06) | none — closes the role-grant gap for the Promotions feature | [brief-159-sysadmin-set-promo-role-tool.md](brief-159-sysadmin-set-promo-role-tool.md) |
| 158b | Promotions — apps/web write affordances: ten write surfaces wired end-to-end on top of Brief 158a's read pages. New `/admin/promotions/new` create form (super_admin / it / marketing; multi-select locations picker pre-loaded from `pricing_simple`; POS-behavior textarea auto-disabled when promoType ∈ {Same, Other}; required-fields client validation matches the worker's defense-in-depth checks). `StatusEditor` client island wired into both the live view's status pipeline card AND the IT ticket page header — Save dispatches the `setPromoStatusAction` which surfaces `unchanged: true` as a quiet "Status unchanged" banner. `MaterialUploadModal` (`<dialog>`-shaped overlay; multipart form posts straight through `uploadMaterialAction`; client-side 50 MB size check matches the worker's hard cap). `MaterialDeleteButton` (Brief 128-style `window.confirm()` then submit). `PtpEditModal` (three textareas, pre-populated from `promo.ptp`; empty submit allowed — worker treats as cleared). `AnnouncementComposeModal` (recipients pre-resolved server-side from `pricing_simple.am_email/rm_email/site_email` via a new `GET /promo/api/locations/recipients?codes=...` endpoint on splash-promo; ad-hoc recipient input with `@splash/types/email-validate isValidEmail` client gate; materials checklist defaults all-checked; include-PTP checkbox disabled with tooltip when `ptp === null`; failed-recipients amber sub-banner when send is partial). IT ticket page surfaces `TicketFieldsForm` (single Save button — diff against initial hidden inputs so only changed fields PATCH; ready-by + roadblocks + amber-tinted internal-note textareas), `AssigneesEditor` (search-as-you-type autocomplete against new `GET /promo/api/users/search?q=...` endpoint that reads `auth_unified WHERE promo_role IS NOT NULL`; remove-with-confirm chips; add fires `addAssigneeAction` which echoes `Status auto-advanced to Scoped` when Brief 155's auto-flip triggers), `LocationProgressToggleable` (React 19 `useOptimistic` — checkbox flips immediately on click; server action runs in background; revert + alert on failure). Every write goes through the Brief 19 `<ActionForm>` pattern — server action returns `ActionResult`, inline success/error banner, `router.refresh()` on success. `ActionResult` type widened (additively) with optional `data?: unknown` + `fields?: Record<string,string>` payloads — `createPromoAction` uses `data: {promoId}` for the redirect-on-success flow without a sentinel string hack. **Three new promo-worker endpoints** layered on top of Brief 154–157 to side-step apps/web's lack of a Supabase service-key binding: `/locations` (list for the create form's multi-select), `/locations/recipients?codes=...` (bulk resolve emails for the announcement modal), `/users/search?q=...` (autocomplete for the assignee editor). All three use the worker's existing service-key binding + 5xx-on-error + fail-soft on apps/web side (empty list, not banner). 4 read pages + 1 new page rebuilt in this brief. No new dependencies — leverages existing `@splash/types/email-validate`. | Completed (2026-06-05) | Brief 159 (sysadmin "Set Promo Role" card so non-super_admins can grant promo roles); promo feature is functionally complete end-to-end | [brief-158b-promo-apps-web-write-affordances.md](brief-158b-promo-apps-web-write-affordances.md) |
| 158a | Promotions — apps/web read pages: four new server-rendered pages under `/admin/promotions/*` sourced live from the splash-promo worker via the Brief 153 `PROMO_WORKER` service binding. `/admin/promotions` (any non-null `promoRole`) — responsive filterable card grid + offset pagination + "+ New promotion" button (super_admin / it / marketing). `/admin/promotions/queue` (super_admin / it only) — work-queue table sorted priority desc → requested_go_live_date asc; "Assigned to me" defaults ON. `/admin/promotions/{id}` (any non-null `promoRole`) — multi-card live view: header + six-state status pipeline + details + locations + materials grid + PTP + announcements (with inline history `<details>`) + activity timeline. "Open IT ticket →" link visible to IT/super_admin only; every "+" write affordance is a disabled stub at 158a. `/admin/promotions/{id}/ticket` (super_admin / it only) — IT ticket view with submitted-request read-only card + IT-response card (`ticket.readyByDate` / assignees / roadblocks / `ticket.internalNote` in amber-tinted "IT only" callout). Two new tiles register under the Operations group in `apps/web/app/admin/dashboard/_lib/tiles.tsx`: "Promotions" (`promoRole != null`) + "IT Promotions Queue" (`promoRole === 'super_admin' \| 'it'`). New `"promotions"` entry in `ADMIN_KNOWN_SUBPATHS` per the CLAUDE.md mandatory rule. Shared components in `apps/web/app/admin/promotions/_components/`: PromoStatusPill, PromoPriorityPill, PromoStatusPipeline, LocationProgress, MaterialChip, ActivityTimeline, PromoFilterBar, AnnouncementHistoryButton, NoAccessCard. Worker-fetch helper at `_lib/worker-fetch.ts` follows Brief 94/109 service-binding-first pattern. Phase 7 user-name resolution intentionally stubbed — the brief's prescribed `@supabase/supabase-js createClient` path requires adding the dep to apps/web AND a `SUPABASE_SERVICE_KEY` binding on `splash-web` (neither exists today); the stub at `_lib/user-lookup.ts` returns an empty map and the UI renders shortened user_ids. Future brief should add a `GET /promo/api/users?ids=...` endpoint to promo-worker. Write affordances all land in Brief 158b. | Completed (2026-06-05) | Brief 158b (write affordances — create form, edit modals, server actions) builds on the components + helpers this brief lands | [brief-158a-promo-apps-web-read-pages.md](brief-158a-promo-apps-web-read-pages.md) |
| 157 | Promotions — announcement send (snapshot + outbound_emails fan-out): new endpoint `POST /promo/api/promos/{id}/announce` on `splash-promo` (super_admin / it / marketing; CSRF gate). JSON body `{subject, bodyText, recipientEmails[], selectedMaterialIds[]?, includePtp?}` with unknown-keys rejection (400 `bad_request`); subject ≤500 chars, bodyText ≤50 000 chars, recipientEmails non-empty + ≤500 entries each validated via `@splash/types/email-validate isValidEmail` (Brief 152) with 400 `invalid_recipients` listing the failing entries, case-insensitive dedup. `selectedMaterialIds` (optional, UUID-v4-shape) checked for membership against this promo — 400 `material_not_on_promo` listing offenders. `includePtp` boolean (default false) — when true, fetches `promo_ptp` and appends a `\n\n---\n\nPTP (Purpose, Tools, Process)\n\nPurpose: ...\nTools: ...\nProcess: ...` block to the queued body; missing PTP row uses `(none)` placeholders (permissive v1). Sequence: validate → INSERT `promo_announcements` snapshot (subject + raw operator body WITHOUT PTP block + recipient_emails + included_material_ids + included_ptp; capture announcement_id + sent_at) → loop recipients with `enqueueOutboundEmail({source_worker:'promo-worker', source_kind:'promo-announcement', source_id:announcement_id, recipient, subject, body_text:<rendered with PTP appended when opted in>, attachments:[{filename, mime, size_bytes, r2_key, bucket:'PROMO_FILES'}]})` — per-recipient errors caught individually and collected into `failedRecipients[]`. Activity log `announcement_sent` with details capturing announcement_id, recipient/enqueued/failed counts, material count, includedPtp, subject. Response 201 `{ok, announcementId, enqueuedCount, failedRecipients, sentAt}`. **Detail-response extension on `GET /promo/api/promos/{id}`** — replaces Brief 154's placeholder `announcements: []` with a real PostgREST embed (`promo_announcements(id, sent_at, sent_by, subject, body_text, recipient_emails, included_material_ids, included_ptp)`) ordered `sent_at.desc` and capped at 20. `body_html` is intentionally omitted from the embed (always null at v1; would inflate the payload). **Cross-worker bucket widening.** `OutboundEmailAttachment.bucket` in `@splash/db-supabase` widened from `"FORMS_FILES"` to `"FORMS_FILES" | "PROMO_FILES"`. forms-worker's `inlineAttachments` dispatch now picks `env.PROMO_FILES` when `bucket === "PROMO_FILES"`; `env.PROMO_FILES?: R2Bucket` is optional on the forms-worker `Env` so an unbound binding (forms-worker not yet redeployed) skips the attachment with a log line — the email still sends. PRE_DEPLOY_PROMO.md documents the new `[[r2_buckets]] binding="PROMO_FILES"` block the operator must add to `apps/forms-worker/wrangler.toml` before announcement attachments arrive cleanly. | Completed (2026-06-05) | apps/web "Compose announcement email" affordance + per-announcement timeline rendering (Brief 158) | [brief-157-promo-announcement-send.md](brief-157-promo-announcement-send.md) |
| 156 | Promotions — materials upload/delete/serve + PTP upsert: four new endpoints on `splash-promo`. `POST /promo/api/promos/{id}/materials` (super_admin / it / marketing; multipart upload; required `name` ≤500 chars + `kind` ∈ {image, video, copy_messaging, signage, email_asset, other} + `file`; HARD_LIMITS = 50 MB/file + 20/promo soft cap; sniffs MIME from first ~4 KB via `file-type`, ignores client Content-Type; deny-list rejects text/html + Windows executables + shell scripts; R2 path `promo-materials/{promoId}/{materialId}.{ext}` with extension via shared `_mime.ts` lookup; sequence R2 PUT → DB INSERT → activity log with best-effort R2 rollback on INSERT failure + loud orphan log when rollback also fails; emits `material_added` activity row). `DELETE /promo/api/promos/{id}/materials/{materialId}` (super_admin / it / marketing; DELETE DB row first, then best-effort R2 delete — inverted vs. upload to avoid dangling pointers on read; emits `material_removed`). `GET /promo/api/promos/{id}/materials/{materialId}/file` (any non-null `promoRole`; no CSRF gate per read-only spec; streams R2 body with `inline` Content-Disposition for `image/*` MIME and `attachment` for everything else; matches forms-worker admin serve route shape from Brief 92). `PUT /promo/api/promos/{id}/ptp` (super_admin / it / marketing; JSON body `{purpose, tools, process}` all required, trim + max 10 000 chars each; rejects unknown keys; upsert via PostgREST `on_conflict=promo_id` + `Prefer: resolution=merge-duplicates`; emits `ptp_updated` with `details.fields` listing fields that actually changed value vs. before-state — no-op submit emits no row). Shared `_mime.ts` centralizes the sniffed-MIME → extension lookup + the deny-list so adding a new file kind is a one-file change. `file-type@^19.6.0` added to `apps/promo-worker/package.json`. | Completed (2026-06-05) | apps/web live-view "Add material" / "Build PTP" affordances (Brief 158); announcement compose (Brief 157) reads the materials list to render the attachment picker | [brief-156-promo-materials-upload-and-ptp-write.md](brief-156-promo-materials-upload-and-ptp-write.md) |
| 155 | Promotions — ticket / status / assignee / location-progress writes: five new endpoints on `splash-promo`. `PATCH /promo/api/promos/{id}/ticket` (super_admin / it; partial update of `ready_by_date` / `roadblocks` / `internal_note`; rejects unknown body keys; trims + caps text fields at 10 000 chars; ISO-date validates `readyByDate`; computes deltas vs. before-state; emits umbrella `ticket_updated` row + per-field-typed `roadblocks_updated` / `internal_note_updated` rows only when those specific fields actually changed value). `PATCH /promo/api/promos/{id}/status` (super_admin / it / marketing — marketing widened in for campaign-end `Ended` flips; required `status` enum-validated; no-op short-circuit with `unchanged: true` and no activity log when the value matches the existing status; stamps `status_updated_{at,by}`; emits `status_changed` with `{from, to, auto: false}`). `POST /promo/api/promos/{id}/assignees` (super_admin / it; body `{userId}` UUID-v4-shape validated; rejects 400 `target_no_promo_role` when target's `auth_unified.promo_role` is null; INSERT with PK-collision-as-409 `already_assigned`; emits `assignment_changed` with `{action:"added", userId, assignedByEmail}`). `DELETE /promo/api/promos/{id}/assignees/{userId}` (super_admin / it; `Prefer: return=representation` counts affected rows; 404 `not_assigned` when zero; emits `assignment_changed` with `{action:"removed", userId, removedByEmail}`; NO auto-status reversal on last-assignee-removed). `PATCH /promo/api/promos/{id}/locations/{locationCode}` (super_admin / it; required `isComplete` boolean; PATCH stamps / clears `completed_{at,by}` together with the flag; 404 `location_not_on_promo` when no matching row; emits `location_marked_complete` or `location_marked_incomplete` with `{locationCode}`). **Auto-status advance.** Worker mirrors Brief 131's terminal-outcome auto-flip pattern — when `promotions.status === "Submitted"` AND assignee count ≥ 1 AND `ready_by_date IS NOT NULL` is true post-write, PATCH ticket and POST assignees auto-advance the promo to `Scoped` in the same write path and emit a second `status_changed` row with `{from:"Submitted", to:"Scoped", auto:true, trigger:"ticket_ready" \| "assignee_added"}`. The response carries `promoStatus: "Scoped"` so apps/web doesn't need a second GET to re-render the pipeline. Shared `logActivity` helper in `apps/promo-worker/src/handlers/_activity.ts` is the single insert point for `promo_activity_log` (fail-soft on insert error — activity log is observability, not correctness). Handlers in `apps/promo-worker/src/handlers/promo-writes.ts`; PostgREST direct fetch() with service-role key (Brief 154 pattern). | Completed (2026-06-05) | apps/web IT ticket detail page + live-view status controls (Brief 158); materials + PTP writes (Brief 156) are sibling, not dependent | [brief-155-promo-ticket-status-assignee-progress-writes.md](brief-155-promo-ticket-status-assignee-progress-writes.md) |
| 154 | Promotions — promo CRUD endpoints (list, create, detail): three operator-facing endpoints land on `splash-promo` — `GET /promo/api/promos` (any non-null `promoRole`; filters `status` / `priority` / `assigned_to_me` / `search` + `limit`/`offset`; returns `{promos[], total, limit, offset}` with PostgREST `Content-Range` total via `Prefer: count=estimated`), `POST /promo/api/promos` (super_admin / it / marketing only; CSRF gate; sequential PostgREST inserts promotions → promo_tickets → promo_locations → promo_activity_log with best-effort rollback DELETE on intermediate failure; 201 response mirrors detail shape), `GET /promo/api/promos/{id}` (any non-null `promoRole`; deep embed pulling ticket + assignees + locations + materials + ptp + activity (latest 20); `internal_note` stripped at the seam for non-IT callers as defense in depth; 404 on missing id or malformed UUID). Handlers in `apps/promo-worker/src/handlers/promos.ts`; PostgREST direct fetch() with service-role key (same pattern as `forms-worker/src/db/admin-forms.ts`). | Completed (2026-06-05) | apps/web pages for the promo feature (`/admin/promotions{,new,/[id]}`); ticket-update endpoints (Brief 155) layer on top of the detail response shape | [brief-154-promo-crud-endpoints.md](brief-154-promo-crud-endpoints.md) |
| 153 | Promotions — foundation: new `splash-promo` worker scaffolding (workers.dev only) + `PROMO_WORKER` service binding on apps/web + `@splash/types` `PromoRole` + `Session.promoRole` + `@splash/db-supabase` `gatePromoRole` helper + `getAuthContext` reads `promo_role` from `auth_unified` + PRE_DEPLOY_PROMO.md. Smoke endpoint `GET /promo/api/ping` returning the four binding flags is the only live route; promo CRUD, material upload, and announcement send deferred to Brief 154+. | Completed (2026-06-05) | Every subsequent promotions brief depends on this substrate | [brief-153-promo-worker-foundation.md](brief-153-promo-worker-foundation.md) |
| 1 | Login page + auth middleware | Completed (2026-05-04) | Both | (archive pending) |
| 2 | URL alignment + redirects + global Header + logout flow | Completed (2026-05-04) | Admin-facing | (archive pending) |
| 4 | Admin landing card grid (/admin/dashboard) + Sysadmin placeholder | Completed (2026-05-04) | Admin-facing | (archive pending) |
| 5 | Damage manager UI at /admin/damage (split into 5a/5b/5c/5d) | Completed (2026-05-04) — all four sub-briefs landed | Admin-facing | see sub-rows |
| 5a | Damage claim list page at /admin/damage | Completed (2026-05-04) | Admin-facing | [brief-005a-damage-claim-list.md](brief-005a-damage-claim-list.md) |
| 5b | Damage claim detail at /admin/damage/[id] (read-only) | Completed (2026-05-04) | Admin-facing | [brief-005b-damage-claim-detail.md](brief-005b-damage-claim-detail.md) |
| 5c | Damage write actions on detail (transitions, notes, check-request PDF preview) | Completed (2026-05-04) | Admin-facing | [brief-005c-damage-write-actions.md](brief-005c-damage-write-actions.md) |
| 5d | Damage documents (Quote/Receipt upload, edit, delete, photo lightbox) | Completed (2026-05-04) | Admin-facing | [brief-005d-damage-documents.md](brief-005d-damage-documents.md) |
| 6 | Performance tracker UI at /admin/performance | Completed (2026-05-04) | Admin-facing | [brief-006-performance-tracker.md](brief-006-performance-tracker.md) |
| 7 | Sysadmin UI at /admin/sysadmin (5 endpoints) | Completed (2026-05-04) | Admin-facing | [brief-007-sysadmin-ui.md](brief-007-sysadmin-ui.md) |
| 11 | Sign In links from auth-failed states | Completed (2026-05-04) | Admin-facing UX | [brief-011-signin-links.md](brief-011-signin-links.md) |
| 11a | User-info endpoint (header email/role display) + damage transition dc_role retrofit | Completed (2026-05-04) | Admin-facing UX (cosmetic) + 5d unblocker | [brief-011a-user-info-endpoint.md](brief-011a-user-info-endpoint.md) |
| 11b | Auth fixes from first end-to-end smoke test (remove isOriginAllowed gate from /api/me + sweep + dcRole diagnostic) | Completed (2026-05-04) | End-to-end damage manager + Header user-row verification | [brief-011b-auth-smoke-fixes.md](brief-011b-auth-smoke-fixes.md) |
| 12 | Fill in / page (auth-aware redirect to /admin/dashboard or /login based on cookie presence) | Completed (2026-05-06 — Brief 50) | Admin-facing UX | [brief-050-signup-flow-ownership-and-root-redirect.md](brief-050-signup-flow-ownership-and-root-redirect.md) |
| 13 | apps/web wrangler routes block | Not started | Both (cutover-time) | (not drafted) |
| 15 | PRE_DEPLOY_WEB.md | Completed (2026-05-06 — Brief 51) | Both (cutover prep) | [brief-051-pre-deploy-web-runbook.md](brief-051-pre-deploy-web-runbook.md) |
| 16 | Staging subdomain end-to-end testing (`staging.splashcarwashes.info`) | Completed (2026-05-04) | End-to-end UI verification | [brief-016-staging-subdomain.md](brief-016-staging-subdomain.md) |
| 17 | Service bindings for apps/web -> worker subrequests (workaround CF same-zone 522 gotcha) | Completed (2026-05-04) | End-to-end UI testing on staging + production cutover | [brief-017-service-bindings.md](brief-017-service-bindings.md) |
| 18 | Damage actions debug + sysadmin email-based UserPicker (drop dcRole filter while Brief 11b mystery is being chased; new `/sysadmin/api/users` GET endpoint + UserPicker; `location_admin` location_code guards) | Completed (2026-05-04) | Day-to-day usability of damage manager + sysadmin UI | [brief-018-damage-and-sysadmin-fixes.md](brief-018-damage-and-sysadmin-fixes.md) |
| 19 | Server-action ActionResult + router.refresh() pattern (replace redirect-based UX on damage detail + sysadmin; shared `<ActionForm>` client wrapper; CLAUDE.md doc of the pattern) | Completed (2026-05-04) | Day-to-day usability of damage manager + sysadmin write surfaces (visible refresh after action) | [brief-019-action-result-refresh.md](brief-019-action-result-refresh.md) |
| 20 | Staging-test bug batch — sysadmin idempotency (changed flag + no-op audit), damage transition clearApprovalDetails, ApprovalDetails null gate, no-quotes hint with #upload-document anchor, Quote-row required field validation (worker + UI), edit `<details>` close-on-save, defensive try/catch on editDocumentAction | Completed (2026-05-05) | Day-to-day usability of damage manager + sysadmin (8 bugs surfaced by first end-to-end staging smoke test) | [brief-020-staging-bug-batch.md](brief-020-staging-bug-batch.md) |
| 21 | dcRole gating restored (show-disabled pattern with per-role hint copy) + Brief 18 diagnostic cleanup (DcRoleDebugLine + log helpers removed) + contact_status "Not Started" pill suppressed | Completed (2026-05-05) | Damage detail UX correctness + production-ready polish | [brief-021-dcrole-gating-cleanup.md](brief-021-dcrole-gating-cleanup.md) |
| 22 | Recent notes sub-box on damage detail + jump-to-add-note anchor button + smooth scroll | Completed (2026-05-05) | Damage detail UX polish | [brief-022-recent-notes-card.md](brief-022-recent-notes-card.md) |
| 23 | Public customer claim form ported to damage-worker (`GET /claims/{slug}` + `GET /claims/{slug}/thanks` + dual-mode `POST /claims-api/submit-claim`) | Completed (2026-05-05) | Customer-facing cutover (decision 9) | [brief-023-customer-claim-form.md](brief-023-customer-claim-form.md) |
| 24 | Sysadmin Add Location (pricing_simple bulk insert) — atomic Supabase REST array POST, 7-row standard package picker, audit log + cache-invalidation TODO | Completed (2026-05-05) | Direct pricing_simple management from sysadmin UI (first of 3) | [brief-024-sysadmin-add-location.md](brief-024-sysadmin-add-location.md) |
| 25 | Customer claim form polish — PIN gate (1981) + multi-photo widget + equipment yes/no toggle + Managers-only pill + JS-driven submit with outcome card + `vehicleYear` required + drop `capture` attribute | Completed (2026-05-05) | Customer-facing claim flow usability | [brief-025-claim-form-polish.md](brief-025-claim-form-polish.md) |
| 26 | Sysadmin Update Package (pricing_simple — search-by-location_code + per-row edit, primarily pkg$/single, also pkg/sort/other; `ilike` search, no SQL UI) | Completed (2026-05-05) | Direct pricing_simple management from sysadmin UI (second of 3) | [brief-026-sysadmin-update-package.md](brief-026-sysadmin-update-package.md) |
| 27 | Sysadmin Update Locations (locations table fields — triggers propagate am_email / area_manager / rm_email / regional_manager / site_email into pricing_simple and auth) | Completed (2026-05-05) | Direct pricing_simple management from sysadmin UI (third of 3) | [brief-027-sysadmin-update-location.md](brief-027-sysadmin-update-location.md) |
| 28 | Cross-worker pricing cache invalidation (signup-worker `pricing_simple_resolved` cache buster) — flagged TODO three times now (Briefs 24, 26, 27); sysadmin-side cache-clear button + signup-worker invalidation hook | Not started | Eliminate the up-to-5-minute lag between sysadmin pricing edits and customer signup form | items 24, 26, 27 |
| 29 | Tab title "Splash Tools" + Add Location writes `locations` row + accept `address` field + claim form `Submitting...` overlay clears on success | Completed (2026-05-05) | Three post-test polish items (cosmetic + Update Location dependency + customer-facing UX) | [brief-029-tab-title-add-location-row-claim-overlay.md](brief-029-tab-title-add-location-row-claim-overlay.md) |
| 30 | Sysadmin UX overhaul — two-mode hub (Manage Users / Manage Tables) replaces flat 8-card list; PageBanner blurb dropped; new GET `/sysadmin/api/audit-log` + filterable Activity log panel (actor / action / table / user / location_code) | Completed (2026-05-05) | Sysadmin page legibility + observability (audit log was previously only mentioned, never surfaced) | [brief-030-sysadmin-mode-hub-and-audit-log.md](brief-030-sysadmin-mode-hub-and-audit-log.md) |
| 31 | Server-action ID stability across deploys: `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` build-time env + `apps/web/app/admin/error.tsx` boundary catches `UnrecognizedActionError` and prompts reload (root cause + defense-in-depth for the 2026-05-05 grant-tool white-page) | Completed (2026-05-05) | Eliminate stale-tab white-page on apps/web redeploys | [brief-031-server-action-id-stability.md](brief-031-server-action-id-stability.md) |
| 32 | Customer claim form: email required + auto-generated claim summary PDF (Splash branded, programmatic pdf-lib layout, R2-stored at `claims/<id>/summary.pdf`) + new `CUSTOMER_CLAIM_WEBHOOK_URL` so Power Automate can email the customer their copy | Completed (2026-05-05) | Customer-facing claim flow closes the loop (customer gets a copy of every claim) | [brief-032-claim-summary-pdf-and-customer-email.md](brief-032-claim-summary-pdf-and-customer-email.md) |
| 33 | Drop D1 locations table — damage-worker resolves `/claims/{slug}` via Supabase pricing_simple (full Path A: single source of truth, dual-store drift problem eliminated). Audit confirmed only `location_code` + `location_pretty` are consumed, so dormant D1 columns can be deleted with the table | Completed (2026-05-05) | Eliminate the dual-store sync burden between D1 and Supabase locations | [brief-033-drop-d1-locations-supabase-authoritative.md](brief-033-drop-d1-locations-supabase-authoritative.md) |
| 34 | Audit log column rename: `created_at` → `occurred_at` in sysadmin-worker + apps/web AuditLogPanel. Brief 30 assumed wrong column name; live table uses `occurred_at`. Three references in sysadmin-worker + two in AuditLogPanel | Completed (2026-05-05) | Audit log panel currently 500s on load — Brief 30's UX deliverable is broken until this lands | [brief-034-audit-log-occurred-at-column-fix.md](brief-034-audit-log-occurred-at-column-fix.md) |
| 35 | Claim summary PDF trim: drop photo thumbnails (entire section + R2 fetch path) and drop `(#location_code)` from the location header. Brief 32 layout is too verbose for customer-facing distribution | Completed (2026-05-05) | Customer-facing PDF cleanup — slimmer artifact, no internal slug leak | [brief-035-claim-pdf-drop-photos-and-code.md](brief-035-claim-pdf-drop-photos-and-code.md) |
| 36 | Test batch (3 parts): A) PDF determination + equipmentRelated humanizer (`no_responsibility` → "No Responsibility"); B) investigate + fix mobile quote-upload digest `924441341@e394` (server-side render exception, reproducible across testers); C) Update Package multi-select rewrite — pick location, multi-select packages, inline-edit pkg$/single/sort across the selection (drops rename/flash/pricing-mode fields from the bulk editor) | Completed (2026-05-05) — Parts A + C land as specified; Part B's defense-in-depth try/catch on uploadDocumentAction landed but the CF-logs root-cause investigation is deferred to operator (headless cannot read CF dashboard logs) | Three test findings from 2026-05-05: PDF customer polish, mobile upload bug, sysadmin bulk-edit ergonomics | [brief-036-test-batch-pdf-humanize-mobile-upload-multi-pkg.md](brief-036-test-batch-pdf-humanize-mobile-upload-multi-pkg.md) |
| 37 | Mobile quote upload — port the legacy worker's working code path (operator hit the same iPhone bug, then accidentally uploaded successfully via legacy `info-signup-worker`); bypass apps/web's Next server action by posting the form directly to damage-worker; replicate legacy's HEIC handling. Plus add a permanent "Add Document" anchor button next to "Add Note" on the claim detail page (smooth-scrolls to upload card) | Completed (2026-05-06) | Mobile upload was broken after Brief 36 Part B's defense-in-depth — root cause is the path through Next server actions, legacy works because it doesn't go through Next | [brief-037-mobile-upload-legacy-port-plus-add-doc-anchor.md](brief-037-mobile-upload-legacy-port-plus-add-doc-anchor.md) |
| 38 | Mobile upload REDO — verify Brief 37's actual landed state (Brief 37 was dispatched with a bogus "if `legacy/damagemanager.js` not in repo, ask operator to paste" hedge before that hedge was corrected; the file IS in the repo). Read the legacy upload handler from `legacy/damagemanager.js` directly, compare against current new-code state, fill any gaps Brief 37 left | Completed (2026-05-06) — Category A: Brief 37 executed Phase 1/2/3 in full with correct legacy line citations; Brief 38's audit confirmed the port is faithful and applied no patches | Brief 37 may have punted Phase 1 due to the bogus hedge; this brief audits + completes whatever it left undone, and re-grounds future briefs in "the legacy/ directory IS in the repo and readable" | [brief-038-mobile-upload-redo-with-correct-legacy-paths.md](brief-038-mobile-upload-redo-with-correct-legacy-paths.md) |
| 39 | Sysadmin Set Role — replace the free-text `location_code` input with a searchable picker. Search by site (varchar), location_code, or location_pretty substring; selection writes the canonical location_code to the hidden form input. Mirrors UserPicker pattern. New `GET /sysadmin/api/pricing-simple/locations?q=` endpoint added (Brief 36 Part C used client-side dedupe; this endpoint returns server-side distinct rows) | Completed (2026-05-06) | Operator can't reliably remember/type 67 location_code slugs (especially with the underscore-vs-no-separator drift); typeahead matches the existing UserPicker UX | [brief-039-set-role-location-code-picker.md](brief-039-set-role-location-code-picker.md) |
| 40 | Sysadmin Create User — same `LocationCodePicker` extension as Brief 39, applied to the Create User card (and any other free-text location_code inputs the executor finds in the user-mgmt cards). One-line wiring per card; reuses Brief 39's component + endpoint | Completed (2026-05-06) | Same UX problem as Brief 39's Set Role; operator wants both cards covered | [brief-040-create-user-location-code-picker.md](brief-040-create-user-location-code-picker.md) |
| 41 | Public claim form — Damage Type selector in the employee section (11-value allow-list with "Other" → free-text reveal); two new D1 columns `damage_type` + `damage_other` (nullable for back-compat); manager detail page renders Damage Type row above Equipment Involved; SharePoint webhook payload picks up the new fields automatically | Completed (2026-05-06) | Brief 42 (MaintainX work order title `Damage Claim - {location_pretty} - {damage_type}`) | [brief-041-claim-form-damage-type-selector.md](brief-041-claim-form-damage-type-selector.md) |
| 42 | MaintainX work order on `equipment_related=1` — new `apps/damage-worker/src/maintainx.ts` `createMaintainXWorkOrder` helper, `MAINTAINX_MODE` test/production assignee switch (Josh in test, Brett+Scott in production), Supabase two-step `getMaintainXLocationId` lookup (`pricing_simple.location_code` → `site` → `locations.maintainx_id`), new `claims.maintainx_workorder_id INTEGER NULL` D1 column + `updateMaintainXWorkOrderId` helper, hooked into `handleClaimSubmission` between writeClaimBatch and the PA POST. Activity-log entries written via the existing `note` activity_type with `[maintainx]` prefix (D1 CHECK constraint blocks new activity_type values). Manager detail page renders WO ID as a link to `app.getmaintainx.com/workorders/{id}` (URL pattern unverified) or a yellow "MaintainX work order not created — see activity log" warning when `equipment_related=1` and `maintainx_workorder_id IS NULL`. Empirical probe deferred to operator (headless cannot exercise live MaintainX); helper is defensive and tries `body.id`, `body.workOrder.id`, `body.data.id` for the WO ID extract | Completed (2026-05-06) | Brief 43 (GM-side modal for setting equipment_related=yes during post-submit quote/in-house determination — reuses `createMaintainXWorkOrder` + dedupe via `claims.maintainx_workorder_id`) | [brief-042-maintainx-workorder-on-equipment-related.md](brief-042-maintainx-workorder-on-equipment-related.md) |
| 43 | GM/RM equipment-related modal on Approve transitions — apps/web `EquipmentOverrideSubmit` client component (modal-gated submit button on the two active-repair approval transitions when `claim.equipment_related === 0`); modal asks "Was this damage equipment related?", reveals an equipment_piece dropdown on Yes (6 options hard-forked from the worker's `EQUIPMENT_CHOICES`), appends `override_equipment_related` + `override_equipment_piece` hidden inputs to the existing transition form and re-submits via `form.requestSubmit()`. Worker `handleStatusTransition` now reads the override fields, validates (yes-only on the two target statuses, only no→yes flips, equipment_piece required + ≤200), folds `equipment_related = 1` + `equipment_piece = ?` into the same UPDATE that lands the status change, then post-batch fires `tryCreateMaintainXIfMissing` — re-reads `claims.maintainx_workorder_id` and short-circuits when non-null (defense-in-depth dedupe), constructs a transient post-UPDATE `ClaimRow`, calls `createMaintainXWorkOrder` with the same fail-soft posture as Brief 42 (8s AbortController timeout, helper never throws, `[maintainx]` activity log on success/failure). Worker response now optionally includes `maintainx_attempted` + `maintainx_ok` flags; `transitionAction` surfaces a "Status updated to X, but the MaintainX work order couldn't be created — see activity log" message on the failure path. Override side-effect annotated on the same `status_change` activity row via a `[Equipment override]` sentinel suffix in the notes | Completed (2026-05-06) | Nothing | [brief-043-gm-equipment-related-modal-on-approve.md](brief-043-gm-equipment-related-modal-on-approve.md) |
| 45 | Portal the EquipmentOverrideModal to fix nested-form bug — Brief 43's modal renders its own `<form onSubmit={handleConfirm}>` inside the parent transition `<form>` because the submit button (which has to live in the parent form for `submitButton.form` to resolve) and the modal block are siblings under a React fragment. Nested-form HTML is invalid; browser recovery is non-deterministic, and operator confirmed 2026-05-06 that the modal's Confirm only triggers the underlying transition POST ~1/5 times. Fix is a one-file change: render the modal `<div role="dialog">…</div>` through `createPortal(…, document.body)` so the dialog DOM lives outside the parent form, leaving the submit button + parent form untouched and `form.requestSubmit(submitButton)` working consistently. SSR-guarded via `typeof document !== "undefined"`. Comment block at the top of `EquipmentOverrideModal.tsx` extended to explain the portal rationale so a future reader doesn't "simplify" it back into the inline conditional | Completed (2026-05-06) | Reliable equipment-related override submission from /admin/damage/[id] | [brief-045-equipment-modal-portal-fix.md](brief-045-equipment-modal-portal-fix.md) |
| 46 | MaintainX assignee `type: "USER"` field fix — Brief 42's executor encoded assignees as `[{ id: <id> }]` matching the brief's documented (incomplete) sample, but MaintainX's API actually requires every assignee object to include `type: "USER"`. Both customer-submission (Brief 42 path) and GM-modal override (Brief 43 path) WO creation 400'd on 2026-05-06 with `assignees.0.type` fieldPath. One-line fix in `apps/damage-worker/src/maintainx.ts`: extend the `ASSIGNEES_PRODUCTION` and `ASSIGNEES_TEST` const arrays to include `type: "USER"` on each entry, and narrow `assigneesByMode`'s return type to `ReadonlyArray<{ type: "USER"; id: number }>`. Also patches the docs gap that caused the bug: brief-042's JSON example + Phase 3.2 bullets + CLAUDE.md's MaintainX integration entry now document the `type: "USER"` requirement so a future executor doesn't reproduce it | Completed (2026-05-06) | Both MaintainX work-order creation paths (Brief 42 customer-submit + Brief 43 GM override) actually succeed | [brief-046-maintainx-assignee-type-field.md](brief-046-maintainx-assignee-type-field.md) |
| 47 | Customer claim-summary PDF — trim Staff Assessment block to three rows only (Submitted By, Determination, Splash Response), drop equipment_related/equipment_piece/damage_type/damage_other/preexisting_damage/customer_demeanor from the customer copy, and rename "What the Customer Was Told" → "Splash Response" on the customer PDF only (admin UI label unchanged). Surgical edit in `apps/damage-worker/src/render/claim-summary-pdf.ts` plus deletion of the now-dead `equipmentRelated` field on `ClaimSummaryPdfInput.assessment` and its corresponding ternary at the call site in `apps/damage-worker/src/index.ts`. Manager detail page on `/admin/damage/[id]` is intentionally NOT modified — managers still see the full assessment | Completed (2026-05-06) | Power Automate webhook setup for `CUSTOMER_CLAIM_WEBHOOK_URL` (operator paused that to land this PDF cleanup first) | [brief-047-claim-pdf-trim-staff-assessment-rename-splash-response.md](brief-047-claim-pdf-trim-staff-assessment-rename-splash-response.md) |
| 48 | Customer claim webhook — add `site_email` (string or null) to the `CUSTOMER_CLAIM_WEBHOOK_URL` JSON payload so Power Automate can wire customer confirmation-email replies to the per-location inbox via the Reply-To header. New sibling helper `getLocationContactInfo(env, locationCode)` in `@splash/db-supabase/locations` (slug → `pricing_simple.site` → `locations.site_email`, two-step like Brief 42's `getMaintainXLocationId`); fail-soft both at the helper level (any throw / non-2xx / missing row → `{ site_email: null }`) and at the damage-worker call site (try/catch around the lookup defaults to null). Webhook payload extended with the new field; `fireCustomerClaimWebhook` signature gains a `siteEmail: string \| null` parameter. CLAUDE.md `CUSTOMER_CLAIM_WEBHOOK_URL` entry updated to document the new field and Reply-To semantics. No schema change, no new secret, no new env var | Completed (2026-05-06) | Power Automate flow's Reply-To wiring (operator-side follow-up: add `site_email` to PA trigger schema, set Send Email action's Reply-To to that dynamic value) | [brief-048-customer-webhook-add-site-email.md](brief-048-customer-webhook-add-site-email.md) |
| 49 | Simplify `getLocationContactInfo` — Brief 48's two-step `pricing_simple.site` → `locations.site_email` join was returning null for at least the Oswego location even though both rows had `site_email` populated; the slug → `site` step doesn't cleanly match `locations.site` (likely the `trg_sync_pricing_simple` trigger writes pricing_simple.site from a different source column than assumed, e.g. `site_number::text`). Fix: drop the locations-side fetch entirely and read `pricing_simple.site_email` directly. The trigger keeps pricing_simple's value eventually consistent with locations (and Brief 26 explicitly rejects direct PATCHes to `pricing_simple.site_email` because of that trigger), so the read is equivalent. Single-file change in `packages/db-supabase/src/locations.ts`; all fail-soft branches preserved (slug regex, fetch throw, non-2xx, empty result, null/empty → null) | Completed (2026-05-06) | Customer confirmation-email Reply-To header actually populating for locations whose `site_email` is set | [brief-049-getlocationcontactinfo-read-from-pricing-simple.md](brief-049-getlocationcontactinfo-read-from-pricing-simple.md) |
| 50 | Signup customer flow ownership decision (item 8) + root `/` redirect (item 12). Decided: signup-worker `splash-signup-next` keeps `/signup/{location}`; apps/web placeholder retired (mirrors Brief 23's "customer forms live in their owning worker" precedent for damage claims). Root `/` page rewritten as a server-component auth-aware redirect — reads `sb-access-token` cookie, 307s to `/admin/dashboard` (cookie present) or `/login` (absent). One deploy; closes the last two customer-facing-route audit items. CLAUDE.md gains a customer-facing-routes ownership callout under constraint #1; pages-built table reorganized into Real / Placeholder / Redirect-only categories | Completed (2026-05-06) | Customer-facing cutover (closes audit items 8 + 12 — the last open ownership decisions) | [brief-050-signup-flow-ownership-and-root-redirect.md](brief-050-signup-flow-ownership-and-root-redirect.md) |
| 51 | PRE_DEPLOY_WEB.md runbook for apps/web (item 15). Pure documentation — closes the gap where the five worker docs each had a `PRE_DEPLOY_<NAME>.md` but apps/web didn't. Doc consolidates every apps/web-specific deploy-time consideration learned across Briefs 1–50: CF Workers Builds wiring, the `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` build-time env var (Brief 31), the five `[[services]]` bindings (Brief 17), the `<ActionForm>` server-action contract (Brief 19), the modal-portal pattern (Brief 45), the cookie-based middleware Edge-runtime constraint (Brief 1 + 50), CF edge propagation lag, customer-route ownership (Brief 50 + 23), service-binding `Origin`-header requirement, rollback procedures (git revert vs. dashboard rollback), and the cutover-day routes-block flip + `NEXT_PUBLIC_*_WORKER_URL` removal. Also updates CLAUDE.md's "Read these every session" list to mention the new sixth doc | Completed (2026-05-06) | Cutover prep (operator now has a deploy runbook for apps/web) | [brief-051-pre-deploy-web-runbook.md](brief-051-pre-deploy-web-runbook.md) |
| 52 | Stop stripping `_` from sysadmin search query sanitization — latent defect from Brief 26's `handleSearchPricingSimple` (and its Brief 39 sibling `handleSearchPricingSimpleLocations`) regex `/[%_,()*]/g` was corrupting any query containing a literal underscore (e.g., `batavia_ii` → `bataviaii`), breaking the auto-fetch step in Brief 36's `UpdatePackageCard` for the majority of locations. Fixed by dropping `_` from the strip set in both handlers (now `/[%,()*]/g`); literal `_` matched as ILIKE single-char wildcard is behaviorally equivalent for substring search. Inline docblocks updated. Two other strip-set sites (`handleSearchLocations`, audit-log actor filter) explicitly out of scope per the brief | Completed (2026-05-06) | Update Package card was 0-rows for any underscored location_code; Set Role / Create User location pickers were symptomatically affected | [brief-052-sysadmin-search-underscore-sanitization.md](brief-052-sysadmin-search-underscore-sanitization.md) |
| 53 | Audit-log diff expansion moves from inside the cramped DIFF cell (~30-40px wide) to a full-width sibling `<tr colSpan={5}>` underneath the primary row. Adds a tiny `"use client"` `AuditRowExpandable` component that holds per-row `useState<boolean>`; `AuditLogPanel.tsx` server-side keeps relativeTime / TargetCell / DiffBlock rendering and passes the four primary cells + the diff content as React-node props. Side-by-side BEFORE/AFTER `<pre>` blocks (single full-width when only one side has data) capped at `max-h-96` so big diffs don't push the page. Toggle button carries `aria-expanded` and the arrow flips ▶/▼. Multiple rows expand independently. No worker change, no API change | Completed (2026-05-06) | Operator readability of large `update_packages_bulk` diffs (e.g., williamsville) — previously rendered as a vertical column of single characters | [brief-053-audit-log-diff-full-width-expansion.md](brief-053-audit-log-diff-full-width-expansion.md) |
| 54 | `handleUpdatePackagesBulk` audit-log entry now captures a real `before` snapshot via a single `pkg=in.(...)` GET against `pricing_simple` (selecting `pkg`, `pkg$`, `single`, `sort` — only the fields the bulk endpoint can touch) issued between the per-entry validation loop and the per-row PATCH loop. `before` payload shape mirrors `after`'s `location_code`+list-of-rows pattern: `{ location_code, rows: [{pkg, "pkg$", single, sort}, …] }`. The fetch is fail-soft (try/catch + before:null fallback) so audit-log degradation never blocks the bulk update. Closes the gap Brief 53's full-width diff panel surfaced — bulk entries now render BEFORE/AFTER side-by-side instead of AFTER-only. New `BulkBeforeRow` interface; docblock above the handler updated to describe the new audit shape | Completed (2026-05-06) | Operator can see WHAT CHANGED in a bulk-edit audit-log row, not just WHAT WAS SUBMITTED | [brief-054-update-packages-bulk-before-snapshot.md](brief-054-update-packages-bulk-before-snapshot.md) |
| 55 | Hide the "Was there an equipment malfunction?" checkbox + label on the public customer claim form by HTML-commenting the markup in `apps/damage-worker/src/render/claim-form.ts` (~L493-L496). The hidden input (`name="equipmentMalfunction"`, default `value="false"`), the form-parse line in `handleClaimSubmission`, the `ClaimSubmissionPayload.equipmentMalfunction: boolean` type, and the PA webhook payload (`JSON.stringify(claimData)`) are all preserved unchanged so the field continues to round-trip through the worker → Power Automate → SharePoint path with its legacy default of `false`. Inline `eqMalToggle`/`eqMalHidden` script lookups stay in place — the existing null-guards already make the script tolerate a missing checkbox — with a Brief 55 comment marker explaining why the dead lookups are still there. Two-step revert path documented inline so a future reader can un-comment the markup block + the handler picks it up automatically. Operator decision 2026-05-06: defer "promote malfunction to a real field (D1 column + admin display + filter)" to a future brief; this brief just removes the UI noise | Completed (2026-05-06) | Form polish — operator decided 2026-05-06 the checkbox isn't doing useful work today (not persisted in D1, not on customer PDF, not surfaced in admin manager detail; only reaches the unverified PA SharePoint write) | [brief-055-hide-equipment-malfunction-checkbox.md](brief-055-hide-equipment-malfunction-checkbox.md) |
| 56 | Rename "Pricing Admin" → "Signup Admin" (umbrella) with two sub-views accessed via a tab nav: Pricing (existing `/admin/pricing` + `/admin/pricing/{loc}`) and Signups (new `/admin/signups` + `/admin/signups/{loc}`). New signup-worker endpoint `GET /admin/api/locations/{loc}/signups?days=N` (`days ∈ {1, 7, 30}`, default 7) reads the most recent 200 rows from `maxpass_signups` via direct PostgREST GET; auth gate verbatim-replicated from the per-location pricing endpoint (`adminGate` + `userCanAccessLocation`, both now exported from `admin-pricing.ts`). New apps/web `getSignupsForLocation` helper added to the existing pricing `_lib/worker-fetch.ts` (reuses the SIGNUP_WORKER service-binding transport). New shared `<SignupAdminTabs>` component mounts above all four pages. URL `/admin/pricing/*` is unchanged for operator bookmark stability; tabs handle cross-view flips. Dashboard tile relabels to "Signup Admin"; H1s on pricing pages flip to "Signup Admin · Pricing". Middleware allow-list extended with `signups` so `/admin/signups/{loc}` doesn't 308-redirect through the legacy `/admin/{slug}` → `/admin/pricing/{slug}` rule | Completed (2026-05-06) | Operator visibility into recent customer signups per location ("did the marketing push pull anyone in this week?") without leaving apps/web | [brief-056-signup-admin-rename-and-signups-viewer.md](brief-056-signup-admin-rename-and-signups-viewer.md) |
| 57 | "Fill Form Again?" success-overlay button on `/signup/{location}/{pkg}` now `window.location.href = '/signup/{locationCode}'` (full navigation back to the package picker) instead of clearing phone/email/agree in-place. One-handler edit at `apps/signup-worker/src/render/form.ts:293-301`; `escHtml(locationCode)` server-interpolated at template render time. Reasoning: multi-signup sessions are usually multi-vehicle households where each vehicle picks its own package, so the next signup should land on the picker, not the same package's form | Completed (2026-05-06) | Customer UX after a successful signup — operator wants the next signup attempt to start from the package picker | [brief-057-fill-again-redirect-to-picker.md](brief-057-fill-again-redirect-to-picker.md) |
| 58 | Pricing pages (`/admin/pricing`, `/admin/pricing/{loc}`) converted from inline-style left-aligned `<section style={{ padding: 24, maxWidth: N }}>` wrappers to centered Tailwind (`<section className="mx-auto w-full max-w-[N] px-5 py-9">`) — matches the convention already in use on sysadmin / damage / performance / signups. Width mapping: 520px for auth/error states (collapsed from inconsistent 480/520), 820px for landing main view (was 720; matches sysadmin), 880px preserved for per-location main view (pricing-grid canvas). Two files touched (5 `<section>` wrappers); no worker change, no API change, no schema change | Completed (2026-05-06) | Visual consistency across the four `/admin/*` sections — flipping between the Pricing and Signups tabs on the same per-location URL no longer "jumps" sideways | [brief-058-pricing-pages-center-align.md](brief-058-pricing-pages-center-align.md) |
| 59 | Damage list filters by Regional Director / Regional Manager + new Reporting tab — adds AM/RM contact-roster lookup (new `listContactRoster` helper in `@splash/db-supabase`, new `GET /manage/api/contact-roster?role=` worker endpoint), extends `getClaimsList` and `ClaimsListFilters` with `regional_director_email` / `regional_manager_email` / `submitted_from` / `submitted_to` params, adds 4 new filter inputs on `/admin/damage` (RD/RM dropdowns + 2 date pickers), introduces a new `/admin/damage/reporting` page with sticky anchor nav (Overview / By Location / By Damage Type), 5 window presets (current month / past month / QTD / past quarter / YTD; default QTD), KPI tiles, by-location table, and damage-type breakdowns. New `GET /manage/api/reporting?...` endpoint runs a single D1 batch of count + cost aggregates; cost = sum of `claim_photos.amount` for `Quote` and `Receipt` rows on Approved-family claims (`Approved —*`, `Closed — Paid`, `Closed — Approved/No Response`). Limitation: claims with both a quote and a receipt double-count (v2 candidate). dc_role scoping holds across both filter and report paths — RD/RM dropdowns are intersected with the user's dcLocations. New shared `<DamageTabs>` component (Claims / Reporting pill nav) above both pages; detail page intentionally untabbed | Completed (2026-05-06) | Operator can slice claims by who's accountable and surface aggregate trends without leaving apps/web | [brief-059-damage-am-rm-filters-and-reporting-tab.md](brief-059-damage-am-rm-filters-and-reporting-tab.md) |
| 60 | Reporting page "By Damage Type" gains a third Open column — new `by_damage_type_open` query in damage-worker's `GET /manage/api/reporting` handler (sibling to existing Approved + Denied breakdowns; filter is `lifecycle_state = 'Open'` matching the KPI tile + per-location pivot), apps/web reporting page converted from `md:grid-cols-2` to `md:grid-cols-3` with Open / Approved / Denied order so mobile single-column stack surfaces "what's coming in" first. Existing `DamageTypeTable` helper reused for all three columns. No schema change, no new env vars, no new endpoints | Completed (2026-05-06) | Operator visibility into the in-flight pipeline by damage type, not just terminal outcomes | [brief-060-reporting-add-open-damage-type-breakdown.md](brief-060-reporting-add-open-damage-type-breakdown.md) |
| 61 | Sysadmin "Set DC Role" tool — sixth Manage Users card writes the damage-claim permission domain (`damage_claim_user_roles` + `damage_claim_user_locations`) atomically. New `POST /sysadmin/api/users/{userId}/dc-role` super_admin-gated endpoint; new `setDcRole(client, args)` helper in `@splash/db-supabase` with before/after snapshot return for audit; new `<LocationCodeMultiPicker>` (sibling to Brief 39's single-select picker) emitting repeated hidden inputs that `formData.getAll()` reads as `string[]`; new `<SetDcRoleCard>` mounted after Set Role on `/admin/sysadmin?mode=users`. Audit-log allow-lists extended (`set_dc_role` action + `damage_claim_user_roles` target_type + audit-log user filter target-type CSV). For super_admin/admin the locations write is skipped (those roles bypass scoping); switching from gm/rm to super_admin/admin/null always wipes existing dc_locations rows to prevent stale-data leakage. Closes the gap that left every sysadmin-created/role-changed user with NO `/admin/damage` access by default | Completed (2026-05-07) | Operators can grant DC access through the sysadmin UI instead of direct SQL | [brief-061-sysadmin-set-dc-role-tool.md](brief-061-sysadmin-set-dc-role-tool.md) |
| 62 | Fix `getMaintainXLocationId` two-step join — Brief 42's helper queried `locations.site=eq.<value>` after resolving `pricing_simple.location_code` → `site` (text), but `locations.site` is the location name (e.g., "Oswego") while `pricing_simple.site` is the denormalized site_number text (e.g., "147") populated by `trg_sync_pricing_simple` from `locations.site_number::text`. Step-2 always returned zero rows; helper fell through to its fail-soft branch and returned null; consumer (`apps/damage-worker/src/maintainx.ts:133`) omitted `body.locationId`; every WO created since Brief 42 shipped without Location attribution. Same bug class Brief 49 fixed for `getLocationContactInfo`. One-line fix: change the URL search-param key from `site` to `site_number` in the locations-side query. Docblock + inline step-2 comment updated to call out the join-key correction and cross-reference Brief 49. CLAUDE.md MaintainX integration block extended with the join-key note. Out-of-scope: backfilling Location on existing WOs (148490, 148492, 148496, 148512, …) — manual MaintainX API script if finance asks; `getMaintainXLocationId` doesn't get refactored to a single pricing_simple query because `pricing_simple` doesn't carry `maintainx_id` | Completed (2026-05-07) | Both MaintainX paths (Brief 42 customer-submit + Brief 43 GM override) finally carry the correct Location attribution on new WOs | [brief-062-getmaintainxlocationid-fix-join-key.md](brief-062-getmaintainxlocationid-fix-join-key.md) |
| 63 | Add `[observability.logs] enabled = true / invocation_logs = true` block to all 6 wrangler.toml files (5 workers + apps/web) so the CF dashboard's "Logs → Enabled" toggle stays sticky across push-triggered deploys. Without the declarative block, CF Workers Builds re-provisions the worker config from `wrangler.toml` on every deploy and resets observability to its default-off state — operator was manually flipping the toggle back on after each push. Mechanical edit, identical block in all 6 files, each carrying a `# Brief 63 (2026-05-07):` comment marker for grep-ability. CLAUDE.md "Working with workers" section gains a one-line rule that every worker's wrangler.toml must include the block. No worker-code change, no schema change, no env var change | Completed (2026-05-07) | Operator stops having to manually flip "Workers Logs → Enabled" after every CF Workers Builds push | [brief-063-wrangler-observability-logs.md](brief-063-wrangler-observability-logs.md) |
| 64 | `setDcRole` (Brief 61) was modeled after `setRole` and assumed `damage_claim_user_roles` carried an `email` column — it does not (table is `(user_id, dc_role)`; email lives on `auth.users` and is joined by the `auth_unified` view at read time). Every Set DC Role submission was 500ing with Postgres 42703 (`column damage_claim_user_roles.email does not exist`; CF requestId `5ZAAKPF23IPE2KMG`). Fix removes `email` from the upsert payload AND from the two `.select("user_id,email,dc_role")` calls (before-snapshot read + upsert RETURNING), and drops `email` from the `DamageRoleRow` interface. Function signature still takes `email: string` (callers continue to pass `session.email` for the audit-log path). Sysadmin-worker `handleSetDcRole` docblock + inline comment updated to drop the bogus "damage_claim_user_roles.email is NOT NULL" claim. CLAUDE.md sysadmin section gains a one-line schema note that future helpers touching `damage_claim_user_roles` / `damage_claim_user_locations` must NOT model their writes after `user_permissions` | Completed (2026-05-07) | Brief 61's "Set DC Role" tool was 500ing on every submission; operator could not grant or modify dc_role through the new tool | [brief-064-set-dc-role-drop-email-column.md](brief-064-set-dc-role-drop-email-column.md) |
| 65 | Daily open-claims summary cron — bolted onto damage-worker (`{ fetch, scheduled }` default export; `[triggers] crons = ["0 13 * * *"]` in wrangler.toml — 13:00 UTC = 8 AM ET). Two new helpers in `@splash/db-supabase/src/summary.ts`: `listSummaryRecipients` (gm/rm/admin/super_admin from auth_unified) and `fetchLocationRoster` (per-location RD/RM contacts from pricing_simple, returned as Map<location_code, …>). The scheduled handler builds a per-recipient `DigestPayload` with hierarchical RD → RM → location → claims structure (admin/super_admin unrestricted; gm/rm filtered to dc_locations); skip-on-empty (zero open claims = no email); per-user errors swallowed with logging; final batch summary log. POSTs each digest to a new optional `DAILY_SUMMARY_WEBHOOK_URL` secret (15s timeout; fail-soft when unbound — same posture as `CUSTOMER_CLAIM_WEBHOOK_URL`). Operator follow-up: bind the secret via wrangler + build the PA flow (HTTP request → email) per Phase 4 of the brief. In-code `SUMMARY_DC_ROLES = ["gm","rm","admin","super_admin"]` constant gives a single edit point for opt-out at the role level; per-user opt-in deferred to v2 | Completed (2026-05-07) | Operator wants every gm/rm/admin/super_admin to get a once-a-day digest of open damage claims relevant to them — today nobody gets one | [brief-065-daily-open-claims-summary-cron.md](brief-065-daily-open-claims-summary-cron.md) |
| 66 | RM revert paths from `Approved — Pending Quotes` and `Pending RM Quote Approval` — operator confirmed 2026-05-07 (testing as RM dc_role) that an RM reviewing a GM-approved claim couldn't send it back; all three "send back" buttons greyed out with "Requires admin or higher". Use case: GM erroneously approves, RM catches it during quote-gathering. Three existing transitions widened from `role: "admin"` → `role: "rm"` (`Approved — Pending Quotes → Pending GM Review`, `Approved — Pending Quotes → Pending RM Review`, `Pending RM Quote Approval → Pending GM Review`); two new RM-allowed transitions added (`Approved — Pending Quotes → Closed — Denied`, `Pending RM Quote Approval → Pending RM Review`). All five carry `requiresNote: true` and `clearApprovalDetails: true` so the worker NULLs `approved_amount` / `approved_quote_id` / `parts_ordered` / `vendor_name` and resets gm/rm/ceo audit stamps on revert. apps/web `transitions.ts` mirror updated in lockstep — the three widened labels drop the `(admin)` suffix, the two new entries land in the per-status forward blocks. `Pending RM Quote Approval → Approved — Pending Quotes` (forward step past where RM might want to revert from) stays admin-only. Post-approval / finance-touched reverts (`Approved — In House — *`, `Approved — Check Request Submitted`, `Submitted for Payment`, `Check Issued`, all `Closed — Paid` reopens) stay admin-only as before. No schema change, no new endpoints, no new env vars | Completed (2026-05-07) | RM can bounce or deny a misrouted GM-approved claim without admin escalation | [brief-066-rm-revert-from-approved-pending-quotes.md](brief-066-rm-revert-from-approved-pending-quotes.md) |
| 67 | Reporting page (`/admin/damage/reporting`) gains four enhancements from operator's 2026-05-07 review: (1) Cost column on the global Approved-by-damage-type table — `byDamageTypeApprovedSql` rewritten as a JOIN-based variant (`LEFT JOIN claim_photos ON ... AND cp.deleted_at IS NULL`, `COUNT(DISTINCT c.claim_id)`, `COALESCE(SUM(CASE WHEN photo_type IN ('Quote','Receipt') AND amount IS NOT NULL THEN amount END), 0) AS cost`); (2) Per-location drill-down via inline expandable rows — new `byLocationDrilldownSql` returns one row per (location_code, outcome_bucket, damage_type) using a 5-bucket CASE (`open / denied / approved / closed_approved / closed_other`); apps/web client component `_components/ByLocationTableClient.tsx` (`useState<Set<string>>` keyed by location_code, mirrors Brief 53's audit-log pattern) renders four damage-type breakdown panels per row (Open / Closed / Approved-with-cost / Denied), aggregating `denied + closed_approved + closed_other` for the Closed view and `approved + closed_approved` for the Approved view; (3) Avg Days Open column on the By Location table between Denied and Cost — new `byLocationAvgDaysOpenSql` uses `AVG(julianday('now') - julianday(submitted_at))` filtered to `lifecycle_state = 'Open'`, rendered as integer days + "d" suffix or "—" when null; (4) Footer reworded — drops the v2 double-count limitation disclaimer per operator's 2026-05-07 decision that summing approved quotes + receipts is the correct framing (receipts paid out + quotes committed = total exposure). Two new D1 queries added to the existing batch (now 13 statements). Response shape extended: `by_location[].avg_days_open: number \| null`, `by_damage_type_approved[].cost: number`, new top-level `by_location_drilldown` array. `DamageTypeTable` helper gains optional `showCost` prop. No schema change, no new env vars, no new endpoints | Completed (2026-05-07) | Operator visibility into per-location damage-type mix without changing filters, plus per-line-item cost on the Approved-by-damage-type table | [brief-067-reporting-cost-column-drilldown-avg-days.md](brief-067-reporting-cost-column-drilldown-avg-days.md) |
| 68 | Age pill on `/admin/damage` claims list — `listClaims` SELECT projection gains a server-computed `age_days` column (`CAST((julianday('now') - julianday(submitted_at)) AS INTEGER)`); apps/web damage list renders an `{N}d` pill next to the submitted_at date with four-tier color escalation (neutral / amber / orange / red at 0-3 / 4-7 / 8-14 / 15+ days) on Open claims; Closed claims render a muted neutral pill so the column stays aligned. Detail page intentionally untouched. No schema change, no new endpoints, no new env vars | Completed (2026-05-07) | Operator visual scan of "this one's been sitting for two weeks" without mental date diff | [brief-068-age-pill-on-damage-claims-list.md](brief-068-age-pill-on-damage-claims-list.md) |
| 69 | Age pill — collapse Brief 68's four-tier curve to two tiers per operator's 2026-05-07 ask. `tierFor` ternary in `apps/web/app/admin/damage/_components/AgePill.tsx` narrowed from `≥15 red / ≥8 orange / ≥4 amber / neutral` to `≥11 red / ≥4 yellow / neutral`; `Tier` type drops `amber`+`orange`, renames `amber`'s slot to `yellow` (same Tailwind classes); `TIER_CLASSES` map shrinks accordingly. Lifecycle gating, pill shape/font/position, title attribute, and `<1d` zero-day label all unchanged. No worker change, no API change, no schema change, no env vars | Completed (2026-05-07) | Operator's preferred urgency curve — fewer color changes makes the signal clearer at a glance | [brief-069-age-pill-two-tier-thresholds.md](brief-069-age-pill-two-tier-thresholds.md) |
| 70 | Work Orders page + new `splash-workorders` worker. Read-only MaintainX integration: `GET /workorders/api/list` endpoint dc_role-gated (super_admin/admin global, gm/rm scoped to dc_locations), grouped by Splash `location_code` and sorted by priority HIGH→MEDIUM→LOW→NONE then `updatedAt` desc. New top-level apps/web page at `/workorders` (NOT under /admin/*); dashboard tile links in. `MAINTAINX_API_KEY` now bound on TWO workers (damage for create, workorders for read; same value, per-worker secrets). Two new bulk Supabase helpers (`getMaintainXIdsForLocationCodes`, `getLocationCodesByMaintainXIds`) resolve location_code ↔ maintainx_id in two PostgREST GETs total per direction. Status filter excludes DONE/CANCELED/SKIPPED; `expand=assignees,location,thumbnail`; limit=200 with `truncated` flag for v2 pagination. Fail-soft: 503 when API key unbound, 502 on MaintainX non-2xx, 504 on timeout. apps/web `WORKORDERS_WORKER` service binding + middleware `/workorders/:path*` matcher + dev rewrite extended | Completed (2026-05-07) | Operator visibility into MaintainX work orders for their location(s) without leaving Splash tooling | [brief-070-workorders-worker-and-page.md](brief-070-workorders-worker-and-page.md) |
| 71 | Work Orders v2 — six-issue batch from operator's first deploy review. (1) Architectural pivot: drop `pricing_simple` from the read path (zero-padding bug + wrong join axis); new `getLocationsByContactEmail` reads `locations` directly. Brief 70's `getMaintainXIdsForLocationCodes` / `getLocationCodesByMaintainXIds` / `MaintainXLocationInfo` deleted. (2) Permission model: pure email-on-locations gate via `am_email`/`rm_email`/`site_email`; super_admin and admin lose their global override. (3) Per-MaintainX-location grouping fixed (was rendering as a flat "Unmapped" table). (4) Click-to-expand rows surface description / created date / age / assignees / categories (`useState<Set<string>>` mirroring Brief 67). (5) Assignee names: two new Supabase tables `maintainx_users` / `maintainx_teams` populated by a daily 11:30 UTC cron (`{ fetch, scheduled }` default export, `[triggers] crons` in wrangler.toml — Brief 65 pattern); on-demand `POST /workorders/api/sync-maintainx-users` super_admin trigger; new `maintainx-users.ts` module in `@splash/db-supabase` exposes `getMaintainXUsersByIds` / `getMaintainXTeamsByIds` / `upsertMaintainXUsers` / `upsertMaintainXTeams`; new `apps/workorders-worker/src/sync.ts` runs the cursor-paginated sync. (6) Reactive vs Preventive tabs split via `wo.type === "PREVENTIVE"` (over-fetch + bucket in code; MaintainX `/workorders` doesn't filter on type). (7) StatusPill IN_PROGRESS rendering fix — keys on uppercased upstream enum, amber background. `expand=categories` added to upstream MX call; `expand=thumbnail` dropped. Response shape rewritten: `{ reactive, preventive, fetchedAt, truncated, accessibleLocationCount, mappedLocationCount, email }` — no more `scope` / `unmatchedWorkOrders` / `missingMaintainxIds` keys. New apps/web client component `WorkOrdersTabsClient` drives tab + per-row expand state; old `EmptyState.tsx` deleted | Completed (2026-05-07) | Operator's first-deploy feedback on `/workorders` (six issues at once) | [brief-071-workorders-v2-gating-grouping-assignees-types.md](brief-071-workorders-v2-gating-grouping-assignees-types.md) |
| 72 | Work Orders v3 — conditional pagination for multi-location users. Brief 71's single 200-cap MaintainX call was hiding older Reactive WOs behind newer Preventive ones (~145 Preventive + 45 Reactive of 200 fetched on operator's queue, leaving an unknown tail invisible). `fetchMaintainXWorkOrders` extended with `paginate` + `maxWorkOrders` inputs; when `paginate=true`, walks `nextCursor` until accumulator hits the cap or cursor goes null, with a hard ceiling of 10 iterations as defense-in-depth. Handler decides via `shouldPaginate = mappedMxIds.length > 1`: single-location users keep the one-call / 200-cap / 8s-timeout posture (nothing changes); multi-location users paginate up to 1000 WOs (5 × 200) under a 30s aggregate timeout. Module-local consts `MAX_WORK_ORDERS_SINGLE` / `MAX_WORK_ORDERS_MULTI` / `TIMEOUT_SINGLE_MS` / `TIMEOUT_MULTI_MS` make tuning one-edit-one-place. New `pageCount: number` field added to `FetchResult` and the worker's `GET /workorders/api/list` response shape (1 for single-call, n for paginated); apps/web `WorkOrdersListResponse` type extended in lockstep. `TruncatedNotice` banner copy reworded to "Showing first 1000 work orders. Older items aren't visible here — log into MaintainX directly for the full list." with the soft amber styling from Brief 71's missing-maintainx_id warning. No D1 / Supabase schema change, no new bindings, no new env vars, no new secrets | Completed (2026-05-07) | Operator visibility into older Reactive WOs that were getting drowned by Preventives at the 200-fetch cap | [brief-072-workorders-conditional-pagination.md](brief-072-workorders-conditional-pagination.md) |
| 73 | Work Orders UX polish — Preventive tab gains a "Due" column rendering a `<DueDatePill>` with three tiers + null fallback (red pill "Overdue Nd" when past due, amber pill "Due today" when within today, muted plain text "Due MMM D" for future, em-dash for null/NaN). Day-floor comparison done in UTC. Reactive tab unchanged (MaintainX auto-sets reactive `dueDate` to same-day creation; not operationally meaningful). Both tabs additionally surface a muted "Nd" age label beneath the priority pill in the collapsed row, computed via `Math.floor((Date.now() - createdAt) / 86_400_000)` with a `<1d` floor matching Brief 68/69's AgePill convention; plain muted text rather than a tiered pill so it doesn't compete with the priority pill for visual weight (operator preference). Expanded-row Age block deleted to avoid duplication; expanded grid collapses to `sm:grid-cols-2` (Created + Assignees). Active tab threaded through `GroupSection` → `WorkOrderRow` so the Due `<th>`/`<td>` and the expanded-row `colSpan` (8 on Preventive, 7 on Reactive) are conditional. Two-file change in apps/web: new `DueDatePill.tsx` + edits to `WorkOrdersTabsClient.tsx`. No worker change, no schema change | Completed (2026-05-07) | Operator scan-ability of `/workorders` — "what's overdue, what's been sitting" — without click-to-expand each row | [brief-073-workorders-due-date-pill-and-age-under-priority.md](brief-073-workorders-due-date-pill-and-age-under-priority.md) |
| 74 | Work Orders — three-issue batch. (1) PriorityPill MEDIUM swapped from `bg-yellow-100 text-yellow-900` (which read as plain text against the white row background per operator screenshot 2026-05-07) to `bg-gray-light text-splash-navy/80` so the tier sits between LOW (sudsy-blue) and NONE (gray-light/60) in visual weight. (2) Age "Nd" muted text under the priority pill gains matching `px-2` horizontal padding so it aligns under the pill's text content, not its left edge. (3) New "New Request" tab on `/workorders` renders a form (NewRequestForm.tsx) instead of a list; plain HTML form posts directly to new worker endpoint `POST /workorders/api/request` as multipart/form-data, bypassing Next 15 server actions per Brief 37/38 pattern. Worker calls MaintainX `POST /v1/workrequests` then per-photo (max 5) `PUT /v1/workrequests/{id}/{thumbnail\|attachment}/{filename}` — first photo → thumbnail, rest → attachments; photo upload failures are non-fatal (request exists either way; partial-fail surfaces via `request_warn=N-of-M-photos-failed` query param). Email-on-locations gate matches the read path; `isOriginAllowed` CSRF gate. Location dropdown has no default — operator must explicitly pick. Requester Name defaults to MaintainX `full_name` via new `getMaintainXUserByEmail` helper in `@splash/db-supabase`. Description gains a structured footer block: `Requested by: {name}\nPhone: {phone or "—"}\nSubmitted via: Splash /workorders`. Worker `/list` response shape extended with `accessibleLocations: AccessibleLocation[]` and `currentUser: { email, full_name }` so the form has its data without a second fetch. New helpers in `apps/workorders-worker/src/maintainx.ts`: `createMaintainXWorkRequest` + `uploadMaintainXWorkRequestFile` (fail-soft, never throw). `WorkOrdersTabsClient` extended to `"reactive" \| "preventive" \| "new"` tab state with URL-search-param init (`?tab=new&request_ok=N` / `&request_error=...` / `&request_warn=...`) driving a banner above the form. Per-upload AbortController timeout 15s; create timeout 15s; total endpoint upper bound ~90s | Completed (2026-05-08) | Operators can file MaintainX work requests without leaving Splash; visual fixes for priority/age column | [brief-074-workorders-new-request-tab-and-priority-pill-fixes.md](brief-074-workorders-new-request-tab-and-priority-pill-fixes.md) |
| 75 | Brief 75 | Work Orders New Request — single-photo + required phone. Brief 74's first-deploy smoke test (2026-05-08) surfaced two issues: (a) MaintainX returned `404 Cannot PUT /v1/workrequests/{id}/attachment/{filename}` for every photo beyond the first — work requests appear to support thumbnail only; (b) operator wanted Requester Phone required. Brief 75 retires Brief 74's multi-photo path: form's photo `<input>` loses `multiple`, helper text updated to "Additional photos can be added in MaintainX after the request is approved.", `useState`-driven photo-count tracker + `MAX_PHOTOS` constant + alert-on-overflow `onChange` deleted. Worker collapses the per-photo loop to a single `/thumbnail/` upload (defense-in-depth: take first and `console.warn` if more than one photo arrives via curl/quirk). New worker-side validation rejects empty `requesterPhone` with `?request_error=requester_phone_required` (no format validation; operators may legitimately enter international formats / extensions). Description footer drops the legacy `Phone: —` placeholder branch. apps/web `WorkOrdersTabsClient` banner translates `requester_phone_required` → "Requester phone is required." and `thumbnail_failed` → "the photo couldn't be uploaded as the request thumbnail". `uploadMaintainXWorkRequestFile`'s `"thumbnail" \| "attachment"` discriminator preserved so a future brief that lands a working work-request multi-photo endpoint can re-call it without a second helper. No D1 / Supabase schema change, no new env vars, no new bindings. | Completed (2026-05-08) — superseded by Brief 76 on the photo-count change; phone-required portion retained | Operators stop losing 4-of-5 attached photos per submission; phone field can no longer be skipped | [brief-075-workorders-new-request-single-photo-and-required-phone.md](brief-075-workorders-new-request-single-photo-and-required-phone.md) |
| 76 | Work Orders New Request — restore multi-photo with corrected `/attachments/` (plural) URL path. Brief 75's diagnosis was wrong: the 404 from Brief 74's multi-photo path came from a one-letter URL typo (`/attachment/` singular vs MaintainX's actual `/attachments/` plural). Empirical 2026-05-08: with the plural path, `PUT /v1/workrequests/{id}/attachments/{filename}` works and the MaintainX UI's "Files" section on a work request renders multiple attached files. Brief 76 reverses Brief 75's photo-count change while preserving its phone-required change. Worker `uploadMaintainXWorkRequestFile` builds the URL via a small `REQUEST_FILE_URL_SEGMENT` lookup so the discriminator type stays `"thumbnail" \| "attachment"` (matches the doc heading) but the URL emits the plural segment. Worker handler restores the per-photo loop: photo[0] → thumbnail endpoint, photo[1..4] → attachments (plural) endpoint; ≤5 cap (`?request_error=too_many_photos` over-limit); per-photo failure collected into a count surfaced via `?request_ok={id}&photo_warn={N}-of-{M}-photos-failed`. Form `<input>` regains `multiple`; helper text reads "Photo(s) (optional, max 5). First photo becomes the thumbnail; additional photos attach to the request." `WorkOrdersTabsClient` banner extended to render a stacked amber `photo_warn` banner under the green `request_ok` banner when both params are present; `request_warn` (Brief 75's name) still read for back-compat. Filename sanitizer collapses runs of underscores and trims a trailing `_` before the extension (`download__2_.jpg` → `download_2.jpg`). | Completed (2026-05-08) | Multi-photo work-request submissions actually work end-to-end; phone-required preserved | [brief-076-workorders-restore-multi-photo-with-correct-attachments-path.md](brief-076-workorders-restore-multi-photo-with-correct-attachments-path.md) |
| 77 | Brief 77 | Global Header — fix mobile logo crush + extend admin-controls gate to `/workorders`. Two issues from operator's 2026-05-08 mobile review: (a) iPhone Safari at `/admin/dashboard` showed the Splash logo crushed because three header buttons (Dashboard / Change Password / Sign Out) plus the email/role display competed for horizontal space; (b) `/workorders` rendered with only the bare logo bar — no email, role, or buttons — because `Header.tsx`'s `usePathname()` gate explicitly checked `/admin/*` and `/sysadmin/*` only, missing the top-level `/workorders` route Brief 70 introduced. One-file change in `apps/web/app/_components/Header.tsx`: (1) `ADMIN_PATH_RE` regex extended from `^/(admin\|sysadmin)(/\|$)` to `^/(admin\|sysadmin\|workorders)(/\|$)`; (2) "Change Password" `<Link>` button removed from the right-side button row and replaced with a small `<Link>` rendered as a third child of the email/role identity column (sibling to email + role badge), styled `text-xs text-white/70 hover:text-white hover:underline` so it doesn't compete with the badge; (3) docblock + inline regex comment rewritten to enumerate the three pathname families and document the demotion rationale (low-frequency action ~once per 90 days; visual hierarchy — Dashboard + Sign Out are nav/lifecycle, Change Password is account maintenance). CLAUDE.md "Global Header" bullet updated. No worker change, no schema change, no env vars. | Completed (2026-05-08) | iPhone Safari logo crush at `/admin/*` resolved; `/workorders` no longer renders without admin controls | [brief-077-header-mobile-fit-and-workorders-pathname-gate.md](brief-077-header-mobile-fit-and-workorders-pathname-gate.md) |
| 78 | Dashboard tile renames + icon swap on Signup tile. Cosmetic-only adjustments to `apps/web/app/admin/dashboard/page.tsx` from operator's 2026-05-08 review: (a) Signup tile eyebrow "Signup admin" → "Signup form", title "Signup Admin" → "MaxPass Admin", icon swapped from inline `$` SVG (vertical bar + S-curve) to lucide `Ticket` SVG path (rounded rectangle with side notches evoking a torn movie stub) — operator chose ticket icon to evoke "admission/membership/pass" semantics over the dollar sign's "money/billing" miscue, since the tool manages MaxPass subscriptions not transactions; (b) Sysadmin tile title "System Admin" → "Database Admin" (eyebrow "Admin" unchanged) — more honest description of the underlying user/role/table editor; (c) Work Orders tile eyebrow "MaintainX" → "Maintenance" + title "Work Orders" → "MaintainX" — invert the pairing so the eyebrow describes the domain and the title names the product. Damage Claims and Performance Tracking tiles unchanged. No `href` changes (URLs `/admin/pricing`, `/admin/sysadmin`, `/workorders` preserved); no description changes; no route, permission, schema, env, or worker changes. Inline SVG icon set continues (no icon library added — lucide path data copied inline). | Completed (2026-05-08) | Dashboard tile labels match operator's mental model; Signup tile icon no longer reads as billing | [brief-078-dashboard-tile-rename-signup-sysadmin-workorders.md](brief-078-dashboard-tile-rename-signup-sysadmin-workorders.md) |
| 79 | Work Orders — rename "Preventive" tab label to "Preventative" (operator preference; internal state literals stay `"preventive"` to match MaintainX's `PREVENTIVE` enum) + worker-side filter drops Preventive WOs more than 90 days past `dueDate` from `bucketByType`. New `PREVENTATIVE_MAX_OVERDUE_DAYS = 90` module constant in `apps/workorders-worker/src/index.ts`; UTC day-floor comparison mirrors Brief 73's `<DueDatePill>` math; NULL / malformed dueDate Preventive WOs are kept (filter only suppresses dated rows past the threshold). Reactive tab unfiltered (Reactive `dueDate` is MaintainX-auto-set to same-day creation, not operationally meaningful). Surgical client-side label flip in 4 sites in `WorkOrdersTabsClient.tsx` plus a sibling docblock flip in `DueDatePill.tsx`; URL search-param values, type unions, response shape field names, and console logs all stay lowercase `"preventive"` for back-compat. `bucketByType` return type widened to include `droppedOverduePreventive` count for observability logging. No schema change, no new endpoints, no new env vars | Completed (2026-05-09) | Operator scan-ability of Preventative tab — drops the long tail of stale auto-spawned MaintainX preventive cycles from the view | [brief-079-workorders-preventative-rename-and-90-day-filter.md](brief-079-workorders-preventative-rename-and-90-day-filter.md) |
| 80 | Work Orders New Request — fix post-submit "View in MaintainX ↗" link 404. Brief 74 inferred the work-request UI URL as `app.getmaintainx.com/workrequests/{id}` by analogy with the work-order pattern (`/workorders/{id}`); MaintainX's actual UI URL for work requests is `/requests/{id}` — no `work` prefix on the segment, segments aren't symmetric. Operator confirmed empirically 2026-05-09 (submitted request #11706935; banner-link 404'd; manual URL substitution to `/requests/11706935` rendered correctly). One-line text fix at `WorkOrdersTabsClient.tsx:227`. Sweep grep for any other `app.getmaintainx.com/workrequests` UI URLs (none found in source today). CLAUDE.md gets a one-line URL-asymmetry warning to discourage future inference (Brief 76 was the same bug class on the API side: singular `/attachment/` vs plural `/attachments/`; Brief 62 same bug class on the Supabase join key) | Completed (2026-05-09) | Operator can click "View in MaintainX ↗" on the success banner and land on the actual request | [brief-080-workorders-new-request-success-link-url-fix.md](brief-080-workorders-new-request-success-link-url-fix.md) |
| 81 | Fleet inquiry worker — lift-and-shift into the monorepo. Net-new `apps/fleet-inquiry-worker/` directory; verbatim copy of `src/index.js` (~1900 LOC plain JS, zero npm deps) from external repo at `C:\Users\Coppsrv\Documents\Projects\fleet-inquiry-worker`; new `wrangler.toml` modeled on workorders-worker (workers.dev-only posture, `[observability.logs]` block, `[vars]` for SUPABASE_URL + TURNSTILE_SITE_KEY, no production routes). New worker name `splash-fleet-inquiry` — does NOT touch the legacy `broad-shape-38b8` deployed at `fleet.splashcarwashes.info`. `tsconfig.json` with `allowJs: true / checkJs: false` so root typecheck passes without converting JS to TS. New `PRE_DEPLOY_FLEET.md` runbook covering Supabase + Turnstile + Google Maps secret binding (fleet is the ONLY monorepo worker with paid third-party API usage — Geocoding API, ~$5/1000 calls beyond free tier, 7-day cache TTL materially lowers volume). Cutover (DNS/route flip from legacy to new worker) explicitly out of scope — operator-driven, future brief if ever. Strict negative scope: NO modifications to any existing file under `apps/` or `packages/`; only doc additions to CLAUDE.md / BUILD_STATE.md / BRIEFS/INDEX.md plus new PRE_DEPLOY_FLEET.md. Deferred to future briefs: TS conversion, `@splash/db-supabase` helper migration, render extraction, anon→service key migration | Completed (2026-05-09) | First step of fleet-inquiry integration into the monorepo without affecting any existing functionality | [brief-081-fleet-inquiry-worker-lift-and-shift.md](brief-081-fleet-inquiry-worker-lift-and-shift.md) |
| 82 | Fleet inquiry worker — bind staging custom domain `fleet.staging.splashcarwashes.info` (subdomain pattern mirroring production's `fleet.splashcarwashes.info`, NOT a path-carve on `staging.splashcarwashes.info` like workorders-worker — chose subdomain because fleet's API endpoints sit at bare `/api/*` and would collide with apps/web staging's `/api/login` / `/api/me` if path-carved). Single edit: uncomment / add the `routes = [{ pattern = "fleet.staging.splashcarwashes.info", custom_domain = true }]` block in `apps/fleet-inquiry-worker/wrangler.toml`. Production routes remain commented out (legacy `broad-shape-38b8` keeps `fleet.splashcarwashes.info`). PRE_DEPLOY_FLEET.md gains a "Staging custom domain" section covering deploy, Turnstile hostname allow-list update (operator dashboard step), and verification. CLAUDE.md gains a one-line note on the subdomain-vs-path-carve choice for fleet | Completed (2026-05-09) | Operator gets a stable, branded staging URL for fleet during the legacy-vs-monorepo parallel period; sets up the URL Brief 83's apps/web admin viewer will hit | [brief-082-fleet-inquiry-staging-custom-domain.md](brief-082-fleet-inquiry-staging-custom-domain.md) |
| 83 | Fleet submissions admin viewer — `/admin/fleet` list with date-range filter + CSV export, `/admin/fleet/[id]` detail page. Mirrors Brief 56's signups viewer pattern. New worker admin endpoints (`GET /admin/api/submissions`, `/admin/api/submissions/{id}`, `/admin/api/submissions.csv`) gated to admin + super_admin via `@splash/auth`; CSV worker-rendered with proper `Content-Disposition`. New apps/web `[[services]]` binding `FLEET_INQUIRY_WORKER`, `cloudflare-env.d.ts` declaration, middleware allow-list extended with `"fleet"`. New shared components `<DateRangePicker>` and `<CsvExportButton>` in `apps/web/app/_components/` (reused by Brief 84 on signups). New dashboard tile "Fleet Inquiries" with lucide `Truck` icon. Fleet-inquiry-worker's first authenticated endpoints — adds `@splash/auth`/`@splash/db-supabase`/`@splash/http` runtime deps; new `src/admin.js` module owns the `/admin/api/*` handlers; one new router branch in the verbatim-lifted `src/index.js`. PRE_DEPLOY_FLEET.md and PRE_DEPLOY_WEB.md updated. No schema change. Auth gate allows `session.role === "super_admin"` OR `session.dcRole === "admin"|"super_admin"` (no `admin` role exists in `user_permissions.role`; the dc_role admin tier from Brief 42 gets read-only fleet visibility). `SUPABASE_SERVICE_KEY` now required on `splash-fleet-inquiry` for admin endpoints (public form continues on anon key) | Completed (2026-05-09) | Operator gets a real admin surface for fleet inquiries instead of querying Supabase directly | [brief-083-fleet-submissions-admin-viewer.md](brief-083-fleet-submissions-admin-viewer.md) |
| 84 | Signups admin viewer — retrofit `/admin/signups/{loc}` with the same `<DateRangePicker>` + `<CsvExportButton>` Brief 83 introduces on the fleet viewer. Worker endpoint extended to accept `from`/`to` query params alongside the existing `days=N` (back-compat preserved — old bookmark URLs with `?days=7` keep working). New sibling endpoint `GET /admin/api/locations/{loc}/signups.csv?from=&to=` worker-renders CSV with proper `Content-Disposition`; same `adminGate` + `userCanAccessLocation` from Brief 56. Apps/web list page swaps the `?days=N` dropdown for the shared date-range component and adds the export button. Worker-fetch helper extended to support both old and new param shapes. CSV `select=*` captures all 18 user-facing `maxpass_signups` columns including `phone` raw, `terms_text`, `confirmation_token`. Filter column = `submitted_at` (NOT `created_at` like fleet — `maxpass_signups` was indexed on `submitted_at`). 10000-row safety cap → 416 on overflow. Legacy day-filter strip preserved on `/admin/signups/{loc}?days=N` URLs so old bookmarks render a sensible UI. No schema change. No new files; reuses Brief 83's shared components verbatim | Completed (2026-05-09) | Symmetric date-range filtering + CSV export across both signup and fleet customer-data surfaces | [brief-084-signups-viewer-date-range-and-csv.md](brief-084-signups-viewer-date-range-and-csv.md) |
| 85 | Fleet inquiry success modal — add "Fill Again" button mirroring Brief 57's signup-form pattern. Operator review 2026-05-09: success modal currently dead-ends after submission. New `<button id="fillAgainBtn" class="btn-submit">Fill Again</button>` lands in the existing success modal markup at `src/index.js` L1374; new IIFE-scope click handler runs `window.location.href = '/'` (RELATIVE URL — works identically on workers.dev / `fleet.staging.splashcarwashes.info` / post-cutover `fleet.splashcarwashes.info` with zero per-environment hardcoding). Sweep verifies no other absolute splashcarwashes.info / workers.dev URLs exist in client-side JS or rendered HTML (server-side fetches to Supabase / Google Maps / Turnstile siteverify stay absolute — those are env vars / external services, not navigation targets). Multi-vehicle / multi-contact / accidental-double-submit recovery use case. No schema change, no new endpoints | Completed (2026-05-09) | Operator can file consecutive fleet inquiries without manually navigating; relative-URL approach eliminates cutover maintenance | [brief-085-fleet-fill-again-button.md](brief-085-fleet-fill-again-button.md) |
| 86 | Fleet admin viewer — fix `created_at` → `submitted_at` column reference. Brief 83's executor wrote `apps/fleet-inquiry-worker/src/admin.js` to filter and order `fleet_submissions` rows on a `created_at` column that doesn't exist on the table; only `submitted_at` does (the public form's submit handler at `src/index.js:368` writes `submitted_at: new Date().toISOString()` and that's the only timestamp column). PostgREST returned 400 with `column does not exist`, the worker non-2xx branch surfaced 500, apps/web `/admin/fleet` showed "Failed to load submissions: 500" on every list/CSV/date-range call. One-file surgical fix in `admin.js`: three `created_at` filter/order sites in the list handler swap to `submitted_at`, three identical sites in the CSV handler, plus prune the bogus `created_at` entry from `CSV_COLUMNS`. Two docblock comments updated in lockstep. CLAUDE.md gains a one-line warning about Supabase column-by-analogy bugs (same class as URL-by-analogy in Briefs 76 / 80 and join-key inference in Brief 62). Brief 84's signups viewer was correctly grounded on `submitted_at` (executor probed the schema for that one); only fleet's was inferred wrong | Completed (2026-05-09) | `/admin/fleet` actually loads its data instead of 500ing | [brief-086-fleet-admin-fix-submitted-at-column.md](brief-086-fleet-admin-fix-submitted-at-column.md) |
| 87 | Fleet detail page — `splash_notes` editable textarea at top of `/admin/fleet/[id]`. Operator added a `splash_notes` text column to `fleet_submissions` 2026-05-09 for sales-follow-up annotations; this brief surfaces it as an inline editor. New `PATCH /admin/api/submissions/{id}` admin-gated worker endpoint accepts `{ splash_notes: string }` body, validates string + 10000-char cap, PATCHes Supabase via service-key. Apps/web detail page renders `<ActionForm>`-wrapped textarea + Save button at the top of the page above the existing key/value grid (per Brief 19's `useActionState` + `router.refresh()` pattern, NOT redirect). New `updateFleetSubmissionNotes(id, notes)` helper in `apps/web/app/admin/fleet/_lib/worker-fetch.ts` mirrors the existing GET helpers' binding-first / URL-fallback shape. `CSV_COLUMNS` in fleet `admin.js` extended with `splash_notes` so it appears in CSV exports too. Same admin/super_admin gate as the read endpoints. v1 has no audit columns (`splash_notes_updated_at`/`_by` deferred); last-write-wins for concurrent edits | Completed (2026-05-09) | Splash reps can write follow-up notes inline while contacting fleet leads | [brief-087-fleet-detail-splash-notes-editor.md](brief-087-fleet-detail-splash-notes-editor.md) |
| 88 | Fleet CSV export — apps/web proxy route handler fixes cross-subdomain 404. Operator hit "Export CSV" 2026-05-09 and Chrome saved a `submissions.txt` 404 page repeatedly. Root cause: `getFleetCsvUrl` in `apps/web/app/admin/fleet/_lib/worker-fetch.ts` falls back to a same-origin relative path `/admin/api/submissions.csv?...`; on `staging.splashcarwashes.info` that resolves to apps/web, but the fleet worker is on a different subdomain (`fleet.staging.splashcarwashes.info`, per Brief 82's subdomain choice). Apps/web has no route at that path → 404 → browser saves the HTML response as `.txt`. Signups CSV works because signup-worker is path-carved on the SAME hostname as apps/web. Fix: new Next.js Route Handler at `apps/web/app/admin/fleet/export.csv/route.ts` proxies via the `FLEET_INQUIRY_WORKER` service binding (with URL fallback for `next dev`), forwards Cookie + Origin headers, streams the upstream response back preserving `Content-Type` + `Content-Disposition`. `getFleetCsvUrl` updated to point at `/admin/fleet/export.csv` (apps/web same-origin) instead of constructing a worker-direct URL. Three options considered (cookie domain widening / fleet path-carve refactor / proxy route); proxy route picked as cleanest — zero changes to fleet worker, zero cookie-domain coordination, zero fleet code refactor. Pattern documented in CLAUDE.md so future workers using subdomain + needing browser-direct downloads follow the same convention | Completed (2026-05-09) | Operator can actually export fleet CSVs instead of getting 404'd HTML downloads | [brief-088-fleet-csv-export-proxy-route.md](brief-088-fleet-csv-export-proxy-route.md) |
| 89 | Forms feature foundation — schema, worker scaffolding, shared package, service binding. Greenfield substrate for the 10-brief form-builder feature (Briefs 89–98). Five new Supabase tables (`forms` / `form_versions` / `form_assets` / `form_submissions` / `form_submission_files`) shipped as `supabase/forms-tables.sql` for the operator to run in the SQL editor. New eighth monorepo worker `apps/forms-worker/` (`splash-forms`) — 404 stub + `Env` interface + `[[r2_buckets]]` FORMS_FILES → `splash-forms-files` + Brief 63 `[observability.logs]` block; staging route path-carved on `staging.splashcarwashes.info/forms/*` per planning Decision 2 (cookie + CSV simplicity over fleet's subdomain pattern). New shared package `packages/forms-schema/` exporting the `Field`/`FormSchema`/`FormVersion`/`FormSubmission` type skeleton + `LOOKUP_SOURCES` registry (8 pricing_simple + 3 locations columns; Brief 59 RD/RM label convention) + a `validators/` stub Brief 90/91 fills in; introduces `zod ^3.23.8` as the first monorepo runtime dep. Service binding `FORMS_WORKER` wired in `apps/web/wrangler.toml` + `cloudflare-env.d.ts`. `resolveLookup` stub helper added to `@splash/db-supabase/lookup.ts` (real implementation Brief 93). `PRE_DEPLOY_FORMS.md` scaffolded; CLAUDE.md gains constraint #10 (forms tables owned by `splash-forms`; no migration framework; direct edits to `form_versions.schema` rewrite history) and a glossary entry for the new worker. Non-deployable on its own; user-visible endpoints land in Brief 90+ | Completed (2026-05-09) | Brief 90 (public form rendering) + Briefs 91–98 (full feature) | [brief-089-forms-foundation-schema-worker-package.md](brief-089-forms-foundation-schema-worker-package.md) |
| 90 | Forms — public render path (`GET /forms/{slug}`). First user-visible deliverable for the form-builder feature. Extends `@splash/forms-schema/types.ts` from the Brief 89 `Field = FieldBase` placeholder into the full 16-interface discriminated union (heading / image / name / email / phone / short_text / long_text / hidden / dropdown / multi / date / time / file / signature / location / lookup) plus a new `LocationOption` interface used by the worker's pre-baked Location dropdown. New `validators/field-config.ts` with one Zod schema per field type plus `formSchemaSchema = z.object({ fields: z.array(fieldSchema) })`; `validators/index.ts` re-exports the new module replacing the Brief 89 `export {};` stub. New worker DB module `apps/forms-worker/src/db/forms.ts` exports `getFormBySlug` / `getCurrentVersion` / `getLocationOptionsFromPricingSimple` — all using direct PostgREST `fetch()` with `SUPABASE_SERVICE_KEY` (matches the Brief 71 `maintainx-users.ts` pattern; no `@supabase/supabase-js` client in worker code). `getCurrentVersion` runs `formSchemaSchema.safeParse` as the runtime boundary check against hand-edited `form_versions.schema` JSONB. New `apps/forms-worker/src/render/` directory: `shell.ts` (`<!DOCTYPE>` + `<head>` with optional Turnstile script + inline ~70-line `SHELL_CSS` with Splash brand tokens + navy header bar with white-script logo from `@splash/storage-r2 ASSETS`), `index.ts` exports `renderFormBody` (iterates fields, dispatches per-type, wraps in `<form action="/forms/api/submit/{slug}">` with hidden `pending_submission_id` + submit button + optional Turnstile widget), `util.ts` (`escapeHtml` / `fieldLabel` / `fieldHelp` shared helpers), `fields/index.ts` (exhaustive `switch (field.type)` dispatcher) + 16 per-type modules. Per-field-type modules average ~15 lines: `heading` + `image` produce no input element (display-only); `lookup` renders disabled with placeholder "Select {keyFieldLabel} to populate" (Brief 93 wires resolution); `file` + `signature` render their inputs but Brief 92 wires upload behavior; `location` pre-bakes `<option>`s from the worker's `getLocationOptionsFromPricingSimple` query at render time. Worker `src/index.ts` replaces the Brief 89 404 stub with a router: `GET /forms/{slug}` → `handleFormRender` (404 on draft / archived / no current_version_id; audience gate: `internal` 302s to `/login?next=...` when `sb-access-token` cookie missing per Decision 8b's render-time cookie-presence check, full session validation deferred to submit time; `public` includes Turnstile widget when `TURNSTILE_SITE_KEY` bound; `link-only` slug-as-gate); everything else 404s. Response headers: `Cache-Control: no-store` (every render reads fresh — schema may have changed via re-publish), `X-Content-Type-Options: nosniff`, `Referrer-Policy: same-origin`. New `supabase/forms-test-data.sql` lands three test forms (`test-public` / `test-internal` / `test-link-only-x4kp9q2m7nf3`) with representative subsets of field types so the operator can verify each audience rendering pattern; idempotency note in file header documents the DELETE statement for re-runs. `PRE_DEPLOY_FORMS.md` Section 5 gains 8 smoke-test entries covering all three audiences plus the 404 / disabled-lookup / hidden-pending-submission-id scenarios. CLAUDE.md glossary entry for forms-worker extended with the per-field-type module convention + 5-step recipe for adding a 17th type. Submit handler intentionally still 404s (Brief 91) — operator should verify "form renders correctly" but not click Submit. | Completed (2026-05-09) | Brief 91 (public submit), Brief 95 (admin builder will iframe or duplicate this renderer; per-field-type render contract lands here first) | [brief-090-forms-public-render.md](brief-090-forms-public-render.md) |

| 92 | Forms — file + signature uploads (R2-backed). Third user-visible deliverable for the form-builder feature; with this brief landed every field type except `lookup` (Brief 93) is functional end-to-end. New `apps/forms-worker/src/limits.ts` defines `HARD_LIMITS` (25 MB per file, 100 MB per submission, 20 files per submission, 1 MB per signature, 24h pending TTL) + `DEFAULT_LIMITS` (10 MB default per-field cap, image/* + application/pdf default MIME allow-list). New `apps/forms-worker/src/uploads/` directory with four modules: `file.ts` (`POST /forms/api/upload/{slug}` — multipart parse, MIME-sniff via `file-type@^19.6.0` reading first ~4 KB, per-field cap clamping, R2 put under `form-submission-files/{form_id}/{pending_submission_id}/{field_key}/{filename}`, returns `{r2_key, mime, size_bytes, original_filename}`); `signature.ts` (`POST /forms/api/signature/{slug}` — same shape but signature-specific, MIME derived from field config not request, returns `{r2_key, format, size_bytes}`); `serve.ts` (`GET /forms/admin/api/files/{r2_key}` — `@splash/auth authenticate()` gate matching Brief 83's fleet admin posture: super_admin role OR admin/super_admin dcRole; inline-display for images, force-download for everything else); `static.ts` (vendored `signature_pad@4.2.0` + `forms-public.js` served from `/forms/api/static/{filename}` with `public, max-age=86400`). Vendored `apps/forms-worker/static/signature-pad.min.js` (11479 bytes, SHA-256 `49050fd4c2a4c66eff11a54f2552af743bb0681cde745760667c61e9c690b3e0`, MIT, fetched once at brief execution from `unpkg.com/signature_pad@4/dist/signature_pad.umd.min.js` — committed, never CDN-loaded at runtime per CLAUDE.md supply-chain posture). New `apps/forms-worker/static/forms-public.js` (~190 LOC vanilla JS, no framework) wires per-form: signature canvases via `SignaturePad` with 800ms debounced `endStroke` upload, file inputs with `change`-event upload + hidden `${key}_r2` companion input the submit body carries, per-field error/status divs. Bundling pattern: wrangler `[[rules]] type = "Text" globs = ["**/static/*.js"] fallthrough = false` inlines both files as default-exported strings (the `**/` prefix is required — bare `static/*.js` does NOT match against the worker's source resolution and the implicit ESM rule wins for `.js` files; this caught the executor on the first dry-run with a "module variable in ESM" parser warning + "no matching export 'default'" error). New `apps/forms-worker/src/static-assets.d.ts` declares the `*.js` text-import default-export shim so TypeScript resolves both imports as strings. `packages/forms-schema/src/validators/payload.ts` un-`null`s the `file` + `signature` validator branches: file → `z.object({r2_key: regex(form-submission-files/), mime, size_bytes: z.number().int().positive(), original_filename: z.string().nullable()})`, signature → `z.object({r2_key: regex(...), format: z.enum(["png","svg"])})`; both `nullable().optional()` when `required === false`. `apps/forms-worker/src/submit/parse.ts` un-skips both types: file reads `${field.key}_r2` hidden, signature reads `field.key` hidden, both produce `{r2_key, ...}` objects (or `null` when required+missing for the validator to flag). `apps/forms-worker/src/submit/index.ts` gains an R2-resolution pass between parse and validate: for each file/signature payload entry, validate the r2_key starts with `form-submission-files/{form_id}/{pending_submission_id}/{field_key}/` (cross-submission key reuse → 400 `bad_r2_key`), HEAD the R2 object (missing → 400 `missing_file`), accumulate per-submission size + count (exceed → 413 `too_many_files` / `submission_too_large`), enrich the payload object with authoritative `head.size` / `head.httpMetadata.contentType` / `head.customMetadata.originalFilename`, queue a `form_submission_files` insert. `apps/forms-worker/src/db/forms.ts` extended with `insertSubmissionFiles(env, rows)` — best-effort PostgREST `Prefer: return=minimal` insert that runs AFTER the canonical submission row lands (intentional non-fatal failure: leaves R2 objects without DB rows for Brief 97's orphan cleanup to sweep). `apps/forms-worker/src/render/shell.ts` injects `<script src="/forms/api/static/signature-pad.min.js" defer>` + `<script src="/forms/api/static/forms-public.js" defer>` in `<head>` (signature-pad must load first — defer preserves script tag order). Worker router gains four new path matchers (file upload / signature upload / static asset / admin file serve). Bundle grew from Brief 91's 872.94 KiB / 163.14 KiB gzip to 975.33 KiB / 187.11 KiB gzip — +12% from `file-type` + 2 vendored static assets; still well inside CF's 3 MiB compressed limit. PRE_DEPLOY_FORMS.md Section 5 gains 8 Brief-92 smoke tests with the SQL to add file+signature fields to the `test-public` form, a MIME-spoof rejection test (`.exe` renamed `.jpg` → 415 `mime_not_allowed`), a 30 MB per-field cap test, and the admin serve route 401-vs-200 cookie test | Completed (2026-05-09) | Brief 95 (admin builder UI inspector for file/signature config), Brief 96 (admin submissions UI uses the serve route for previews), Brief 97 (cron cleanup depends on R2 path conventions land here) | [brief-092-forms-file-signature-uploads.md](brief-092-forms-file-signature-uploads.md) |

| 93 | Forms — lookup mechanism (resolver + on-demand endpoint + submit-time re-resolve). Fifth user-visible deliverable for the form-builder feature; with this brief landed every one of the 16 field types is functional end-to-end. `packages/db-supabase/src/lookup.ts` replaces the Brief 89 stub with the real `resolveLookup` — dispatches on `source.table`: pricing_simple → direct SELECT WHERE keyColumn = keyValue; locations → two-hop, first derive `pricing_simple.site` from pricing_simple WHERE keyColumn = keyValue, then SELECT FROM locations WHERE site_number = <that> (the Brief 62 fix's right join column, NOT `locations.site` which is the location name). The two-hop is hidden inside the helper — callers specify `sourceTable: 'locations'` and `keyColumn: 'pricing_simple.location_code'` and the helper does the rest. Returns `string | null`; null + log on Supabase error / unknown source. New `apps/forms-worker/src/lookup/resolve.ts` exports `handleLookupResolve` for `POST /forms/api/lookup/{slug}`: reads form by slug, validates the body's `lookup_field_id` is a `lookup` field in the current schema, validates `sourceTable + sourceColumn` are in `LOOKUP_SOURCES` (defense against hand-edited `form_versions.schema` JSONB exfiltrating arbitrary columns), instantiates a service-key client via `createServiceClient`, calls `resolveLookup`, returns `{value, resolved_at}`. Same-origin via `isOriginAllowed`. `apps/forms-worker/src/submit/index.ts` extended with the canonical server-side re-resolve loop (planning Decision 5a.ii) — runs after the file/signature R2 HEAD pass, before validation: for each lookup field, find its key field, read the submitted key value, resolve fresh, log `[forms.lookup] drift detected at submit` if the client value differs (tampering or mid-fill drift signal), store the FRESH value (`""` when null + allow_empty); for `display_only` mode delete the payload key entirely; for `nullBehavior === 'block_submit'` + required + null fresh return 422 `{error: 'lookup_failed', fields: {...}}` so the operator sees a precise per-field error. `packages/forms-schema/src/validators/payload.ts` lookup branch un-stubbed: display_only returns null (validator skipped — submit deletes the key); otherwise returns `z.string().min(1)` when block_submit + required, plain `z.string()` otherwise (allow_empty). `apps/forms-worker/src/submit/parse.ts` lookup skip is now intentional rather than deferred — header docblock + inline comment updated. `apps/forms-worker/src/render/fields/lookup.ts` extended with mode-aware variants: `prefill_hidden` → `<input type="hidden">` (no UI, value round-trips silently); `prefill_visible` → disabled `<input type="text">` populated visibly when key changes; `display_only` → styled `<div>` callout, no input, no payload entry. All three carry `data-lookup-field-id="${field.id}"` so the client JS can route resolves back to the right element. `apps/forms-worker/static/forms-public.js` extended with `wireLookups(formEl, slug)` — groups dependent lookups by their key field id, listens on `input` + `change` (250ms debounce), POSTs to `/forms/api/lookup/{slug}`, populates the visible UI (text input value or display callout div). Initial resolve fires if the key field has a value at load time (browser-restored back-button state). `apps/forms-worker/src/render/shell.ts` `SHELL_CSS` extended with `.field-display-only .field-display-value` rule — light-gray background + cyan left border + 44px min-height callout. `apps/forms-worker/src/index.ts` routes `POST /forms/api/lookup/{slug}` to the new handler. Caching: none — sub-10ms point reads on indexed columns; cache invalidation complexity isn't worth the marginal speedup at v1. Bundle grew from Brief 92's 975.33 KiB / 187.11 KiB gzip to 989.26 KiB / 190.44 KiB gzip (+13.93 KiB / +3.33 KiB gzip from the new lookup module + the resolveLookup helper). PRE_DEPLOY_FORMS.md Section 5 gains 7 Brief-93 smoke tests covering visible / hidden lookup population, drift defense (DevTools tampering of visible value → server still writes canonical), block_submit `lookup_failed` 422, and direct curl of the resolve endpoint with cross-origin / unknown-field error paths. CLAUDE.md forms-worker glossary extended with the Brief 93 paragraph (resolver as single source of truth, two-hop hidden, drift logging, three render shapes, no caching) | Completed (2026-05-10) | Brief 94 (admin API CRUD — needs the lookup-sources endpoint to populate inspector dropdowns), Brief 95 (admin builder UI — lookup field inspector depends on Brief 94's endpoint) | [brief-093-forms-lookup-mechanism.md](brief-093-forms-lookup-mechanism.md) |

| 91 | Forms — public submit path (`POST /forms/api/submit/{slug}`). Second user-visible deliverable for the form-builder feature; combined with Brief 90 the system has end-to-end working text-only submissions for all three audiences. New `packages/forms-schema/src/validators/payload.ts` exports `payloadValidatorFor(field)` returning per-field-type Zod value validators (or `null` for the deferred types — `file`/`signature` get wired in Brief 92, `lookup` in Brief 93). New `apps/forms-worker/src/submit/` directory containing `parse.ts` (multipart-form-data → typed payload keyed by `field.key`, multi-checkbox via `getAll`, empty-optional skip, file/signature/lookup ignored at this stage), `turnstile.ts` (Cloudflare siteverify wrapper mirroring fleet Brief 81 — fail-soft when secret unbound, 8s `AbortSignal.timeout`), `success.ts` (Splash-branded thank-you page with navy header + checkmark + form's `success_message` + "Fill out another" relative-URL button per Brief 85's pattern), and `index.ts` (`handleSubmit(env, req, slug)` route handler — orchestration). `apps/forms-worker/src/db/forms.ts` extended with `insertSubmissionIdempotent` using direct PostgREST `fetch()` + `Prefer: return=representation,resolution=ignore-duplicates` + `?on_conflict=id`; on conflict (empty `[]` body or 409) falls back to a SELECT-by-id read so caller always gets the canonical `SubmissionRow`. Returns `{row, wasNew}` for observability — Brief 97's webhook fire can use `wasNew` to avoid double-firing on idempotent retries. Worker `Env` interface gains required `SUPABASE_ANON_KEY: string` (needed by `@splash/auth authenticate()` for `/auth/v1/user`; Brief 90 didn't need it because the render-time gate only inspected cookie presence). Audience model per planning Decision 8: `public` → Turnstile verified when `form.turnstile_required`, anonymous submitter, captures `submitter_ip`; `internal` → `authenticate()` validates the cookie, returns 401 `session_expired` JSON on miss (Decision 8b — Brief 90's user-facing JS surfaces "session expired" without losing in-memory form state); `link-only` → opportunistic session capture if cookie valid, anonymous fallback (slug is the gate). CSRF gate via `isOriginAllowed`. `pending_submission_id` UUID-regex validated. Validation errors aggregated into `{error: "validation_failed", fields: {key: msg}}` 422 so user sees every problem in one round-trip. Bundle grew from Brief 90's 152.42 KiB / 27.04 KiB gzip to 872.94 KiB / 163.14 KiB gzip — 5.7× growth from `@splash/auth` + transitive `@supabase/supabase-js`; well inside CF's 3 MiB compressed limit. Operator must `wrangler secret put SUPABASE_ANON_KEY` before internal-audience submits work; flagged in PRE_DEPLOY_FORMS.md Section 2 + wrangler.toml comment block. PRE_DEPLOY_FORMS.md Section 5 gains 8 Brief-91 smoke tests covering all three audiences + idempotency curl + validation_failed + bad_origin defenses | Completed (2026-05-09) | Brief 92 (file/signature uploads need a submission row to attach to), Brief 93 (lookup re-resolve happens inside this submit handler), Brief 96 (submissions admin UI needs real submissions to render) | [brief-091-forms-public-submit.md](brief-091-forms-public-submit.md) |

| 94 | Forms — admin API (CRUD + draft/publish lifecycle + assets + lookup-sources). Sixth user-visible deliverable for the form-builder feature; with this brief landed the worker has its complete admin-side API surface (Brief 95 builds the UI; Brief 96 adds submission-viewer endpoints). New `apps/forms-worker/src/admin/` directory: `auth.ts` exports `adminGate` + `requireServiceKey` mirroring fleet's Brief 83 posture (`session.role === "super_admin"` OR `session.dcRole === "admin"|"super_admin"`; service-key-unbound returns 503); `forms.ts` exports six handlers (list / create / get / updateDraft / publish / statusChange) each calling `requireServiceKey` → `isOriginAllowed` (mutations) → `adminGate` → DB helper, with structured JSON errors throughout; `assets.ts` exports `handleAssetUpload` + `handleAssetDelete` for in-form display images (Image field type; planning Decision 4) — multipart, MIME-sniffed via `file-type`, JPEG/PNG/GIF/WebP only, 10 MB hard cap, R2 path `form-assets/{form_id}/{asset_id}.{ext}`, `form_assets` row written after R2 put with best-effort R2 rollback on row-insert failure; `lookup-sources.ts` returns Brief 89's `LOOKUP_SOURCES` registry as JSON for builder UI inspector dropdowns (cached `private, max-age=300`). New `apps/forms-worker/src/db/admin-forms.ts` exports `listForms` (single PostgREST call with embedded counts via FK column hint `form_versions!form_id(count)` + `form_submissions!form_id(count)` plus a follow-up call resolving `published_at` for `current_version_id`s), `createForm` (3 sequential PostgREST writes — form insert, version insert, draft_version_id back-link — relying on Brief 89's DEFERRABLE FKs for safety; throws `slug_taken` on uniqueness pre-check), `getFormDetail` (form + draft schema + version history), `updateDraft` (PATCH form_versions.schema with `is_draft=true` defense-in-depth filter + form last_edited bump), `publishDraft` (4-call sequence: promote draft → update form.current_version_id + status → insert new draft cloning just-published schema → back-link draft_version_id), `setFormStatus` (unpublish/republish), and asset CRUD helpers (insert/get/delete). All write paths use direct PostgREST `fetch()` matching the Brief 89 pattern in `db/forms.ts`. `apps/forms-worker/src/index.ts` extended with 9 new admin route matchers; comment block at top updated. New `apps/web/app/admin/forms/_lib/worker-fetch.ts` mirrors fleet's Brief 83/87 helper — service-binding-first (`FORMS_WORKER`), URL-fallback for `next dev` — exposes typed wrappers for every Brief 94 endpoint plus shared response interfaces (`FormListItem`, `FormDetail`, etc.). `apps/web/package.json` gains `@splash/forms-schema` workspace dep so apps/web's helper can re-export `FormMeta` / `FormSchema` types Brief 95 will consume. PATCH /draft validates the schema body via `formSchemaSchema.safeParse` from `@splash/forms-schema` — same Zod boundary that protects the public render path in `db/forms.ts:getCurrentVersion`. No Delete endpoint per planning Decision 7 — destructive ops via SQL only; CASCADE FKs from Brief 89 make `DELETE FROM forms WHERE id=...` safe. Atomicity: 3-call create + 4-call publish are sequential PostgREST writes (no transaction wrapper); partial-failure recovery via SQL only — flagged on next admin GET (orphan form with NULL draft_version_id, or dangling current_version_id pointing at a non-draft with no new draft spawned). Acceptable for v1; surface in Outcome.atomic-create. PRE_DEPLOY_FORMS.md Section 5 gains 15 Brief-94 smoke tests covering full lifecycle (list → create → save draft → publish → public render → unpublish → republish), auth gating (401 / 403 / 503), slug + schema validation, CSRF defense, asset upload + delete, and lookup-sources. CLAUDE.md forms-worker glossary extended with the Brief 94 paragraph (endpoint inventory, auth posture, lifecycle state machine, atomicity caveats, no Delete endpoint, apps/web helper). | Completed (2026-05-10) | Brief 95 (admin builder UI consumes every endpoint in this brief), Brief 96 (submissions admin UI builds on this brief's auth posture and `_lib/worker-fetch.ts` helper) | [brief-094-forms-admin-api-crud.md](brief-094-forms-admin-api-crud.md) |
| 95 | Forms — admin builder UI on apps/web (`/admin/forms`, `/admin/forms/new`, `/admin/forms/[id]`). Seventh user-visible deliverable for the form-builder feature; with this brief landed operators can build/save/publish forms entirely via the browser. 3-column builder (palette / canvas / inspector) with `useReducer` (9 actions) for canvas+inspector state and `@dnd-kit/core` + `@dnd-kit/sortable` for drag-to-reorder (PointerSensor 4px activation + KeyboardSensor for Tab/Space/arrow accessibility). 16 per-field-type modules under `apps/web/app/admin/forms/[id]/_field-types/{type}/{Renderer.tsx,Inspector.tsx,index.ts}` plus `_shared/` helpers (LabeledInput, LabeledTextarea, LabeledCheckbox, LabeledSelect, KeyEditor with snake_case sanitizer, OptionListEditor for dropdown/multi). `nanoid(8)` for field IDs, `nanoid(6)` for key suffixes. New apps/web deps: `@dnd-kit/core@^6.1.0`, `@dnd-kit/sortable@^8.0.0`, `nanoid@^5.0.0`. Save Draft + Publish via two thin server actions in `apps/web/app/admin/forms/[id]/actions.ts` that wrap `updateDraftAdmin` + `publishFormAdmin` from the SSR worker-fetch helper (BuilderClient is a `"use client"` component and can't import `next/headers` directly — surfaced as a build error and resolved with the action shim). Beforeunload warning when dirty. New `apps/web/app/admin/forms/_components/{FormsAdminTabs,NoAccessCard}.tsx` mirror SignupAdminTabs (Brief 56) and the sysadmin NoAccessCard (Brief 7) respectively; FormsAdminTabs is pathname-driven and only shows the per-form sub-tabs (Builder / Submissions / Versions) when `formId` is provided — Brief 96 wires Submissions and Brief 98 wires Versions. Same admin gate as fleet (Brief 83): `session.role === "super_admin"` OR `session.dcRole === "admin"|"super_admin"`. Pages render NoAccessCard on misses; worker re-validates every PATCH/POST as defense in depth. Bundle: route-specific 25.8 kB / First-Load 131 kB on `/admin/forms/[id]` — under the 150 kB target flagged in the brief Report. KeyEditor sanitizes on every keystroke (lowercase + strip non-alphanumeric + drop leading non-letters); invalid result paints the input red. PRE_DEPLOY_FORMS.md Section 5 gains 18 Brief-95 smoke tests. CLAUDE.md forms-worker glossary extended with the Brief 95 paragraph. Latent issue (form-meta persistence): the FormMetaInspector renders editable inputs for title/description/audience/notify_webhook/etc. and the TopBar title is editable, but Save Draft only persists `schema.fields` to the PATCH /draft endpoint — form-level metadata edits stay client-side until a future brief widens the admin endpoint. | Completed (2026-05-10) | Brief 96 (submissions admin UI — reuses FormsAdminTabs + worker-fetch helper + per-field-type renderers concept), Brief 98 (polish — error boundary, dashboard tile, Versions tab content) | [brief-095-forms-admin-builder-ui.md](brief-095-forms-admin-builder-ui.md) |
| 96 | Forms — submissions admin UI + version history. Eighth user-visible deliverable for the form-builder feature; with this brief landed operators have a complete admin surface for built forms — list, build, see submissions, annotate, export CSV. Three new apps/web pages: `/admin/forms/[id]/submissions` (list with DateRangePicker default last-30-days + status filter + submitter-kind filter + CSV export — list columns are meta-only because schemas vary across versions per planning Decision 7), `/admin/forms/[id]/submissions/[subId]` (detail page with single-button ActionForm POSTing both Status + Splash Notes via Brief 19 pattern, payload rendered against THE SUBMISSION'S version's schema, metadata grid below), and `/admin/forms/[id]/versions` (audit-trail table — Version / Status / Published at + by / Field count / Submission count; no diff renderer at v1). Five new worker endpoints under `/forms/admin/api/forms/{id}/{submissions{,.csv,/{subId}},versions}` extending Brief 94's admin gate (super_admin OR dcRole admin/super_admin); PATCH gated on `isOriginAllowed` (CSRF defense-in-depth); `(form_id, submission_id)` tuple scoping on read + PATCH so admin can't touch a submission belonging to another form by guessing the UUID. CSV is **schema-union across all versions in the date range** — header row is the union of every field key ever used (display-only `heading` / `image` skipped), rows have empty cells where a field doesn't exist on a given submission's version. Same-origin URL works because forms is path-carved (Brief 89 / Decision 2) — no Brief 88-style proxy route. Status enum (`new` / `in_progress` / `closed`) + splash_notes mirror fleet Brief 87 last-write-wins; audit columns (`status_updated_{at,by}` / `splash_notes_updated_{at,by}`) from Brief 89 schema get stamped automatically on each PATCH. PayloadRenderer dispatches per field type: text/email/phone render plain; multi-line strings render in `<pre>`; dropdown/multi map values to labels; lookup gets a `(resolved from <key field label>)` annotation; file/signature entries thumbnail-display image MIME inline via `/forms/admin/api/files/{r2_key}` (Brief 92 serve route) and link for non-image MIME; "Other payload entries" appendix renders any payload key not present in the version's schema (defense against schema drift / hand-edited JSONB). PostgREST list embed is `version:form_versions!inner(version_number)` (many-to-one FK embeds as a single nested object, not array — confirmed against the actual response shape). New worker DB module `apps/forms-worker/src/db/admin-submissions.ts` exposes `listSubmissions` / `getSubmission` / `updateSubmission` / `listSubmissionsForCsv` / `listVersionsWithCounts` (all direct PostgREST `fetch()`, matching Brief 89/94 pattern). New worker handlers `apps/forms-worker/src/admin/{submissions,versions}.ts`. apps/web extends the Brief 94 worker-fetch helper with five new methods. CSV row cap 10000; 416 on overflow. List default limit 200; `limit_hit` boolean returned so the UI shows a "narrow the date range" amber banner. No new env vars, no new bindings. | Completed (2026-05-10) | Brief 98 (polish — error boundary, dashboard tile, Versions tab diff renderer) | [brief-096-forms-submissions-admin-ui.md](brief-096-forms-submissions-admin-ui.md) |
| 98 | Forms — polish (dashboard tile, segment-level error boundary, CF rate-limit operator step, final smoke pass). Tenth and final brief in the form-builder feature; with this brief landed the feature is shippable end-to-end on workers.dev / staging. Three small additions plus one large documentation pass: (1) Dashboard tile "Forms" appended to `/admin/dashboard`'s tile grid (`/admin/forms` href, "Builder" eyebrow, "Build and manage admin-built forms." description, lucide ClipboardList icon inlined per the existing convention used by sibling tiles — the page uses inline SVGs, not lucide-react components); per Brief 78 precedent the tile renders unconditionally and per-tool access is enforced at the destination via Brief 95's NoAccessCard. (2) `apps/web/app/admin/forms/error.tsx` segment-level boundary mirroring `apps/web/app/admin/error.tsx` (Brief 31): detects `UnrecognizedActionError` via the `"Server Action"` + `"was not found on the server"` message-substring check, renders "App was updated / Reload" with `window.location.reload()` on the stale-action branch and "Couldn't load form builder / Try again" with `reset()` on the generic branch. The forms-specific boundary lives below the global admin one and gives forms-aware error copy for any `/admin/forms/*` page throw. (3) `apps/web/middleware.ts` `ADMIN_KNOWN_SUBPATHS` set extended with `"forms"` — without this entry the legacy `/admin/{slug}` redirect rule (Brief 2) would 308 `/admin/forms` to `/admin/pricing/forms` and break the route. (4) PRE_DEPLOY_FORMS.md large documentation pass: Section 4 (Cutover plan — pre-flight checks, route binding, post-cutover smoke + monitor + ops); Section 5 gains the top-level "Run all in order" note plus 5 Brief-98 smoke tests (dashboard tile click; legacy-redirect non-interception; segment-level error boundary fires on a forced throw with `reset()` recovery; stale-action branch fires after a real redeploy of `splash-web`; CF rate-limit verification — 6 POSTs in 5 min from one IP yields a 429 on request 6, action expires after 10 min); Section 6 (Known limitations / v2 candidates — 12 entries: per-form webhook URLs, auto-save, per-location scoping, submitter self-view, version diff renderer, multi-file upload UX, cross-form CSV, Delete from UI, edge caching of public form HTML, image dimensions, per-form rate limit overrides, form-meta persistence); Section 7 (the operator step for the CF Rate Limiting Rule — `splash-forms-submit-rate-limit`, `URI Path contains "/forms/api/submit/"` AND POST, Block, IP source, 5-req/5-min, 10-min action duration; lives at the zone level, NOT in worker code). No CF rate-limit rule was actually created — operator-driven step per Decision 8c; documented but not provisioned. CLAUDE.md unchanged for this brief — the form-builder glossary section is already comprehensive across Briefs 89–97; Brief 98's polish bullets are pure UI/middleware/docs. | Completed (2026-05-10) | none — wraps the form-builder feature | [brief-098-forms-polish-and-smoke-tests.md](brief-098-forms-polish-and-smoke-tests.md) |
| 97 | Forms — submission webhook + daily R2 cleanup cron. Ninth user-visible deliverable for the form-builder feature; with this brief landed the worker has its complete runtime — Power Automate gets pinged on every successful submission (when bound), and orphan R2 cleanup happens daily without operator intervention. New `apps/forms-worker/src/submit/webhook.ts` exports `fireSubmissionWebhook` — POSTs the submission payload to `FORMS_SUBMISSION_WEBHOOK_URL` (worker secret, optional, fail-soft when unbound) with 15s `AbortController` timeout; per-form opt-out via `forms.notify_webhook = false`. Files-by-URL in payload (NOT base64 — planning Decision 6): each file/signature entry carries `download_url` pointing at `/forms/admin/api/files/{r2_key}` (Brief 92 admin-gated serve route). PA fetches the URL when needed, dodging the >3MB base64 cap pain that hit damage-worker. `inferAdminBase` helper rewrites `*.workers.dev` request origins to `https://splashcarwashes.info` for the `splash_admin_url` link in the payload (operators click these from email; staging/workers.dev URLs would 404 for recipients). `apps/forms-worker/src/submit/index.ts` extended: `handleSubmit` signature gains `ctx: ExecutionContext` parameter (plumbed through from the worker fetch handler), webhook fire wrapped in `ctx.waitUntil` after `inserted.wasNew && fileRowsToInsert` insert lands — does NOT block the success page response; SKIPPED on idempotent re-submits so a network retry never delivers duplicate PA notifications; in-place type fix on the `inserted` declaration (was `{row: {id: string}}`, now `{row: SubmissionRow}` to match the helper's actual return type — caught by the typecheck pass after webhook payload construction needed `submitterKind` / `submitterEmail` / `submittedAt` fields). New `apps/forms-worker/src/cron/cleanup.ts` exports `runDailyCleanup(env)`: two passes, both fail-soft. Pass 1 (orphan submission files) lists R2 under `form-submission-files/`, groups objects by `pending_submission_id` extracted from the path, queries Supabase `form_submissions.id IN (...)`, deletes R2 objects with no matching row AND `uploaded > cutoff` (24h grace covers the gap between out-of-band uploads from Brief 92 and the eventual submit POST); hard pagination cap 50 pages × 1000 = 50K objects per run. Pass 2 (orphan form assets) lists R2 under `form-assets/`, queries `form_assets.r2_key IN (...)`, deletes R2 objects with no matching row AND uploaded >1h ago (conservative grace against race with fresh asset uploads); hard pagination cap 20 pages × 1000 = 20K objects. Returns `{submissionFilesDeleted, assetsDeleted, submissionPagesScanned, assetPagesScanned, errors}`; logs `[forms.cleanup] complete` with counts on every run. `apps/forms-worker/src/index.ts` default export now `{fetch, scheduled}` — scheduled handler runs `runDailyCleanup` via `ctx.waitUntil` and logs `[forms.cleanup.cron] result`. Submit route plumbs `ctx` through (was `_ctx`). `apps/forms-worker/wrangler.toml` `[triggers] crons = ["0 11 * * *"]` — 11:00 UTC slot picked to NOT collide with damage-worker's daily summary at 13:00 UTC (Brief 65) or workorders-worker's MaintainX sync at 11:30 UTC (Brief 71); the `[observability.logs]` block from Brief 89 covers scheduled invocations automatically (eventType: scheduled in CF dashboard). PRE_DEPLOY_FORMS.md Section 5 gains 10 Brief-97 smoke tests (webhook fire + opt-out + file-URL payload + idempotency-no-double-fire + cron manual trigger + pre-create-orphan submission file + pre-create-orphan asset + Workers Logs verification + webhook 5xx fail-soft). CLAUDE.md forms-worker glossary extended with the Brief 97 paragraph. No new env vars beyond the optional `FORMS_SUBMISSION_WEBHOOK_URL`; no new bindings. | Completed (2026-05-10) | Brief 98 (polish — final smoke pass should include webhook + cron observability) | [brief-097-forms-webhook-cron-cleanup.md](brief-097-forms-webhook-cron-cleanup.md) |

| 100 | Forms admin list — audience filter + copy-link button. Twelfth brief in the form-builder feature; strictly additive UX pass on the existing `/admin/forms` page with no schema, auth, or env changes. Worker side: `ListFormsFilter` in `apps/forms-worker/src/db/admin-forms.ts` gains `audience?: string` and the helper appends `audience=eq.<value>` to the PostgREST URL when set and not `"all"` (mirrors the existing `status` clause); `handleListForms` in `apps/forms-worker/src/admin/forms.ts` reads `audience` from query, validates against the 4-value allow-list (`public` / `internal` / `link-only` / `all`), returns 400 `bad_audience` on miss, passes through to the filter when not `"all"`. apps/web side: `ListFormsParams` in `apps/web/app/admin/forms/_lib/worker-fetch.ts` gains `audience?: string` and `listFormsAdmin` forwards it as a query param; `apps/web/app/admin/forms/page.tsx` reads the `audience` searchParam, renders a third dropdown next to Status (Audience: All / Public / Internal / Link-only) via the existing plain-GET form (no client JS for the filter), and passes through to `listFormsAdmin`. Public-link column added to `<FormsTable>`: published rows render the new `<CopyLinkButton>` client island (`apps/web/app/admin/forms/_components/CopyLinkButton.tsx`); draft/archived rows render an em-dash because `/forms/{slug}` returns 404 until publication. CopyLinkButton uses `navigator.clipboard.writeText(${window.location.origin}/forms/${slug})` with a 2-second "Copied ✓" affordance and a `window.prompt()` fallback for browsers where the Clipboard API rejects (matches the Brief 85 relative-URL convention so the copied link works unchanged on workers.dev / staging / production cutover). PRE_DEPLOY_FORMS.md Section 5 gains 8 Brief-100 smoke tests (audience All / Internal / Public / Link-only / bad-value 400 + copy-link published / unpublished / clipboard fallback). CLAUDE.md forms-worker glossary extended with the Brief 100 paragraph. Bundle: `/admin/forms` First-Load JS 106 kB (was ~104 kB) — small CopyLinkButton client island. All 5 validation steps green: forms-worker typecheck + deploy:dry-run (substituted for `build`; no monorepo worker has a `build` script), apps/web typecheck + build, root `pnpm typecheck` 17/17. | Completed (2026-05-10) | none — strictly additive on top of the form-builder feature | [brief-100-forms-admin-list-audience-filter-and-copy-link.md](brief-100-forms-admin-list-audience-filter-and-copy-link.md) |

| 99 | Forms — `/forms` index page for credentialed users (visible-to-me). Eleventh brief in the form-builder feature; landed AFTER the 10-brief feature was declared done in Brief 98 — strictly additive discovery layer with no schema changes. New `apps/forms-worker/src/visible-to-me.ts` exports `handleVisibleToMe` for `GET /forms/api/visible-to-me`: `requireServiceKey` → `authenticate()` (any valid session, not gated to admin) → direct PostgREST `fetch()` on `forms` filtered to `status='published' AND audience='internal'`, ordered by title asc, limit 500, returns `{forms: [{slug, title, description, audience}]}`. Endpoint name is intentionally semantic ("visible to me") so option 3 (per-role / per-location visibility) is a strictly additive filter inside the handler with no contract change. Forward-compat: response always includes `audience` even though v1 only returns `"internal"` rows. New `apps/web/app/forms/_lib/worker-fetch.ts` mirrors the Brief 17 service-binding pattern (FORMS_WORKER binding first, URL fallback for `next dev`); returns `[]` on 401 so the page can render its signed-out state without throwing. New `apps/web/app/forms/page.tsx` server component renders an alphabetical cards grid (max-w-4xl, sm:grid-cols-2) with title + description + audience badge per card; `force-dynamic` so the session-scoped list never gets statically cached. AudienceBadge component handles all three audience values for forward compat with option 3 even though v1 only renders "internal" badges. EmptyState card when no forms are visible. `apps/web/app/_components/Header.tsx` `ADMIN_PATH_RE` extended from `^/(admin\|sysadmin\|workorders)(/\|$)` to include `forms` — admin chrome (Dashboard / Sign Out / role badge / Change Password link) renders on `/forms`. Note: `/forms/{slug}` (the public render) is served by splash-forms worker via path-carved CF route, not apps/web — the Header regex only ever runs on apps/web-rendered pages, so the change is safe with respect to the public render path. `apps/web/middleware.ts` matcher extended with `/forms/:path*` (option B per brief Phase 5 — prefix rather than path-equality; cleaner because the apps/web side has only one prefix to think about; the prefix is effectively a no-op for `/forms/{slug}` since those requests never reach apps/web's edge). `forms` was already in `ADMIN_KNOWN_SUBPATHS` (Brief 98), so the legacy `/admin/{slug}` redirect rule continues to no-op for `/admin/forms`. PRE_DEPLOY_FORMS.md Section 4 gains a "Brief 99 routing note" warning about CF route-pattern matching of bare `/forms` vs `/forms/*` at production cutover; Section 5 gains 7 Brief-99 smoke tests (endpoint auth gate / pass / page render / signed-out redirect / empty state / audience filter forward compat / Header gate); Section 6 gains 4 v2-candidate entries (per-role/per-location visibility option 3, dashboard tile for end users, recently-used / favorites, link-only on the index). CLAUDE.md "Real pages" list extended with `/forms`; forms-worker glossary paragraph extended with the visible-to-me endpoint description. No new env vars. No new secrets. No new bindings (apps/web's `FORMS_WORKER` from Brief 94 covers this brief). No schema migrations. Bundle: route-specific 164 B / First-Load 105 kB on `/forms` — well under the 80 kB target the brief Report flagged (the 105 kB First Load includes the shared chunks; the per-route surface is 164 B). | Completed (2026-05-10) | none — strictly additive on top of the form-builder feature | [brief-099-forms-visible-to-me-index.md](brief-099-forms-visible-to-me-index.md) |

| 101 | Damage — manage-page update notifications (note + status-change webhook) + "Needs quotes" UI indicator. Fully additive. New `CLAIM_UPDATE_WEBHOOK_URL` secret on damage-worker (optional, fail-soft when unbound). New `apps/damage-worker/src/notifications.ts` exports `STATUS_NOTIFIES_NEXT` (5-entry `to`-keyed map: `Pending GM Review` / `No Responsibility — Pending Review` / `Approved — Pending Quotes` → site_email; `Pending RM Review` / `Pending RM Quote Approval` → rm_email; every other destination is no-notify), `resolveRecipients` (case-insensitive actor-exclusion + dedupe + lowercased), `fireClaimUpdateWebhook` (15s AbortSignal timeout, non-2xx logged + swallowed), and the `ClaimUpdateWebhookPayload` type. `handleAddNote` fires `notifyClaimUpdate({changeType:"note"})` after the D1 write commits with `notifies = "both"` (rm_email + site_email). `handleStatusTransition` fires `notifyClaimUpdate({changeType:"status"})` after the batch commits and before the MaintainX hook with `notifies` resolved from `STATUS_NOTIFIES_NEXT[finalTo]`; non-mapped destinations exit cleanly. `packages/db-supabase/src/locations.ts` widens `getLocationContactInfo` to return `{site_email, rm_email}` — additive; the existing Brief 32 customer-webhook call site reads only `contact.site_email` and is unaffected. New `apps/web/app/admin/damage/_components/StatusActionPill.tsx` renders an amber "Needs quotes" pill beside `Approved — Pending Quotes` statuses on `/admin/damage` and `/admin/damage/[id]`. PRE_DEPLOY_DAMAGE.md gains the Brief 101 section with the status→recipient map, payload shape, and PA-flow setup steps. CLAUDE.md glossary gains the `CLAIM_UPDATE_WEBHOOK_URL` entry. No schema change. No D1 migration. No Supabase column add. Validation: root `pnpm typecheck` 17/17 green; `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1723 KiB / 390 KiB gzip; `pnpm --filter @splash/web build` succeeds. | Completed (2026-05-11) | none — strictly additive | [brief-101-damage-manage-update-notifications.md](brief-101-damage-manage-update-notifications.md) |

| 103 | Workorders — work-request form drops the auto-pre-fill on the Requester Name input. Single-file UX change in `apps/web/app/workorders/_components/NewRequestForm.tsx`: `defaultValue={currentUser.full_name ?? ""}` → `defaultValue=""`, add `placeholder="Your name"`, and add a muted help-text `<p>` below the input ("Enter the name of the person submitting this request — even when using a shared location account."). Rationale: shared per-location MaintainX accounts (e.g., `binghamtonwash@splashcarwashes.com` whose `full_name` is "Binghamton Wash") were silently degrading submitter attribution — the location label auto-populated, operators hit submit, the WO description footer recorded the location name instead of the human submitter. Operator confirmed shared accounts are the common case; ~30 individually-owned accounts trade small typing friction for reliable attribution across the fleet. Alternatives (email-pattern detection / `maintainx_users.is_shared` flag) rejected as brittle. No worker change, no API contract change — `currentUser.full_name` stays on `/workorders/api/list` (harmless, ignored by the form now; removable as v2 cleanup). MaintainX `creatorContactInfo` unchanged (still the session email, including shared-account emails). Validation: root `pnpm typecheck` 17/17 green; `pnpm --filter @splash/web build` succeeds (`/workorders` route-specific 5.45 kB / First-Load 107 kB — unchanged vs Brief 102 baseline). | Completed (2026-05-11) | none — strictly additive UX | [brief-103-workorders-request-no-prefill-requester-name.md](brief-103-workorders-request-no-prefill-requester-name.md) |

| 102 | Damage — internal new-claim notification (rm/site/am/incidents alert with PDF + photo links). Fully additive; parallel to Brief 32's customer-email webhook in the same `handleClaimSubmission` post-submit block. New `INTERNAL_NEW_CLAIM_WEBHOOK_URL` secret on damage-worker (optional, fail-soft when unbound) plus new `INCIDENTS_EMAIL` `[vars]` entry (placeholder `incidents@splashcarwashes.com`; operator confirms or amends before push). Third widening of `getLocationContactInfo` — return shape now `{site_email, rm_email, am_email}` so the single `pricing_simple` query covers customer-webhook (Brief 32, reads site_email), claim-update-webhook (Brief 101, reads site_email + rm_email), and now internal-new-claim-webhook (Brief 102, reads all three). Brief 101's call sites still compile because they consume a structural subset. `apps/damage-worker/src/notifications.ts` extended with `ClaimPhotoForWebhook` / `InternalNewClaimPayload` types, `resolveInternalRecipients` (lowercase + dedupe + drop nulls/blanks; rm → site → am → incidents order; NO actor exclusion — customer submitter is not a location contact), and `fireInternalNewClaimWebhook` (15s AbortSignal timeout, non-2xx logged + swallowed, mirrors Brief 101's fire helper). New private `fireInternalNewClaimNotification` in `apps/damage-worker/src/index.ts` (placed next to `fireCustomerClaimWebhook`) — resolves contacts, lists photos via `listPhotosForClaim` and maps to `{url, mime, original_filename, photo_type, uploaded_at}` against the request origin (same pattern as `summaryPdfUrl`), builds the payload with the same `CUSTOMER_WEBHOOK_BASE64_MAX_BYTES` (~3 MB raw) PDF base64 ceiling, and fires. Reuses Brief 32's `pdfBytes` + `summaryPdfUrl` — no duplicate PDF generation. Fired AFTER the Brief 32 customer-webhook in the same try-block; wrapped in its own try/catch as defense-in-depth. Photo URLs assemble against `baseOrigin` so dev / staging / production all serve correctly without per-env hardcode. PRE_DEPLOY_DAMAGE.md gains the Brief 102 section with recipient resolution table, full payload sample, and PA-flow setup steps; the secrets/vars table gains rows for `INTERNAL_NEW_CLAIM_WEBHOOK_URL` and `INCIDENTS_EMAIL`. CLAUDE.md glossary gains `INTERNAL_NEW_CLAIM_WEBHOOK_URL` + `INCIDENTS_EMAIL` entries and the damage-worker bindings paragraph notes the new env vars. No schema change. No D1 migration. No Supabase column add. Validation: root `pnpm typecheck` 17/17 green; `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1728.60 KiB raw / 391.03 KiB gzipped (≈ +5 KiB raw vs Brief 101 baseline). | Completed (2026-05-11) | none — strictly additive | [brief-102-damage-new-claim-internal-alert.md](brief-102-damage-new-claim-internal-alert.md) |

| 104 | Damage — strip `claims/` prefix in internal-new-claim webhook photo URLs. Single-line bug fix in `apps/damage-worker/src/index.ts` `fireInternalNewClaimNotification` (~L3402): photo URLs built by Brief 102 were `${baseOrigin}/claims-api/photo/${p.r2_key}` verbatim, but `r2_key` already includes the `claims/` prefix that `serveClaimPhoto` (packages/storage-r2) re-prepends before the R2 `.get()` — every photo link in the internal email resolved to "Photo not found" (`/claims-api/photo/claims/MIL-…/damage_1.jpg` → R2 lookup against `claims/claims/MIL-…/damage_1.jpg`). Fixed by replicating the existing `damagePhotoUrl` helper pattern (`apps/web/app/admin/damage/_lib/worker-fetch.ts` L140-144): strip the leading `claims/` (defensive `.startsWith` check) then split on `/`, URL-encode each segment, rejoin. Preserved existing field-name shape (`p.content_type` → `mime`, `p.filename` → `original_filename`) — brief's verbatim example used `p.mime` / `p.original_filename` which don't exist on the row type. No data / contract / wrangler.toml change; PA Flow B is unaffected since the photo URL field name (`triggerBody()?['photos']?[*]?['url']`) is unchanged. Customer-claim webhook (Brief 32) unaffected — it doesn't include photo URLs in its payload. Out of scope: extracting a shared `buildPhotoServeUrl` helper in `packages/storage-r2` (3 lines of duplication; v2 candidate when a third caller appears) and changing `serveClaimPhoto` to accept either prefixed or unprefixed keys (contract change with risk of breaking existing callers). Validation: root `pnpm typecheck` 17/17 green; `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1728.81 KiB raw / 391.10 KiB gzipped (≈ unchanged vs Brief 102 baseline). | Completed (2026-05-11) | none — bug fix in a fail-soft optional webhook | [brief-104-internal-claim-webhook-photo-url-strip-claims-prefix.md](brief-104-internal-claim-webhook-photo-url-strip-claims-prefix.md) |

| 105 | Fleet submissions — dashboard status editor + per-edit webhook to PA for SharePoint sync. Brief 87's `PATCH /admin/api/submissions/{id}` widened from `splash_notes`-only to accept either or both `splash_notes` and `status` (`new`/`reviewed`/`contacted`/`closed` enum, worker-validated). Four new audit columns on `fleet_submissions` — `status_updated_{at,by}` and `splash_notes_updated_{at,by}` — stamped server-side per-field on each PATCH (operator runs Phase 1 SQL in Supabase Editor; no migration framework in this repo). New `FLEET_SUBMISSION_UPDATE_WEBHOOK_URL` worker secret (optional, fail-soft when unbound — dashboard PATCH still succeeds, SharePoint just lags). On every successful PATCH the worker fires a single POST with `{id, change_type, changed_fields, actor:{email}, row}` so PA can upsert SharePoint by submission id without a separate read. New `fireFleetSubmissionUpdateWebhook` helper co-located at the bottom of `apps/fleet-inquiry-worker/src/admin.js` (worker is verbatim-lifted JS per Brief 81; modularity gain from a separate file is small). 15s `AbortSignal.timeout` matches Brief 101 / Brief 102 / Brief 32 fail-soft posture; non-2xx + throws logged as `[fleet-submission-update]` and swallowed. The 30-min PA new-submission ingest flow stays untouched — Brief 105 adds a parallel update channel keyed on dashboard edits only. apps/web side: new `apps/web/app/admin/fleet/_lib/constants.ts` exports `FLEET_STATUS_OPTIONS` + `FLEET_STATUS_PILL_CLASS` so worker validator / server-action defense-in-depth check / detail-page dropdown / list-page `StatusPill` share one allow-list and one color map. `updateFleetSubmissionNotes(id, notes)` helper renamed to `updateFleetSubmission(id, {splashNotes?, status?})` (parameter-object pattern); server action renamed `updateSplashNotesAction` → `updateSubmissionAction` and now reads both `splash_notes` + `status` from FormData on every save (single ActionForm submit covers both fields). Detail-page "Splash Notes" section relabeled "Status & Notes" with the new dropdown above the textarea and a single "Save" button driving the combined PATCH. `StatusPill` widened to four discrete colors (blue/sudsy/amber/emerald; default neutral for unexpected values). PRE_DEPLOY_FLEET.md gains a Brief 105 section with the secret table, payload shape, sample bodies (notes-only, status-only, combined), and PA-flow setup steps. CLAUDE.md Fleet inquiries admin glossary entry extended with the dashboard status-editing note + new `FLEET_SUBMISSION_UPDATE_WEBHOOK_URL` glossary entry. Out of scope (deferred to v2): bidirectional SharePoint→Supabase sync, per-status notification rules, per-actor permission scoping, backfill of audit columns on existing rows, one-time SharePoint backfill from Supabase, full audit-log history beyond last-write-wins. Validation: root `pnpm typecheck` 17/17 green; `pnpm --filter @splash/web build` succeeds (`/admin/fleet/[id]` 739 B / 106 kB First Load); `pnpm --filter @splash/fleet-inquiry-worker exec wrangler deploy --dry-run` 786.25 KiB / 149.73 KiB gzip (≈ +1 KiB vs Brief 88 baseline). | Completed (2026-05-11) | none — strictly additive (new audit columns + new optional secret) | [brief-105-fleet-submissions-status-editor-and-update-webhook.md](brief-105-fleet-submissions-status-editor-and-update-webhook.md) |

| 106 | Fleet admin — render `submitted_at` instead of `created_at` on `/admin/fleet` list + `/admin/fleet/[id]` detail. Three render-site fixes for a visible bug: SUBMITTED column on the list was empty for every row because `fleet_submissions.created_at` is unpopulated (worker writes `submitted_at` explicitly per Brief 86). Detail page had the same bug in two places — the "Created at" key/value row was always blank, and the under-title "Submitted N days ago" caption read `created_at` too. Brief 86 fixed the worker-side filter / sort + CSV ordering; this brief closes the apps/web render-time gap. List-row Submitted cell + under-title caption now read `row.submitted_at`; the always-blank "Created at" entry is dropped entirely from the detail page's key/value grid. Option A on the type: `created_at: string` stays on `FleetSubmissionRow` in `apps/web/app/admin/fleet/_lib/worker-fetch.ts` — harmless leftover, worker still `SELECT *`s, no downstream consumers after the fix. CLAUDE.md "Fleet inquiries admin" glossary entry gains a one-liner noting that apps/web reads `submitted_at` post-Brief-106 (matching authoritative timestamp post-Brief-86). No worker change, no schema change, no env/secret. Validation: root `pnpm typecheck` 17/17 green; `pnpm --filter @splash/web build` succeeds (`/admin/fleet` 920 B / 106 kB First Load, `/admin/fleet/[id]` 739 B / 106 kB First Load — unchanged vs Brief 105 baseline). | Completed (2026-05-11) | none — visible bug fix in apps/web rendering only | [brief-106-fleet-admin-use-submitted-at-not-created-at.md](brief-106-fleet-admin-use-submitted-at-not-created-at.md) |

| 126 | Custom forms — "My Requests" view at `/admin/my-requests` + dashboard tile in the Submissions group. Companion to Brief 121's Pending Approvals: Approvals surfaces items waiting on YOU; My Requests surfaces submissions YOU submitted with their current status (waiting on whom, approved, denied). New worker endpoint `GET /forms/admin/api/my-requests` (any-session auth; scoped by `form_submissions.submitter_email=eq.{caller_email_lowercase}` — Brief 120 normalizes the column at insert time, so case-insensitive match works without ilike). Query params: `status=waiting|done|all` (default all) + `limit` (default 200, max 500) + `offset` (default 0). Server-side resolution per row: `status_kind` ("waiting" when stage has `approver_source`, "outcome" when terminal — matches Brief 125 outcome predicate); `status_tint` ("info" for in-flight, else Brief 125 `stage.tint` if set, falling back to label-keyword heuristic — `/\bapprov/` → success, `/\bden/` → danger, else neutral); `outcome_reached_at` (newest `workflow_history` entry whose `to` matches the current stage; null while waiting); `stage_label` resolved off the row's version schema; `detail_path` = `/admin/forms/{form_id}/submissions/{submission_id}`. New apps/web page `/admin/my-requests/page.tsx` — any-session gate (worker query naturally returns empty for callers with no submissions). Filter tabs (All / Waiting / Approved / Denied) — All/Waiting drive the worker `status` param; Approved/Denied request `status=done` and narrow client-side by `status_tint`. Pagination via offset (Prev / Next, page size 50). Per-row Status pill (info/success/danger/warning/neutral palettes), "Waiting on" sub-line listing approver emails for in-flight rows, "Reached" timestamp for outcome rows (formatEst per Brief 113). Empty states per tab; avoids "stage" / "outcome" / schema vocabulary in operator-facing copy. New "My Requests" tile in the Submissions group on `/admin/dashboard` (anySession). New `apps/web/middleware.ts` `ADMIN_KNOWN_SUBPATHS` entry for `"my-requests"` (CLAUDE.md mandatory rule — prevents the legacy `/admin/{slug}` → `/admin/pricing/{slug}` redirect from rewriting `/admin/my-requests` to a 404). New `_lib/worker-fetch.ts` helper `listMyRequestsAdmin({status?,limit?,offset?})` mirrors `listPendingApprovalsAdmin`. Files created: `apps/forms-worker/src/admin/my-requests.ts` (~210 LOC), `apps/web/app/admin/my-requests/page.tsx` (~270 LOC). Files modified: `apps/forms-worker/src/index.ts` (route mount + header comment), `apps/web/app/admin/forms/_lib/worker-fetch.ts` (new helper + types), `apps/web/app/admin/dashboard/_lib/tiles.tsx` (My Requests tile + sendIcon SVG), `apps/web/middleware.ts` (ADMIN_KNOWN_SUBPATHS), `CLAUDE.md` (forms-worker glossary Brief 126 paragraph), `BRIEFS/INDEX.md` + `BUILD_STATE.md` + brief Outcome. No Supabase / R2 / wrangler.toml / secret changes (Brief 120 owns the underlying columns). Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/my-requests` 193 B / 107 kB First-Load JS; other routes unchanged); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1108.89 KiB raw / 212.49 KiB gzipped (≈ +5 KiB raw / +1 KiB gzip vs Brief 125 baseline, all my-requests handler). Out of scope per the brief: cancel/withdraw, re-submit denied, in-app workflow history rendering (per-submission detail page already has the timeline), email notifications on outcome (Brief 125 owns), submissions without a workflow (filtered out — no status to display). | Completed (2026-05-14) | none — strictly additive on top of Brief 120/121/125 | [brief-126-forms-my-requests-view.md](brief-126-forms-my-requests-view.md) |

| 125 | Custom forms — workflow builder UX redesign + outcome notification webhook. Top-level Form tabs (Fields / Workflow / Settings) replace the right-panel Inspector workflow editor from Brief 120/123. New `/admin/forms/[id]?tab=workflow` renders the redesign: entry "Form submitted" node → vertical step cards (drag-to-reorder via `@dnd-kit/sortable`) → "+ Add approval step" → Outcomes pill row (Approved + Denied seeded; Add outcome / tint picker / remove with confirm) → Notifications panel → Flow preview (Mermaid, lazy-loaded; renamed from `WorkflowMermaidPreview` to `WorkflowFlowPreview` with new entry / step / outcome node classes). New per-step "Who approves?" picker auto-detects email-shaped lookup fields, email-type fields, and location fields from the form's schema; "Specific person" / "Multiple people" options use a new org-directory autosuggest backed by `GET /forms/admin/api/users/search?q=` (admin-tier gate; matches `email` ilike substring against `auth_unified`; 20-row cap). Each step has a grid of action sub-cards (Approve / Deny / custom). Action sub-cards expose "Required action" checkboxes (Signature / Note / Typed name) and a "Then go to" dropdown listing every Outcome + every other Step. No operator-facing surface anywhere in the Workflow tab uses schema vocabulary (stage / transition / approver_source / payload_field / site_role / terminal / default_stage). Schema additions (additive + optional): `WorkflowStage.kind?: "step" | "outcome"` (UI bucket hint; predicate-derived when omitted), `WorkflowStage.tint?: "success" | "danger" | "warning" | "info" | "neutral"` (outcome pill color), `FormWorkflow.notifications?: { notify_approver_on_assignment?, notify_submitter_on_outcome?, notify_approvers_on_outcome? }` (defaults true/true/false applied at read time via `getWorkflowNotifications`). Brief 120/123 schemas continue to render — outcome stages without `kind` fall through to the predicate detection. New optional secret `FORMS_OUTCOME_NOTIFICATION_WEBHOOK_URL` (single PA flow handles both assignment + outcome events, discriminated by a top-level `type` field). Worker fires assignment events on submit-time default-stage seed AND on every transition into a non-outcome destination with a non-empty `current_approver_emails`; fires outcome events on every transition into an outcome stage with recipient list = submitter (when opt-in) + acted-on approvers from `workflow_history` (when opt-in). Both fires are fail-soft + 15s timeout + ctx.waitUntil-ed. Actor-exclusion: caller's own email stripped from assignment recipient list. New `apps/forms-worker/src/notifications.ts` is the canonical home for forms notification fires; the existing submission webhook can move here in a future cleanup. Files created: `packages/forms-schema/src/types.ts` (schema additions appended), `apps/forms-worker/src/{admin/users-search,notifications}.ts`, `apps/web/app/admin/forms/[id]/_components/FormBuilderTabs.tsx`, `apps/web/app/admin/forms/[id]/_workflow/{WorkflowTab,StepCard,ApproverPicker,PersonAutosuggest,OutcomesSection,NotificationsPanel,SettingsTab,WorkflowFlowPreview}.tsx`. Files modified: `packages/forms-schema/src/validators/field-config.ts` (additive Zod), `apps/forms-worker/src/{index,submit/index,admin/submissions}.ts`, `apps/web/app/admin/forms/[id]/{page,_builder/{BuilderClient,Inspector,reducer}}.tsx`. Files deleted: `apps/web/app/admin/forms/[id]/_builder/{WorkflowEditor,WorkflowMermaidPreview}.tsx` (replaced by the new `_workflow/` tree). Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 34.5 kB / 142 kB First Load JS — adds ~4 kB for the new tabs + step cards + autosuggest, under the 200 kB DoD threshold; Mermaid still code-splits to a separate chunk and stays out of First Load); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1103.92 KiB raw / 211.76 KiB gzipped (≈ +10 KiB raw / +2 KiB gzip vs Brief 123 baseline, all webhook + autosuggest + helper module). No Supabase / R2 / wrangler.toml / secret changes — the new `FORMS_OUTCOME_NOTIFICATION_WEBHOOK_URL` is operator-bound post-deploy. New PA flow build doc at `PA_FLOWS_BRIEF_125.md` (mirrors Brief 121). Form-meta save persistence stays client-only (Brief 95 limitation) — Settings tab renders the editors but the existing PATCH /draft endpoint accepts schema only; a follow-up brief widens the endpoint. PDF-of-completed-form attach flagged as v2 placeholder in the Notifications panel; conditional transitions + outcome drag-reorder also v2. | Completed (2026-05-14) | none — strictly UX overhaul + additive webhook on top of Brief 120/121/123 | [brief-125-forms-workflow-builder-ux-redesign.md](brief-125-forms-workflow-builder-ux-redesign.md) |

| 108 | Fleet update webhook — coerce top-level `id` to integer (use `arr[0].id`). Single-line fix in `apps/fleet-inquiry-worker/src/admin.js` `handleUpdateSubmission`: webhook payload's top-level `id` switched from the URL-path string to the PostgREST response row's integer `arr[0].id`. After operator migrated `fleet_submissions.id` from UUID to bigint identity, PA's HTTP-trigger schema (correctly typed as `integer`) started 400-ing on every dashboard PATCH webhook fire because the worker was still shipping `"21"` (string). `row.id` inside the payload was already an integer (PostgREST returns the integer column as a JSON number) — top-level `id` and `row.id` now agree. PA flow itself needs no edit (operator already updated its HTTP-trigger schema + Filter Query post-column-switch). No D1 / R2 / Supabase / wrangler.toml / secret changes. CLAUDE.md "Fleet inquiries admin" glossary entry extended with a one-liner noting the update-webhook `id` field is sourced from the PostgREST response row (`arr[0].id`), not the URL param, to guarantee wire type matches the underlying column type. Latent reminder: when a Supabase column changes type, audit any worker code that passes URL-path params as that column's value in downstream payloads. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/fleet-inquiry-worker exec wrangler deploy --dry-run` 786.26 KiB raw / 149.74 KiB gzipped (≈ unchanged vs Brief 105 baseline). | Completed (2026-05-11) | none — bug fix in a fail-soft optional webhook | [brief-108-fleet-update-webhook-coerce-id-to-integer.md](brief-108-fleet-update-webhook-coerce-id-to-integer.md) |

| 107 | JotForm submissions — new worker, Supabase storage, backfill, webhook receiver, admin read API + CSV. Ninth worker on the monorepo (`splash-jotform`, `apps/jotform-worker/`, JS not TS — mirrors fleet-inquiry-worker shape per Brief 81). Ingests JotForm Enterprise submissions for four onboarded forms (rewash `250165655616055`, salt-log `243523811897060`, retention `250855287972067`, time-card-edit `250193775451056`) into two new Supabase tables `jotform_forms` (registry, slug + display_name + enabled) and `jotform_submissions` (one row per submission; common filterable fields `site_number` / `site` / `site_email` / `jotform_created_at` promoted to columns, rest of the payload in JSONB `answers`). ~50K rows lifetime, dominated by ~30K rewash. Sync mechanism: webhook + one-time backfill. `POST /jotform/webhook/{token}/{form_id}` validates a URL-path token (`JOTFORM_WEBHOOK_TOKEN`, constant-time compare; JotForm Enterprise webhook UI does NOT expose a signing secret per operator screenshot — URL secrecy is the entire auth posture), re-fetches the rich submission payload via JotForm API (`fetchSubmissionById`), normalizes via `normalizeSubmission` (strips `control_head`/`control_pagebreak`/`control_button`/`control_text` noise — retention's ~20KB raw payload becomes ~3-5KB stripped — and extracts the three common fields via JotForm-`name`-keyed walk: `typeA` → site_number, `site` → site, `siteEmail`/`siteEmail56` → site_email with email-typed preferred when both present), and upserts via PostgREST `Prefer: resolution=merge-duplicates`. 2xx for accepted / 5xx for retry so JotForm retries network failures; unknown form_id → 200 to halt retries. `POST /admin/jotform/api/{form_id}/backfill?after_id=` is super_admin-only, paginates one 1000-row JotForm API page per call (operator drives the loop with the returned `last_id`); `id`-ascending order so the cursor advances monotonically. Admin read API at `/admin/jotform/api/*`: `GET /forms` (admin-tier — super_admin/admin/dcRole-admin/dcRole-super_admin — returns enabled forms + COUNT-via-Content-Range submission counts), `GET /{form_id}/submissions` + `GET /{form_id}/submissions/{id}` + `GET /{form_id}/submissions.csv` (any authenticated session passes; permission scope via `accessibleSiteNumbersForSession` — super_admin/admin sees all, RM/RD/GM scopes by email-on-locations match against `locations.am_email`/`rm_email`/`site_email` via `getLocationsByContactEmail`, converting each integer site to BOTH zero-padded 3-digit AND unpadded string forms because the JotForm `typeA` widget returns site numbers as strings sometimes padded ("090" Milford) and sometimes not ("127" Elmira Heights)). Detail-endpoint anti-leak: out-of-scope row returns 404, not 403 (don't expose existence). CSV uses the Brief 96 schema-union pattern — header is the union of every `answers` key across the date range as `answers__{key}__answer` columns (filled with `prettyFormat` value, falling back to `answer`); 10000-row safety cap, 416 on overflow. New `apps/web/wrangler.toml` `[[services]] binding = "JOTFORM_WORKER" service = "splash-jotform"` + `apps/web/cloudflare-env.d.ts` `JOTFORM_WORKER: Fetcher` declaration. New deploy doc `PRE_DEPLOY_JOTFORM.md` (Phase 1 SQL, secret bind commands, per-form backfill loop sample in PowerShell, webhook URL pattern, 7-step first-run smoke). No CLAUDE.md "Working with workers" table change at this brief; glossary gains a `jotform-worker` entry + `JotForm submissions` paragraph. Workers.dev only at v1 per the operator-driven-cutover posture (CLAUDE.md constraint #9 / Brief 81 mirror); production routes operator-driven. New worker module shape: `src/{index.js, jotform.js, normalize.js, auth-gate.js, db.js, handlers/{webhook.js, admin.js}}`. Validation: root `pnpm typecheck` 18/18 green (jotform-worker added to the pipeline); `pnpm --filter @splash/jotform-worker exec wrangler deploy --dry-run` 741.06 KiB raw / 140.34 KiB gzipped. | Completed (2026-05-11) | Brief 108 (apps/web JotForm viewer pages) | [brief-107-jotform-worker-storage-backfill-webhook.md](brief-107-jotform-worker-storage-backfill-webhook.md) |

| 123 | Custom forms — workflow builder UX overhaul (focus loss, cascade rename, terminal stages, Mermaid preview, publish-time validators). Fast-follow polish on Brief 120's `WorkflowEditor`. Five concrete defects fixed: (1) Stage ID / Display Label / Transition Label inputs lost focus on every keystroke — root cause was the `key={stage.id}` prop on `<StageEditor>` unmounting the row on every dispatch as `stage.id` is the controlled value being edited. Stage rows now keyed by a stable `_uiKey` (nanoid generated at `workflow_add_stage` / `workflow_enable` / `initialState` time; stripped from the schema before save via new `stripBuilderArtifacts` helper). (2) Stage ID renames silently broke the workflow because `default_stage` and every `transition.to` referenced the old id. New `workflow_rename_stage` reducer action does the three-update cascade atomically; the renamer input sanitizes snake_case per Brief 95's `KeyEditor` pattern and refuses to dispatch when the new id collides with another existing stage. (3) Transition default label flipped from `"Advance"` to `"Move to {dest.label}"` — computed in `workflow_update_transition` when the destination changes and the prior label is empty / matches `/^Move to /` / equals the legacy `"Advance"` literal; operator-customized labels are left alone. New transition button placeholder reads `"e.g. Approve, Decline, Send back"`. (4) `workflow_enable` now seeds three stages — `approval` (default; `site_role: rm_email` approver; `Approve → approved` + `Decline → denied` transitions) + `approved` (terminal) + `denied` (terminal) — instead of a single confused self-loop stub. (5) Terminal stages (no `approver_source` AND no transitions) render with a slate `TERMINAL` pill, muted `bg-gray-50/60` row background, hidden ApproverSource picker (replaced with a "Make approval step" CTA), and a disabled "+ Add transition" button with a hover hint explaining the path back. Transition destination dropdowns disable the current stage as an option ("(current — cannot self-transition)" suffix) and surface a red border + inline hint when an existing transition still points self-ward. New `WorkflowMermaidPreview` client island (lazy-loaded via `next/dynamic({ ssr: false })`) renders a live `flowchart LR` graph of the workflow below the stages list (~300ms debounce; default-stage gets a `START · ` prefix; terminal nodes get a muted slate fill via `classDef`). Publish-time strict validator extended with three new Zod issues: orphaned approval stages (approver set, no transitions out), unreachable terminal stages (BFS from `default_stage` finds no zero-out stage), and self-transitions (`transition.to === stage.id`). The original Brief 120 strict checks (duplicate ids / default_stage reference / transition.to reference / payload_field field_key) stay. `WorkflowStage.approver_source` becomes optional (`?:`) at both the TypeScript and Zod-schema layers so terminal stages can omit it cleanly; `apps/forms-worker/src/{submit/index.ts, admin/submissions.ts}` guard the two call sites — submit-time default-stage seed skips `resolveApproverEmails` when the default stage is terminal (rare edge case); admin transition gate returns 403 `not_approver` for non-admin callers on a terminal current-stage, and post-transition `current_approver_emails` stamps `[]` for a terminal destination. New `mermaid@^11.4` workspace dep on `@splash/web` (lazy-loaded ⇒ bundle only ships to operators opening `/admin/forms/[id]`). Files modified: `packages/forms-schema/src/{types,validators/field-config}.ts`, `apps/forms-worker/src/{submit/index,admin/submissions}.ts`, `apps/web/app/admin/forms/[id]/_builder/{WorkflowEditor,reducer,Inspector,BuilderClient}.tsx`, `apps/web/package.json`. Files created: `apps/web/app/admin/forms/[id]/_builder/WorkflowMermaidPreview.tsx`. CLAUDE.md `forms-worker` glossary Brief 120 entry extended with a Brief 123 follow-up sentence. Validation: root `pnpm typecheck` 18/18 green (forms-schema + forms-worker + web ran fresh; others cached); `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 30.7 kB / 138 kB First Load JS — adds ~5 kB for the rename cascade + terminal styling + Mermaid dynamic-import shim; Mermaid itself code-splits to a separate chunk and stays out of First Load); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1094.24 KiB raw / 209.86 KiB gzipped (≈ +3 KiB raw / unchanged gzip vs Brief 121 baseline, all worker-side `approver_source` optional-guard adds). No Supabase / R2 / wrangler.toml / secret changes. Visual graph EDITING (drag stages, click-draw edges), conditional transitions, drag-to-reorder, and multi-user editing conflict resolution all deferred to v2. | Completed (2026-05-13) | none — strictly UX polish on top of Brief 120 | [brief-123-forms-workflow-builder-ux-fixes.md](brief-123-forms-workflow-builder-ux-fixes.md) |

| 122 | Custom forms — localStorage autosave + resume banner on the public form-render path. Long-form persistence so a GM interrupted mid-fill on a retention or workflow form survives a refresh / tab close / multi-day gap. Single-file change to `apps/forms-worker/static/forms-public.js` (Brief 92's client bundle): autosave debounced 500 ms on every `input`/`change` event bubbled to the form root; values serialized into a flat `name → value` map (radio = checked value, checkbox or multi-checkbox-group = `string[]` of checked values, select multiple = `string[]`, file inputs skipped — their OOB-upload `_r2` hidden companion captures the r2_key); persisted to `localStorage["forms.draft.{slug}"]` with `{values, pendingSubmissionId, savedAt}`. On page load, if a <30-day draft exists, an amber banner renders above the first field with "📋 You have a saved draft from N hr/day(s) ago. [Resume draft] [Discard and start fresh]" buttons. Resume restores values + the saved `pending_submission_id` (so existing R2-uploaded files/signatures stay linked — their keys embed the original pending id) and dispatches synthetic `input` + `change` events so the Brief 93 lookup-resolver re-fires on touched key fields. Stale drafts (>30 days) get cleared silently. Discard removes the key. Clear-on-submit via a `submit` event listener (option B from the brief — optimistic clear; trade-off documented: a rare 422 validation_failed loses the draft but the user can hit Back to recover DOM state from bfcache and the v1 4xx-JSON-page UX is a known limitation tracked separately). Refactor of the Brief 92 signature/file upload handlers to read `pending_submission_id` lazily inside the upload closure (via new `currentPendingId(formEl)` helper) rather than capturing it at wire time, so a resume that rewrites the hidden input flows through to new uploads transparently. Banner styling inline via `style.*` properties (no edits to `shell.ts` required). Quota / private-browsing failures swallowed with a `console.warn`. No render-time / submit-handler / worker-endpoint / wrangler.toml / Supabase / R2 / secret changes. CLAUDE.md `forms-worker` glossary entry gains a Brief 122 one-liner noting the autosave contract. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, forms-worker ran fresh after the static-asset cache key invalidated); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1066.77 KiB raw / 204.58 KiB gzipped (≈ +12 KiB raw / +3 KiB gzip vs Brief 119 baseline, all autosave). | Completed (2026-05-13) | none — strictly additive UX polish on top of Briefs 90 + 92 | [brief-122-forms-localstorage-autosave-and-resume.md](brief-122-forms-localstorage-autosave-and-resume.md) |

| 121 | Custom forms — Pending Approvals dashboard surface + daily digest cron + single PA flow. Builds on Brief 120's `current_approver_emails text[]` denormalization to surface "items waiting on me" across every form in one query (GIN-indexed). New worker endpoint `GET /forms/admin/api/pending-approvals` (any-session auth; `?all=1` admin-tier widens to every pending approval in the org) — joins `form_submissions` to `forms.title` + `form_versions.schema` for server-side stage-label resolution; 500-row safety cap; returns `{items, total, scope, caller_email, limit_hit}` shape with each item carrying `submission_id`/`form_id`/`form_title`/`stage_label`/`submitter_email`/`location_code`/`review_path`. New apps/web page at `/admin/approvals` server-renders pending items grouped by form (forms with more items float, alphabetical tie-break) with per-row Submitted-by / Stage pill / submitted-at relative-time / Review-→ button linking to Brief 96's per-submission detail page (where Brief 120's transition modal lives); admin-tier "Mine / All Approvals" toggle for ops oversight; empty state copy varies by scope. New `apps/forms-worker/src/cron/approval-digest.ts` runs the daily digest (groups every non-empty `current_approver_emails` row by email + form, builds per-recipient `{recipient_email, total_pending, by_form:[{form_id, form_title, count, oldest_submitted_at}], dashboard_url}` payload, POSTs each to `FORMS_APPROVAL_DIGEST_WEBHOOK_URL`); fail-soft per recipient (Brief 65 / 101 posture — 15s timeout, swallow non-2xx, `[forms.approval-digest]` log line); when secret unbound the cron logs would-fire counts and skips the POST. Worker's `scheduled` handler dispatches on `event.cron` literal — `"0 12 * * *"` runs the digest, `"0 11 * * *"` (and unrecognized crons) fall through to the Brief 97 cleanup pass. `wrangler.toml` crons array becomes `["0 11 * * *", "0 12 * * *"]` (12:00 UTC = 7 AM EDT — fires BEFORE damage 13:00 UTC summary AFTER 11:00 UTC cleanup + 11:30 UTC MaintainX sync slot). New dashboard tile "Pending Approvals" in the Operations group (any-session — most users will have items at some point). New `_lib/worker-fetch.ts` helper `listPendingApprovalsAdmin({all?})` parallels the existing pattern. New optional secret `FORMS_APPROVAL_DIGEST_WEBHOOK_URL` (single PA flow for the entire forms feature — adding new forms with workflows automatically participates; zero PA work per new form). New doc `PA_FLOWS_BRIEF_121.md` mirroring the Brief 101/102/105 PA-flow construction guide. CLAUDE.md `forms-worker` glossary gains a Brief 121 paragraph documenting the endpoint, dashboard tile, daily cron + secret. Validation: root `pnpm typecheck` 18/18 green (forms-worker + web ran fresh; others cached); `pnpm --filter @splash/web build` succeeds (`/admin/approvals` 179 B / 105 kB First Load JS); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1091.41 KiB raw / 209.31 KiB gzipped (≈ +10 KiB raw / +2 KiB gzip vs Brief 120 baseline, all digest + pending-approvals). No Supabase / R2 schema changes (Brief 120 owns the column adds). | Completed (2026-05-13) | none — Brief 120 was the data-model blocker | [brief-121-forms-pending-approvals-dashboard-and-digest.md](brief-121-forms-pending-approvals-dashboard-and-digest.md) |

| 120 | Custom forms — workflow schema + transitions + per-stage approver resolution. Operator review on 2026-05-13 asked for approval flows. Generalizes the damage workflow pattern (Briefs 19/59/66/101) into form-builder-configurable stages. `packages/forms-schema` gains `ApproverSource` (`site_role` / `static_emails` / `payload_field`), `WorkflowTransitionRequirements` (`signature` / `typed_name` / `note`), `WorkflowTransition`, `WorkflowStage`, `FormWorkflow`, and `WorkflowHistoryEntry` types + Zod validators (strict + lenient-draft variants; strict variant `.superRefine()` enforces no-duplicate-stage-ids, default_stage references a real stage, every transition.to references a real stage, payload_field approver_source.field_key references a real field). `FormSchema` now carries an optional `workflow`. Three new columns on `form_submissions` (SQL run by operator per Phase 2): `workflow_stage text`, `workflow_history jsonb default '[]'`, `current_approver_emails text[] default '{}'` + two indexes (`workflow_stage` partial; `current_approver_emails` GIN). New worker endpoint `POST /forms/admin/api/forms/{id}/submissions/{subId}/transition` (auth = any session; per-stage authority via approver-email membership against `session.email`; super_admin/admin tier escape hatch). New `apps/forms-worker/src/workflow-resolution.ts` exports `resolveApproverEmails(env, source, {schema,payload})` resolving each source type to the deduped email list — `site_role` reuses Brief 101's `getLocationContactInfo` (key field = the form's `location` field or any lookup keyed on `pricing_simple.location_code`); `static_emails` filters empties; `payload_field` reads from the payload. Submit handler (Brief 91) seeds `workflow_stage = default_stage` + pre-resolves `current_approver_emails` for the default stage on insert when the version has a workflow. Builder UI: `Inspector` (when no field is selected) now renders a `WorkflowEditor` below the form-meta panel — Enable/Disable toggle, default-stage dropdown, per-stage card with id/label inputs, ApproverSourceEditor (radio + type-specific sub-form), per-transition row (to-dropdown + label + requires checkboxes), move-up/down + remove. Save Draft persists the workflow as part of `schema.workflow` via the existing PATCH /draft endpoint (Zod-validated against the lenient draft variant; publish re-validates strictly). Submission detail page: when the version has a workflow, renders a new `WorkflowSection` above the existing payload renderer — current stage label, per-transition buttons (only allowed transitions enabled; disabled when caller is neither admin-tier nor on `current_approver_emails`), inline modal with the transition's `requires` block, vertical history timeline. Server action `transitionAction` uses the Brief 19 `useActionState` pattern. Files created: `packages/forms-schema/src/types.ts` (workflow types appended), `packages/forms-schema/src/validators/field-config.ts` (workflow Zod), `apps/forms-worker/src/workflow-resolution.ts`, `apps/web/app/admin/forms/[id]/_builder/WorkflowEditor.tsx`, `apps/web/app/admin/forms/[id]/submissions/[subId]/_components/WorkflowSection.tsx`. Files modified: `apps/forms-worker/src/index.ts` (route mount), `apps/forms-worker/src/admin/submissions.ts` (`handleTransition`), `apps/forms-worker/src/db/admin-submissions.ts` (`transitionSubmission` helper + extended SubmissionDetail), `apps/forms-worker/src/db/forms.ts` (`insertSubmissionIdempotent` workflow-seed args), `apps/forms-worker/src/submit/index.ts` (default-stage seed), `apps/web/app/admin/forms/_lib/worker-fetch.ts` (`transitionSubmissionAdmin`), `apps/web/app/admin/forms/[id]/actions.ts` (`saveDraftAction` accepts workflow), `apps/web/app/admin/forms/[id]/_builder/{BuilderClient,Inspector,reducer}.tsx`, `apps/web/app/admin/forms/[id]/submissions/[subId]/{page,actions}.ts`. Operator runs the Phase 2 SQL in Supabase before the worker deploys; backward-compatible because every new column is nullable / has a sensible default and the worker reads with `??` fallbacks. Validation: root `pnpm typecheck` 18/18 green (forms-schema + forms-worker + web ran fresh; others cached); `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 28.4 kB / 133 kB First Load JS — adds ~2.6 kB for the WorkflowEditor; `/admin/forms/[id]/submissions/[subId]` 2.63 kB / 108 kB — adds ~1 kB for the WorkflowSection); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1081.89 KiB raw / 207.37 KiB gzipped (≈ +15 KiB raw / +3 KiB gzip vs Brief 122 baseline, all workflow). Notification webhook fire + Pending Approvals dashboard + daily digest deferred to Brief 121. Department-approver source + conditional/branching transitions + workflow analytics deferred to v2. | Completed (2026-05-13) | Brief 121 (Pending Approvals dashboard) | [brief-120-forms-workflow-schema-and-transitions.md](brief-120-forms-workflow-schema-and-transitions.md) |

| 134 | Forms — workflow email step HTML rendering with Splash-branded shell. Operator review of Brief 133's smoke test surfaced "the email is gross" — Brief 127's plain-text body collapsed into one paragraph in Outlook's reading view, the full submission URL was unreadable inline, and there was no logo. Fix is server-side HTML auto-rendering on top of the operator's plain-text `body_template`. New `renderTemplateHtml(template, schema, payload, runtime): string` export in `apps/forms-worker/src/workflow-email-step.ts` mirrors the existing `renderTemplate` token surface and produces an HTML fragment ready for embedding. New file `apps/forms-worker/src/workflow-email-shell.ts` exports `wrapInEmailShell(bodyHtml, opts: EmailShellOptions): string` — Outlook-safe table-based shell with DOCTYPE + minimal head + hidden preheader div + navy (`#0E2745`) header band with the public R2 white-script logo at `ASSETS.logoWhite` + 600px white content area + light-gray (`#F4F6F8`) footer band carrying the canonical `Splash Car Wash · splashcarwashes.info` brand line + optional "View All Open Approvals" / "View My Requests" secondary CTAs (`showApproverFooter` / `showSubmitterFooter` opts). Token-level HTML rendering: `{submission.url}` → primary blue (`#1FB6E0`) CTA "View Submission" button (table-wrapped for Outlook-safe spacing, `mso-padding-alt: 0` hint to defend against Word renderer collapse); `{approvals.url}` (new token) + `{my_requests.url}` (new token) → secondary outlined navy CTA buttons; `{payload.summary}` → `<table>` with one row per non-empty field (35%-width navy-bold key column, plain text value column, `#E5E7EB` hairline border); `{outcome.label}` → `<strong>` tinted by keyword heuristic (`/approv/` → success green `#047857`, `/(den|reject)/` → danger red `#B91C1C`, else navy); `{form.title}` + `{submitter.name}` escape via the 5-char map; `{submitter.email}` wraps in a monospace `<span>`; `{form.url}` → inline `<a>` styled link; `{field.<key>}` → escaped text with `<br>` for multi-line. Body text outside tokens normalizes via paragraph + linebreak rules (`\n\n` → `<p>` with `margin-bottom: 16px`, single `\n` → `<br>`); sentinel-pair (`\x01` / `\x02`) wraps trusted token HTML so the subsequent escape pass via `escapeOutsideSentinels()` doesn't double-encode. Plain-text `renderTemplate` also gained the two new tokens — `{approvals.url}` emits `Pending Approvals: https://splashcarwashes.info/admin/approvals`, `{my_requests.url}` emits `Your Submissions: https://splashcarwashes.info/admin/my-requests` for the plain-text fallback. Cascade wiring: `cascadeThroughEmailSteps` now calls both `renderTemplate` and `renderTemplateHtml` per recipient, wraps the HTML fragment via `wrapInEmailShell` with `title: subject`, `preheader: body.slice(0, 100)`, and footer flags derived from `localRuntime.outcome.outcomeLabel == null` (assignment pre-outcome → approver footer; outcome emails → submitter footer), and populates `body_html` on every `OutboundEmailPayload`. The shared `outbound_emails.body_html` column was already round-trip-ready from Brief 127; Brief 134 just stopped sending `null` there. Logo source: existing `ASSETS.logoWhite` constant from `@splash/storage-r2/src/assets.ts` (`https://pub-88f136a47a5846d5b7e47fbce605719b.r2.dev/SplashScriptWhite_RedCar.png`, operator-confirmed public per Brief 32's damage check-request PDF). Brand line lifted verbatim from `apps/forms-worker/src/pdf/layout-footer.ts:16`. PA flow update (operator-side, deferred): Send Email V2 "Body" expression should be `if(not(empty(items('Apply_to_each')?['body_html'])), items('Apply_to_each')?['body_html'], items('Apply_to_each')?['body_text'])` with "Is HTML" set to the same conditional; documented in the brief Outcome as a recipe. Files modified: `apps/forms-worker/src/workflow-email-step.ts` (renderTemplateHtml + new tokens in renderTemplate + cascade wiring populates body_html). Files created: `apps/forms-worker/src/workflow-email-shell.ts`. No worker / Supabase / R2 / wrangler.toml / secret changes. CLAUDE.md `forms-worker` glossary entry gains a Brief 134 paragraph documenting the HTML rendering posture + the two new template tokens + the future-executor extension recipe (renderTokenHtml dispatch + renderCtaButton / renderInlineLink helpers). Validation: `pnpm typecheck` 18/18 green (forms-worker ran fresh, 17 cache hits); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` succeeds at 2023.77 KiB raw / 445.27 KiB gzip. Operator post-deploy smoke (deferred): submit a fresh test workflow, click Approve, verify navy header band with white-script logo renders in Outlook, structured payload table renders with one row per field, blue "View Submission" CTA renders below body, footer carries either "View All Open Approvals" (assignment email) or "View My Requests" (outcome email), plain-text fallback reads cleanly via view-source. v2 candidates flagged in the brief Out-of-scope: per-form custom email branding, operator-authored HTML body templates, dark-mode CSS, VML rounded-corner buttons for older Outlook, embedded images other than the logo, email open/click-tracking pixels. | Completed (2026-05-14) | none — strictly polish on top of Brief 127's email queue + plain-text rendering | [brief-134-workflow-email-html-render.md](brief-134-workflow-email-html-render.md) |

| 133 | Forms — workflow transition three-bug pass: uuid type mismatch, dedup 409, PDF Unicode. Real-world testing of the full workflow flow (operator clicked Approve on a submission with signature attached) surfaced three distinct bugs in one transition attempt. **Bug A**: Brief 131 Phase 5's auto-status update wrote `status_updated_by: "system@workflow"` (a literal string) against the `form_submissions.status_updated_by` column, which is `uuid` (supabase/forms-tables.sql line 73), NOT `text`. Postgres 22P02 → PATCH 400 → transition handler 500 → operator sees `transition_failed` banner on every terminal-outcome transition. Option 1 chosen: dropped `status_updated_by` from the statusPatch entirely. The canonical actor-audit for system-initiated status flips lives in `workflow_history[-1].actor_email` (captures the operator who triggered the terminal-outcome transition), so a null column isn't a loss. **Bug B**: `enqueueOutboundEmail` in `packages/db-supabase/src/outbound-emails.ts` sent `Prefer: resolution=ignore-duplicates` without `?on_conflict=<column-list>`. PostgREST honors `ignore-duplicates` no-op semantics ONLY when the URL carries `on_conflict`; without it, the unique-index hit propagates as Postgres 23505 → PostgREST 409 Conflict. Every transition that re-entered an already-enqueued email step spammed `[forms.workflow.email-step] enqueue failed` ERROR into Workers Logs. Added `url.searchParams.set("on_conflict", "source_worker,source_kind,source_id,recipient")` matching the unique index on `outbound_emails`. The existing `was_duplicate` heuristic (compares `row.created_at` to `Date.now()` with 3s clock-skew tolerance) now triggers correctly. **Bug C**: PDF workflow-history page hardcoded `→` (U+2192 RIGHTWARDS ARROW) in the "From → To" header. pdf-lib's standard Helvetica uses WinAnsi encoding, which cannot represent U+2192 — `drawText` threw `WinAnsi cannot encode "→" (0x2192)`. Brief 129's outer try/catch caught it (so transitions proceeded) but `attach_pdf` email steps lost their PDF attachment + spammed `[forms.pdf] generation threw` per transition. Replaced the literal arrow with ASCII `->`. Added a `sanitizeForWinAnsi` helper to `apps/forms-worker/src/pdf/layout-utils.ts` mapping `→`/`←`/`•`/`—`/`–`/curly-quotes/`…` to ASCII equivalents; sanitized inside `wrapText` + `truncateToWidth` (which between them carry almost every user-text path through drawText) plus explicit `sanitizeForWinAnsi(...)` wraps at the direct drawText sites that interpolate user-generated content (header title, workflow-history from→to + actor lines, payload signature/file labels, drawLabelValue / drawSectionHeading / drawKeyValueGrid). Audited all `apps/forms-worker/src/pdf/layout-*.ts` files: replaced every Splash-owned literal `"—"` placeholder with `"-"`, the `truncateToWidth` ellipsis `"…"` with `"..."`, the layout-header subText `•` with `*`, the layout-footer brand-line `—` with `-`, and the layout-workflow-history actor-bullet `•` with `*`. Files modified: `apps/forms-worker/src/admin/submissions.ts` (Bug A — dropped status_updated_by from statusPatch type + write), `packages/db-supabase/src/outbound-emails.ts` (Bug B — on_conflict query param), `apps/forms-worker/src/pdf/layout-utils.ts` (Bug C — sanitizeForWinAnsi export + sanitize inside wrapText + truncateToWidth + drawLabelValue + drawSectionHeading + drawKeyValueGrid; ASCII source literals), `apps/forms-worker/src/pdf/layout-header.ts` (sanitize titleText + `•` → `*` in subText), `apps/forms-worker/src/pdf/layout-footer.ts` (BRAND_LINE em-dash → hyphen), `apps/forms-worker/src/pdf/layout-payload.ts` (sanitize signature/file labels + `"—"` → `"-"`), `apps/forms-worker/src/pdf/layout-workflow-history.ts` (Bug C call site — `→` → `->` + sanitize headerText/actorText/drawSubLabel + `•` → `*`), `apps/forms-worker/src/pdf/layout-metadata.ts` (`"—"` placeholders → `"-"`). No worker / Supabase / R2 / wrangler.toml / secret changes. Validation: root `pnpm typecheck` 18/18 green (forms-worker + web ran fresh; rest cached); `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 37.9 kB / 145 kB First-Load JS unchanged vs Brief 132); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 2013.01 KiB raw / 442.47 KiB gzipped (≈ +0.5 KiB raw / +0.15 KiB gzip vs Brief 131's 2012.50/442.32 — sanitizeForWinAnsi helper plus the source-literal swap). Out of scope: embedding a Unicode-capable TTF (v2 if operators want emoji-rendered output), changing `status_updated_by` column type from uuid to text (workflow_history is the better canonical audit anyway), rebuilding the PDF layout to dispatch on field type for Unicode, widening the sanitize helper to other workers' PDF surfaces (damage check-request PDF — fix when surfaced). | Completed (2026-05-14) | none — three small correctness fixes that together unblock the workflow approval flow | [brief-133-workflow-transition-three-bug-pass.md](brief-133-workflow-transition-three-bug-pass.md) |

| 132 | Forms — workflow `approver_source` seed fix on lookup-keyed forms. Brief 131 Phase 2 fixed the picker's forward path so `lookup_role` picks save as `payload_field`, but the initial seed in `makeWorkflowSeed` and the `email_rm_on_submission` Quick Pattern still wrote `{type: "site_role", role: "rm_email"}` unconditionally — and the resolver's `site_role` branch only walks `type: "location"` fields, so every workflow on a lookup-keyed form (no Location field) landed with `current_approver_emails = []`. **Phase 1 (Bug A — initial seed)**: `makeWorkflowSeed` now accepts `fields: Field[]` and delegates to a new `pickSeedApproverSource` helper mirroring the picker's `detectFromFields` priority order (1) first lookup `rm_email` → `payload_field`, (2) any lookup `*_email` → `payload_field`, (3) first email-type field → `payload_field`, (4) Location field present → `site_role rm_email`, (5) empty `static_emails` (operator picks before publishing). The `workflow_enable` reducer case threads `state.fields` through. **Phase 2 (Bug B — email_rm_on_submission Quick Pattern)**: the lookup branch of the ternary now emits `{type: "payload_field", field_key: rmLookup.key}` instead of the broken `site_role` shape; the Location-field branch correctly stays `site_role`. Audit of the four other quick patterns (`email_submitter_on_outcome`, `email_approver_when_assigned`, `email_specific_person_on_submission`, `email_submitter_on_approve_and_deny`) — none had the same bug class: `email_submitter_on_*` patterns target the synthetic `submitter.email` payload_field key; `email_approver_when_assigned` reuses the approval step's existing source (correct provided the approval step was seeded correctly, which Phase 1 now guarantees); `email_specific_person_on_submission` writes empty recipients. **Phase 3 (Bug C — picker auto-upgrade for legacy schemas)**: new `useEffect` in `ApproverPicker` detects saved `site_role` shapes whose role matches an available `lookup_role` option (form has a lookup field with that `sourceColumn`) and fires `onChange` with the upgraded `{type: "payload_field", field_key: matchingLookup.sourceFieldKey}` shape on first render. The next Save Draft persists the corrected schema; in-flight `current_approver_emails` denormalizations on existing rows stay untouched (column is stamped at insert time, resubmission against the fixed schema is the path forward). Picker docblock updated to document the auto-upgrade contract. `sourceToAutoKey`'s legacy `site_role → lookup_role` fallback removed (Brief 131's "round-trip as UI courtesy" workaround) — auto-upgrade migrates first, so the matching `lookup_role` branch becomes dead. Backfill SQL drafted in the brief Outcome for operator-driven cleanup of older published `form_versions` rows. CLAUDE.md `forms-worker` glossary gains a Brief 132 one-liner under the Brief 131 paragraph naming `pickSeedApproverSource` as the extension point for new lookup-shape columns added to `LOOKUP_SOURCES`. Files modified: `apps/web/app/admin/forms/[id]/_builder/reducer.ts` (Bug A + Bug B), `apps/web/app/admin/forms/[id]/_workflow/ApproverPicker.tsx` (Bug C). Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 37.9 kB / 145 kB First-Load JS — +0.2 kB vs Brief 131's 37.7 kB for the helper + useEffect). No worker / Supabase / R2 / wrangler.toml / secret changes. Out of scope: adding new lookup `sourceColumn` values to `LOOKUP_SOURCES`; surfacing the "no approver resolved" pill on additional pages; fixing the `LOOKUP_SOURCES` "Location name (eg oswego)" label that actually returns `site` (separate brief candidate, flagged in BUILD_STATE.md). | Completed (2026-05-14) | none — strictly additive correctness fix on top of Briefs 120/125/127/131 | [brief-132-workflow-approver-source-seed-fix.md](brief-132-workflow-approver-source-seed-fix.md) |

| 131 | Forms — workflow + approval flow correctness pass (picker mis-mapping, loading state, signature canvas, navigation, status auto-update, label polish, Quick Patterns). Real-world testing of Brief 125/127's workflow + email infrastructure surfaced a chain of correctness + UX gaps; this brief closes them as a single pass. **Phase 1 diagnostic confirmed**: payload keys are `field.key`, not `field.id` (`apps/forms-worker/src/submit/parse.ts:53` writes `payload[field.key]`; `workflow-resolution.ts:98` reads `payload[f.key]`; `pending-approvals.ts:203` reads `payload[f.key]` — three independent sites in agreement). **Phase 2**: `ApproverPicker.tsx` `pickOption` now saves `{type: "payload_field", field_key: <lookup field's key>}` when an operator picks an auto-detected lookup option whose `sourceColumn` is `am_email`/`rm_email`/`site_email` (the lookup has already resolved the email at submit time — its payload value IS the email). `location_role` picks keep `site_role` because the resolver finds `location_code` and goes through `getLocationContactInfo`. `sourceToAutoKey` widened to round-trip `payload_field` shapes against `lookup_role` options too. Local `mode` selector variable renamed to `selectMode` so the new contextual `mode` prop ("approver" | "recipient") can drive header copy. **Phase 3**: `resolveApproverEmails` `payload_field` branch now `console.warn`s `[forms.workflow.resolve] payload_field "{field_key}" resolved to empty / non-email value` so a future misconfiguration is grep-able in worker logs. **Phase 4**: transition handler in `apps/forms-worker/src/admin/submissions.ts` auto-flips `form_submissions.status = 'closed'` when the destination is a terminal outcome (`workflowStageIsOutcome(destStage)`) AND the current status is `new` or `in_progress`. Stamps `status_updated_at` + `status_updated_by = 'system@workflow'` in the same `transitionSubmission` PATCH (extended `TransitionPatch` to accept optional status columns). **Phase 5**: new `apps/web/app/admin/forms/[id]/submissions/[subId]/loading.tsx` renders an animated skeleton mirroring the real page's section layout; Next.js serves it immediately on click while SSR fetches resolve. WorkflowSection's transition modal + Status & Splash Notes Save button both switched to the shared `<SubmitButton>` (Brief 130) with contextual `pendingText` derived from the transition's label ("Approving…" / "Denying…" / "Sending back…" / etc.). **Phase 6**: new `_components/SignatureCanvas.tsx` client component replaces the literal-text "paste an r2_key" input — loads the vendored `signature_pad.min.js` (Brief 92) via `<Script src="/forms/api/static/signature-pad.min.js" strategy="afterInteractive">` (same-origin in production via path-carving), renders a 180px canvas + Clear + Confirm buttons. Confirm POSTs the PNG blob to the new `POST /forms/admin/api/transition-signatures/{submission_id}` endpoint (admin-tier gate, R2 at `transition-signatures/{submissionId}/{nanoid}.png`); returned `r2_key` populates the hidden `signature_r2_key` input via React state. **Phase 7**: `/admin/approvals` Review buttons now `?from=approvals`; detail page reads `searchParams.from === "approvals"` and prepends a `← Back to Pending Approvals` link to the breadcrumb row. **Phase 8**: `SubmissionListItem` worker shape gains `workflow_stage?: string | null` (both `include=payload` and metadata-only branches of `listSubmissions`). New `_components/WorkflowOutcomeCell.tsx` renders three states: no workflow → muted em-dash; terminal outcome → tinted pill (Brief 125 `stage.tint` + keyword-heuristic fallback); in-flight → info-tinted "stage label" pill. Wired into both wide and compact list tables on `/admin/forms/[id]/submissions`. **Phase 9**: pending-approvals worker `?all=1` drops the `current_approver_emails != {}` filter — admin-tier callers can now see in-flight rows whose approver resolution failed. Each row carries `approver_resolution_status: "resolved" | "empty"`; `/admin/approvals` renders an "⚠ No approver resolved" warning pill on empty rows. Skips rows whose current stage has no `approver_source` (terminal / email-step stages — not pending-approval surface). **Phase 10**: EmailStepCard "Use placeholders like {field.label} or {field.key}" → "Use dynamic fields"; the step-label input gained dashed amber border + "Click to name this step…" placeholder + hover-edit affordance. `ApproverPicker` accepts new `mode: "approver" | "recipient"` prop that retitles "Who approves this step?" → "Send email to", and the empty placeholder + missing-source warning copy. EmailStepCard passes `mode="recipient"` to its inner pickers. **Phase 11**: new `workflow_add_email_step_from_action` reducer action + matching dispatch `onCreateAndRouteToEmailStep` + `<option>` sentinel `+ Create new email step here` appended to each ActionSubCard's "Then go to" dropdown. Selecting it creates a fresh email step (label derived from the matching outcome — "Approved email" / "Denied email"), splices it immediately after the origin approval step, points the action's `to` at it, and routes the new step's auto-advance transition to the keyword-matched outcome (success-tinted for approve-ish action labels; danger-tinted for deny-ish; first outcome otherwise). **Phase 12**: new Quick Pattern `email_submitter_on_approve_and_deny` — for each approval step, finds the Approve and Deny actions (keyword-matched), creates two email steps (approval email with `attach_pdf: true` → Approved outcome; denial email with `attach_pdf: false` → Denied outcome), rewires both actions through them. Files created: `apps/forms-worker/src/admin/transition-signatures.ts` (~80 LOC), `apps/web/app/admin/forms/[id]/submissions/[subId]/loading.tsx`, `apps/web/app/admin/forms/[id]/submissions/[subId]/_components/SignatureCanvas.tsx` (~165 LOC), `apps/web/app/admin/forms/[id]/submissions/_components/WorkflowOutcomeCell.tsx`. Files modified: `apps/forms-worker/src/index.ts` (route mount), `apps/forms-worker/src/workflow-resolution.ts` (defensive log), `apps/forms-worker/src/admin/submissions.ts` (status auto-update on terminal), `apps/forms-worker/src/admin/pending-approvals.ts` (resolution status + widen `?all=1`), `apps/forms-worker/src/db/admin-submissions.ts` (`SubmissionListItem.workflow_stage`, `TransitionPatch` status columns), `apps/web/app/admin/forms/_lib/worker-fetch.ts` (`SubmissionListItem.workflow_stage`, `PendingApprovalItem.approver_resolution_status`), `apps/web/app/admin/forms/[id]/_workflow/{ApproverPicker,EmailStepCard,StepCard,WorkflowTab,QuickPatternsPopover}.tsx`, `apps/web/app/admin/forms/[id]/_builder/{BuilderClient,reducer}.tsx`, `apps/web/app/admin/forms/[id]/submissions/[subId]/{page.tsx,_components/WorkflowSection.tsx}`, `apps/web/app/admin/forms/[id]/submissions/page.tsx`, `apps/web/app/admin/approvals/page.tsx`. No Supabase / wrangler.toml / secret changes. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 37.7 kB / 145 kB First-Load JS — +0.9 kB vs Brief 129 for the email-step quick pattern + routed-from-action + inline picker; `/admin/forms/[id]/submissions/[subId]` 5.59 kB / 113 kB — +1 kB for SignatureCanvas client island; `/admin/approvals` 193 B / 107 kB unchanged); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 2012.50 KiB raw / 442.32 KiB gzipped (≈ +3 KiB raw / +1 KiB gzip vs Brief 129 baseline, all transition-signature handler + resolver log + status auto-update). Operator post-deploy smoke deferred per brief Phase 13.5. Latent issue surfaced (not blocking this brief): the per-submission detail page (`/admin/forms/[id]/submissions/[subId]/page.tsx`) is admin-tier-only gated on apps/web, but Brief 120's transition handler allows any approver — RM/GM approvers can hit the worker's transition endpoint but can't reach the apps/web page UI today. Out of scope per brief: refactor workflow schema, transition undo, inbox-style prev/next within queue, bulk approve/deny, public form signature, migrating other webhooks to outbound_emails. | Completed (2026-05-14) | none — additive correctness + UX on top of Brief 120/121/125/127/130 | [brief-131-forms-workflow-approval-flow-correctness-pass.md](brief-131-forms-workflow-approval-flow-correctness-pass.md) |

| 129 | Forms — completed-form PDF generator + per-email-step `attach_pdf` flag. v1 design: auto-generated PDF, no PDF builder UI, one per-field `exclude_from_pdf` flag in the Field Inspector's Advanced section, one per-email-step `attach_pdf` flag in the Workflow tab's email step card. New module under `apps/forms-worker/src/pdf/` (six files): `generate.ts` (public entry `generateCompletedFormPdf(bucket, ctx) → Uint8Array`), `layout-utils.ts` (shared helpers — fonts, cursor model, wrap/measure, key/value grid, R2 image fetch+embed, EST timestamp formatter, filename slugifier), `layout-header.ts` (navy band + white-script logo from `assets/splash-logo-white.png` R2 + form title + submission id), `layout-metadata.ts` (two-column grid: submission id / submitted at / submitter / kind / version / outcome), `layout-payload.ts` (per-field-type renderer — text-family / dropdown / multi / location / lookup / file w/ image thumbnails / signature inline image / heading as section break; honors `exclude_from_pdf`), `layout-workflow-history.ts` (from→to per entry with actor / timestamp / note / typed_name / signature image; system-actor entries render as one-line cascades), `layout-footer.ts` (every page: brand line + Page N of M). pdf-lib programmatic (Helvetica + Helvetica-Bold standard fonts, no custom font embedding). Schema additions are additive + optional: `FieldBase.exclude_from_pdf?: boolean` (every field type inherits via FieldBase), `WorkflowStage.attach_pdf?: boolean` (email-step-only flag). Zod validators (both strict + draft) widened to accept both. Worker hook: `cascadeThroughEmailSteps` gains optional `submissionMeta` + `priorWorkflowHistory` params; when set AND the email step has `attach_pdf: true`, calls `generateOrReuseCompletedPdf` (in new `pdf/cascade-attach.ts`) — reads R2 at `form-submission-pdfs/{form_id}/{submission_id}.pdf`, reuses when `Last-Modified` is AFTER the latest `workflow_history[*].at` timestamp (cumulative across cascade), regenerates + PUTs otherwise (15s `AbortController` timeout). Attachment shape `{filename, r2_key, mime: "application/pdf", size_bytes, bucket: "FORMS_FILES"}` pushed onto every recipient's `OutboundEmailPayload.attachments`. Brief 127's claim endpoint (`/forms/internal/api/email-queue/claim`) base64-inlines on PA fetch. Multi-email-step cascade reuses the just-written R2 object — no duplicate generation per submit/transition. Fail-soft: PDF generation failure / R2 write failure / timeout → email step still enqueues without attachment (log only). Cron extension: Brief 97's daily R2 cleanup at 11:00 UTC adds a third pass — `form-submission-pdfs/{form_id}/{submission_id}.pdf` objects with no matching `form_submissions.id` row → delete (1h grace, 20-page pagination cap = 20K orphan PDFs/run). PDFs for active submissions are NEVER deleted (no TTL — referenced indefinitely for re-send / re-attach). UI: shared `apps/web/app/admin/forms/[id]/_field-types/_shared/AdvancedSection.tsx` (collapsed `<details>`-wrapped `LabeledCheckbox` "Don't include in PDF exports"); rendered once at the bottom of the Inspector wrapper via `Inspector.tsx` rather than touching all 16 per-type Inspectors (cleaner DRY; same operator-facing behavior). EmailStepCard gains an "Attach PDF of completed form" checkbox between Body textarea and "Then go to" dropdown. Reducer's `workflow_update_email_step` action's Pick widened to include `attach_pdf`. Quick patterns templates: "Email submitter on outcome" now seeds `attach_pdf: true`; "Email approver when assigned" seeds `attach_pdf: false`. pdf-lib added as a direct forms-worker dep (`^1.17.1` — same version damage-worker uses for bundle dedupe). Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 36.8 kB / 144 kB First-Load JS, +0.3 kB vs Brief 127 baseline for AdvancedSection + email-step PDF checkbox); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 2008.90 KiB raw / 441.54 KiB gzipped (+870 KiB raw / +222 KiB gzip vs Brief 128, all pdf-lib; comfortably under the 3 MiB compressed free-tier ceiling). No Supabase / wrangler.toml / secret changes — R2 bucket `FORMS_FILES` reused with the new `form-submission-pdfs/` prefix. CLAUDE.md `forms-worker` glossary extended with a Brief 129 paragraph documenting the generator, R2 key convention, per-submission reuse posture, and cleanup-cron extension. Operator post-deploy smoke deferred per brief Phase 7.5. Out of scope per the brief: custom layout / field ordering (v2), section grouping beyond heading fields (v2), per-form branding override (v2), watermarks (Approved/Denied stamp overlay, v2), multi-language PDFs (English only at v1), custom fonts (Helvetica only), attaching the PDF to webhooks (damage-worker's claim summary PDF remains its own surface), "Download PDF" button on the per-submission detail page (fast-follow candidate), PDF preview in the form builder (operators submit a test entry). | Completed (2026-05-14) | none — strictly additive on top of Briefs 120 (workflow), 125 (workflow tab), 127 (queue + attachments) | [brief-129-forms-completed-form-pdf-generator.md](brief-129-forms-completed-form-pdf-generator.md) |

| 128 | Forms — admin viewer for the Brief 127 outbound email queue + Retry / Abandon actions. Four new endpoints on splash-forms — `GET /forms/admin/api/email-queue/list` (filter by `status=pending|claimed|sent|stuck|all` (default `all`) + `source_worker` + `source_kind` + `from`/`to` ISO 8601 against `created_at` (default last 7 days) + `limit` (default 100, max 500) + `offset`; returns `{items, total, limit_hit, from, to}`; per-item shape is metadata-only — body / attachments only on detail), `GET /forms/admin/api/email-queue/{id}` (returns full row incl. body_html / body_text + attachments metadata `[{filename, mime, size_bytes, has_r2_key, has_base64}]` — never inlines base64 on the admin viewer, only Brief 127's claim endpoint does that for PA), `POST /forms/admin/api/email-queue/{id}/retry` (resets `claimed_at`, `claim_id`, `send_attempts=0`, `last_error=NULL`; row becomes eligible for the next PA poll), `POST /forms/admin/api/email-queue/{id}/abandon` (stamps `send_attempts=99` + `last_error="Manually abandoned by {admin_email} at {now}"`; row stays for audit but never sends). Status taxonomy derived in code from row state (the table has no `status` column): pending = `sent_at IS NULL AND claimed_at IS NULL AND send_attempts < 5`; claimed = `sent_at IS NULL AND claimed_at IS NOT NULL AND send_attempts < 5`; sent = `sent_at IS NOT NULL`; stuck = `sent_at IS NULL AND send_attempts >= 5`. Admin-tier gate (super_admin OR dcRole admin/super_admin) + service-key-503 + `isOriginAllowed` CSRF on POSTs. apps/web `/admin/email-queue` list page: status / source_worker (datalist autosuggest from current page values) / source_kind (datalist) / date-range filters (hidden inputs preserve date range across status/source filter submits — outer GET form would otherwise drop them); table columns Created (relative + absolute on hover) / Source (worker / kind stacked) / Recipient (truncate 30) / Subject (truncate 60) / Status pill (sent=success, stuck=racecar-red, claimed=sudsy-blue, pending=gray) / Attempts / View →; offset pagination (page size 50). apps/web `/admin/email-queue/{id}` detail page: metadata grid (id / source / recipient / cc / reply-to / scheduled-for / claim_id / all timestamps / attempts), Subject + Body sections (body_html rendered as escaped source via React's auto-escape — NEVER `dangerouslySetInnerHTML`; defense against any future operator-authored HTML rendering as live markup), Last error block when present, Attachments metadata list, Retry now + Abandon action buttons (Brief 19 `<ActionForm>` pattern; Abandon disabled unless status=stuck with hover hint; client-island `<ConfirmSubmitButton>` runs `window.confirm()` before the submit dispatches). New "Email Queue" tile in the Admin group on `/admin/dashboard` (admin-tier `visibleTo`; new `mailIcon` SVG added to the tile registry). `"email-queue"` added to `ADMIN_KNOWN_SUBPATHS` in `apps/web/middleware.ts` per the CLAUDE.md mandatory rule. No Supabase / R2 / wrangler.toml / secret changes — Brief 127 landed the table + indexes + the partial `outbound_emails_source_idx` that backs the list query's `(source_worker, source_kind, created_at DESC)` filter. Files created: `apps/forms-worker/src/admin/email-queue.ts` (~360 LOC), `apps/web/app/admin/email-queue/page.tsx` (~370 LOC), `apps/web/app/admin/email-queue/[id]/page.tsx` (~280 LOC), `apps/web/app/admin/email-queue/actions.ts` (~60 LOC), `apps/web/app/admin/email-queue/_components/ConfirmSubmitButton.tsx` (~25 LOC). Files modified: `apps/forms-worker/src/index.ts` (route mounts + header comment), `apps/web/app/admin/forms/_lib/worker-fetch.ts` (Brief 128 helper block — types + 4 helpers), `apps/web/app/admin/dashboard/_lib/tiles.tsx` (Email Queue tile + mailIcon), `apps/web/middleware.ts` (ADMIN_KNOWN_SUBPATHS), `CLAUDE.md` (outbound_emails glossary entry append), `BRIEFS/INDEX.md` + `BUILD_STATE.md` + brief Outcome. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/email-queue` 939 B / 108 kB First-Load JS; `/admin/email-queue/[id]` 854 B / 108 kB First-Load JS); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1138.91 KiB raw / 218.64 KiB gzipped (≈ +11 KiB raw / +2 KiB gzip vs Brief 127 baseline, all admin-viewer handler). React auto-escape confirmed safe for body_html rendering — `<pre>{html}</pre>` renders raw HTML markup as visible text, no DOM injection. Out of scope per the brief: bulk retry / bulk abandon (v2), editing body before retry (delete row + re-submit instead), per-row admin-action audit log (add `claim_audit` JSONB or separate table when multi-admin contention shows up). | Completed (2026-05-14) | none — strictly additive on top of Brief 127's queue infrastructure | [brief-128-email-queue-admin-viewer.md](brief-128-email-queue-admin-viewer.md) |

| 127 | Forms — outbound email queue + workflow email-step type + Brief 125 outcome webhook migration. Three-part redesign: (1) New shared `outbound_emails` Supabase table holds fully-rendered emails; any monorepo worker holding `SUPABASE_SERVICE_KEY` can enqueue via the new `enqueueOutboundEmail` helper exported from `@splash/db-supabase`. Unique index on `(source_worker, source_kind, source_id, recipient)` makes re-fires idempotent. New SQL function `claim_outbound_emails(claim_id, limit)` encapsulates the `FOR UPDATE SKIP LOCKED` pattern PostgREST can't expose at the table layer. (2) Two new endpoints on `splash-forms` worker — `POST /forms/internal/api/email-queue/claim` (RPC-via-PostgREST + inlines R2-backed attachments as base64) and `POST /forms/internal/api/email-queue/confirm` (per-row sent / failed → stamp `sent_at` or release claim + increment `send_attempts` + record `last_error`). Auth: shared-secret `X-Email-Queue-Token` (new optional secret `FORMS_EMAIL_QUEUE_TOKEN`); when unbound, both endpoints return 503 — workers keep enqueueing safely, queue idles until PA flow + token are wired. Single Power Automate flow polls the queue every 5 minutes and drains every email for the entire forms feature (and any future worker that writes here). (3) Workflow schema gains `kind: "email"` stage variant with `recipients: ApproverSource[]` + `subject_template` + `body_template` + single auto-advance `transitions[0]`. Strict publish-time validator enforces email stages have exactly one outgoing edge + non-empty recipients + no approver_source. Predicate fallback in apps/web (`stageIsEmail` / `stageIsApproval` next to the existing `stageIsOutcome`) handles legacy + new stages cleanly; existing `kind: "step"` rows continue to validate. Worker auto-advances through email-step stages at both submit time (default-stage cascade) and transition time (dest-stage cascade); each step in the cascade renders templates + resolves recipients + enqueues + stamps a system-actor `workflow_history` entry + advances. Cascade depth cap 10 (defense in depth — strict validator should reject cycles at publish). Brief 125's `FORMS_OUTCOME_NOTIFICATION_WEBHOOK_URL` webhook fires removed at both call sites; `fireAssignmentNotification` + `fireOutcomeNotification` helpers become documented no-op log lines for one cycle so existing operator config doesn't error. The `notifications` block on the workflow schema stays for back-compat but is documented as deprecated. apps/web Workflow tab UX: "+ Add approval step" replaced with "+ Add step" choice popover (Approval / Email), new `EmailStepCard.tsx` (amber-toned header, To: picker reusing the Brief 125 ApproverPicker shape with multi-recipient add-another, Subject / Body textareas, placeholder reference panel, "Then go to" single-destination select), new `AddStepPopover.tsx`, new `QuickPatternsPopover.tsx` with four one-click templates ("Email submitter on outcome" / "Email approver when assigned" / "Email RM on submission" / "Email a specific person on submission") that produce normal email-step schema entries. Brief 125's `NotificationsPanel.tsx` removed (its use cases now expressed as explicit email steps); the `notify_*` booleans stay in the schema for back-compat but no longer drive any side effect. `WorkflowFlowPreview` (Mermaid) gains an `:::emailstep` node class. New `apps/forms-worker/src/workflow-email-step.ts` (~370 LOC) houses the cascade + template rendering + runtime context helpers. New PA flow build guide at `PA_FLOWS_BRIEF_127.md`. CLAUDE.md `forms-worker` glossary entry extended; new top-level `outbound_emails table` glossary entry documents the schema + claim function + polling contract. Files created: `supabase/forms-tables.sql` (additions), `packages/db-supabase/src/outbound-emails.ts`, `apps/forms-worker/src/email-queue/{claim,confirm,attachments}.ts`, `apps/forms-worker/src/workflow-email-step.ts`, `apps/web/app/admin/forms/[id]/_workflow/{AddStepPopover,EmailStepCard,QuickPatternsPopover}.tsx`, `PA_FLOWS_BRIEF_127.md`. Files modified: workflow types + Zod validators, `forms-worker/src/index.ts` (Env binding + route mount), `submit/index.ts` (cascade at submit), `admin/submissions.ts` (cascade at transition + removed Brief 125 fires + new `fetchFormMetaForCascade`), `notifications.ts` (no-op fires), `wrangler.toml` (binding doc), `apps/web/app/admin/forms/[id]/_builder/{reducer.ts,BuilderClient.tsx}`, `apps/web/app/admin/forms/[id]/_workflow/{WorkflowTab,WorkflowFlowPreview}.tsx`. Files deleted: `apps/web/app/admin/forms/[id]/_workflow/NotificationsPanel.tsx`. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]` 36.5 kB / 144 kB First-Load JS, +2 kB vs Brief 125); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1127.87 KiB raw / 216.90 KiB gzipped (≈ +24 KiB raw / +5 KiB gzip vs Brief 125 baseline of 1103.92 / 211.76, all email-queue + cascade + email-step UI). Operator post-deploy smoke deferred per brief Phase 9.6. | Completed (2026-05-14) | none — strictly additive on top of Briefs 120/121/125 + back-compat for existing workflows | [brief-127-outbound-email-queue-and-workflow-email-steps.md](brief-127-outbound-email-queue-and-workflow-email-steps.md) |

| 119 | Forms submissions viewer — wide-column table (every answer as a column, no click-through). Brief 96's narrow meta-only renderer was a per-row tap-through; for forms with 5–15 fields that's a click per row to see anything. Brief 119 makes the wide-column view the default and keeps the compact view as a `?view=compact` fallback (with localStorage persistence). Worker change: `GET /forms/admin/api/forms/{id}/submissions` accepts a new `?include=payload` query param; when set, each row carries `payload` JSONB + `form_version_id` + a `version: { id, version_number, schema }` embed (drawn from the existing `form_versions!inner` FK join — no extra round-trip). Default shape is back-compat for any non-Brief-119 caller. apps/web changes: new `_components/AnswerCell.tsx` lifts the Brief 96 detail-page `FieldValue` per-field-type renderer into a compact table-cell variant (truncates long strings to 80 chars with hover-tooltip, renders file/signature thumbnails inline via `/forms/admin/api/files/{r2_key}` from the payload's own `r2_key` references — no separate files-table fetch needed since the submit handler enriches the payload with `r2_key`/`mime`/`size_bytes`/`original_filename` per Brief 92); new `_lib/schema-union.ts` computes the column set as "most-recent-version's fields in schema order, then older-version-only fields appended alphabetically by key" — display-only `heading` + `image` skipped; new `_components/ViewToggle.tsx` client island writes `forms.submissions.view` to localStorage and `router.push(?view=wide|compact)` on click, with a soft `router.replace` to the saved choice on mount when no `?view=` param is present. Page rewrite: when wide view is active, pulls `?include=payload` and renders a sticky-header / sticky-first-column overflow-x-auto table; the meta columns (Submitted / Status / Submitter / Splash Notes / Version) stay on the left, schema-union answer columns follow, and a trailing `View →` link preserves the focused per-row detail surface. Cells outside a row's own version schema render a muted `—` with a hover-tooltip noting which version was used. Detail page (`[subId]/page.tsx`) untouched — `PayloadRenderer` keeps its full-fidelity rendering for focused review. Validation: root `pnpm typecheck` 18/18 green (forms-worker + web ran fresh); `pnpm --filter @splash/web build` succeeds (`/admin/forms/[id]/submissions` 1.64 kB / 107 kB First Load JS — adds the ViewToggle client island over Brief 96's 924 B); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1055.04 KiB raw / 201.25 KiB gzipped (≈ +1 KiB vs Brief 118 baseline). No schema / R2 / wrangler.toml / secret changes. CLAUDE.md `forms-worker` glossary entry gains a Brief 119 paragraph. Operator post-deploy smoke (deferred): default `/admin/forms/{id}/submissions` renders wide table with every schema-union field as a column; toggling to Compact falls back to Brief 96's narrow renderer; localStorage preserves the choice across reloads; submissions whose old version had different fields show `—` in columns that didn't exist on their version. | Completed (2026-05-13) | none — strictly UX refactor on top of Brief 96 | [brief-119-forms-submissions-wide-column-table.md](brief-119-forms-submissions-wide-column-table.md) |

| 118 | Forms — admin submissions index + post-submit navigation. Two UX gaps in the custom-forms feature surfaced 2026-05-13. (1) New apps/web page at `/admin/forms/submissions` lists every published + archived form as a card (mirrors the Brief 109 JotForm index card grid) with status pill, submission count, and "View submissions →" deep-link to Brief 96's per-form `/admin/forms/[id]/submissions` viewer. Sort: submission count desc, alphabetical tie-break. Drafts filtered out (no public URL, no submissions). Admin-tier gated (`session.role === "super_admin"` OR `session.dcRole === "admin"|"super_admin"`); non-admins get the existing `<NoAccessCard>` from Brief 95. Static segment `/admin/forms/submissions` coexists with `/admin/forms/[id]` exactly the same way `/admin/forms/new` does — Next.js App Router resolves static routes before dynamic ones. Page uses the existing `listFormsAdmin()` helper from `_lib/worker-fetch.ts` (no new worker endpoint; the existing list response already carries `submissionCount`). (2) `apps/web/app/admin/dashboard/_lib/tiles.tsx` Submissions-group "Forms" tile retargeted: `href` changed from `/forms` (Brief 99's fill-out index) to `/admin/forms/submissions`; `description` changed to "View submissions to admin-built custom forms."; `visibleTo` tightened from `anySession` to `isAdminTier` so RM/RD/GM operators don't see the viewer tile (they can still hit the fill-out flow via direct `/forms` bookmark or per-form `/forms/{slug}` link). (3) `apps/forms-worker/src/submit/success.ts` confirmation page rewired: removed the lone "Fill out another" CTA in favor of three buttons — primary "Back to Forms" (→ `/forms`, same-origin path-carve makes a plain anchor work), secondary "Dashboard" (→ `/admin/dashboard`, rendered ONLY for `audience: "internal"` forms; public + link-only forms suppress it because non-authed visitors would just bounce to `/login`), tertiary "Fill Out Another" (→ `/forms/{slug}`, unchanged behavior). New `.nav-actions` flex layout in the inline SUCCESS_CSS with `nav-btn-primary` / `nav-btn-secondary` / `nav-btn-tertiary` classes; no Header port to splash-navy bar (the bar is already there from the existing shell — only the action area changed). Audience field is already on the FormMeta type (Brief 89 schema), so the per-audience branch is one read at render time. No worker endpoint changes, no schema/R2/wrangler.toml/secret changes. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, web ran fresh — initial run flagged a `FormListItem.status` type narrowing in the StatusPill subcomponent, fixed by widening its prop type); `pnpm --filter @splash/web build` succeeds (`/admin/forms/submissions` 177 B / 105 kB First Load JS); `pnpm --filter @splash/forms-worker exec wrangler deploy --dry-run` 1053.80 KiB raw / 201.06 KiB gzipped (≈ +0.5 KiB vs Brief 100 baseline — the success page nav HTML/CSS additions). CLAUDE.md `forms-worker` glossary entry gains a Brief 118 paragraph. | Completed (2026-05-13) | none — strictly additive UX on top of Briefs 95/96/99/117 | [brief-118-forms-submissions-admin-index-and-post-submit-nav.md](brief-118-forms-submissions-admin-index-and-post-submit-nav.md) |

| 117 | Dashboard — two-level drill-down navigation (top page = three group tiles, group landing pages render sub-tiles). apps/web-only UX refactor that restructures the Brief 116 rendering surface without touching the tile registry or visibility predicates. `/admin/dashboard` rewritten to render exactly three group tiles (Submissions / Operations / Admin) — each linking to `/admin/dashboard/{groupId}`. Group tile visibility = at least one sub-tile in the group visible to the caller; empty-state copy ("No tools available — contact a super_admin") renders when zero groups have visible sub-tiles (theoretical, harmless safety net). New `apps/web/app/admin/dashboard/[group]/page.tsx` (single dynamic route covering all three groups) renders the resolved group's sub-tiles via the unchanged `<DashboardTile>` component from Brief 116. Unknown `params.group` values 404 via `notFound()`; group landing pages with zero accessible sub-tiles also 404 — matches the hidden-tile state at the top level so direct-URL probing doesn't leak existence. Per-group description copy lifted verbatim from the brief (Submissions / Operations / Admin). Group tile count badge ("N tools") rendered under each group's description so the operator sees at a glance how many sub-tools they have. Header (`apps/web/app/_components/Header.tsx`) "Dashboard" link already pointed at `/admin/dashboard` (no change needed; the prior unified entry-point was already top-level). Middleware (`apps/web/middleware.ts`) needs no change — `/admin/dashboard/{anything}` is multi-segment under `/admin/` so it bypasses the single-segment legacy `/admin/{slug} → /admin/pricing/{slug}` redirect rule; `dashboard` is already in `ADMIN_KNOWN_SUBPATHS`. Self-correction: Brief 116 implemented "three stacked sections on one page", operator wanted "drill-down with separate pages". Same data model, different page layer — Brief 117 keeps Brief 116's registry verbatim and only changes the rendering surface. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, web ran fresh); `pnpm --filter @splash/web build` succeeds (`/admin/dashboard` 164 B / 105 kB First Load JS; new `/admin/dashboard/[group]` 174 B / 105 kB First Load JS). No worker change, no Supabase / R2 / wrangler.toml / secret change. CLAUDE.md gains a brief "Admin dashboard" glossary entry noting the two-level model. | Completed (2026-05-13) | none — strictly UX refactor on top of Brief 116 | [brief-117-dashboard-drill-down-two-level-navigation.md](brief-117-dashboard-drill-down-two-level-navigation.md) |

| 116 | Dashboard tile reorganization — three groups + per-tile visibility. apps/web-only refactor of `/admin/dashboard` to replace the flat 8-tile grid with three labelled groups (Submissions / Operations / Admin) driven by a new registry at `apps/web/app/admin/dashboard/_lib/tiles.tsx`. Each tile carries its own `visibleTo(session)` predicate; group headers auto-suppress when no tile in the group is visible to the current session. MaxPass Admin tile splits along the Brief 56 `SignupAdminTabs` boundary — Signups Viewer lands in Submissions, Pricing lands in Admin; both URLs (`/admin/signups`, `/admin/pricing`) unchanged. "Forms" tile renamed to "Form Builder" to disambiguate from the new "Forms" submissions tile (links to the Brief 99 `/forms` credentialed index). Ten tiles total: Signups Viewer / Fleet Inquiries (admin-tier) / JotForm / Forms (Submissions group), Damage Claims / Work Orders / Performance Tracking (`pertrack` tool grant — confirmed `pertrack` not "performance"; super_admin role bypasses, admin-tier dcRole sees) (Operations group), Pricing / Form Builder (admin-tier) / Database Admin (super_admin) (Admin group). New `apps/web/app/admin/dashboard/_components/DashboardTile.tsx` is the per-card renderer — preserves the existing eyebrow / icon / title / description / "OPEN →" JSX shape from the Brief 4 / Brief 78 flat grid, so visual identity is unchanged. Deviated from the brief's `tiles.ts` filename to `tiles.tsx` because each tile carries an inline SVG `ReactNode` icon (the brief's `Tile` interface omitted `icon` but the brief's "no styling changes" requirement and the existing flat grid both ship icons; documented in the brief's Outcome). New tiles got fresh inline lucide-derived SVGs: UserPlus for Signups, Inbox for Forms (submissions). Form Builder kept the prior "Forms" tile's ClipboardList icon (visual continuity for users who already know it). Validation: root `pnpm typecheck` 18/18 green (17 cache hits, web ran fresh); `pnpm --filter @splash/web build` succeeds (`/admin/dashboard` 164 B / 105 kB First-Load JS — down slightly from prior baseline because icons + grid logic moved to the registry/component). No worker change, no Supabase / R2 / wrangler.toml / secret change. CLAUDE.md "Signup admin" glossary entry updated to note the MaxPass dashboard split. | Completed (2026-05-13) | none — strictly additive UX refactor on top of the dashboard | [brief-116-dashboard-grouped-tiles-and-per-tile-visibility.md](brief-116-dashboard-grouped-tiles-and-per-tile-visibility.md) |

| 115 | JotForm viewer — comprehensive UX restructure (default-today, role-aware gating, alphabetical full-scope grouping, signature proxy fix, location-pretty enforcement). Fifth and largest iteration on the JotForm viewer following Briefs 109–114. (1) Worker `parseDateRange` default flipped from "last 30 days UTC" to "today EST" (DST-aware via the Brief 114 `Intl.shortOffset` probe pattern); new `todayInEastern()` + `easternWallClockToUtcMs()` helpers in `apps/jotform-worker/src/handlers/admin.js`. (2) `handleListSubmissions` rewritten to dispatch on two new query flags: `?count_only=1` returns `{ total_rows, from, to, scope }` only (single COUNT via `Prefer: count=exact`, no rows transferred); `?group=location` returns `{ groups: [{site, site_number, rm_email, rm_name, count, rows}], total_rows, cap_reached, from, to, scope }` with rows alphabetically grouped by site (case-insensitive), 2000-row safety cap, oldest-dropped-first on overflow. RM info pre-resolved via a new `fetchRmRosterMap(env)` helper that joins `locations.site_number` → `regional_manager`/`rm_email`. Legacy paginated shape preserved for back-compat. `db.js`'s `listSubmissions` gained an `exactCount` flag to switch `Prefer: count=estimated` ↔ `count=exact`. (3) Asset proxy diagnostic + fix (Phase 1). Three defensive changes to `handleAssetProxy`: host allow-list expanded from "JOTFORM_BASE_URL host only" to "JOTFORM_BASE_URL host OR www.jotform.com" (JotForm Enterprise sometimes serves widget/signature URLs against the public jotform.com host even on Enterprise accounts); path-prefix allow-list expanded from `/uploads/` to `/uploads/` + `/widget-uploads/` + `/server.php` (signature widget URLs land under `/widget-uploads/` on Enterprise); auth posture upgraded to send `APIKEY` HTTP header in addition to the `?apikey=` query param (JotForm's `/uploads/` direct file server has been observed rejecting query-param-only auth — `APIKEY` header is the documented JotForm asset auth posture); pre-existing query params on the target are now augmented rather than dropped. New `fetchJotformAssetFollowingRedirects()` private helper manually follows up to 3 redirects, validating each hop stays on the allow-listed host set (Brief 113's `redirect: "manual"` + reject-3xx posture was dropping legitimate JotForm CDN bounces on the floor). (4) Roster handler (`apps/jotform-worker/src/handlers/roster.js`) location_pretty resolution rewritten — never falls back to `locations.location` (postal address). Resolution chain is now `pricing_simple.location_pretty` → `pricing_simple.location_code` → `Site {site_number}` placeholder. Closes the bug class Brief 111 patched with a client-side address-shape heuristic; the heuristic in `FilterBar.tsx` is removed (dead code post-Brief-115). apps/web side: `worker-fetch.ts` exports two new helpers (`listSubmissionsGrouped`, `listSubmissionsCount`) + shape types (`JotformSubmissionsGroupedResponse`, `JotformSubmissionsCountOnlyResponse`, `JotformSubmissionsGroup`). The per-form list page (`/admin/jotform/[form_id]`) was rewritten end-to-end: default URL date range is today; role-aware decision tree (`isAdminTier × isTodayOnly × hasFilter`) routes to either `CountOnlySummary` (large centered tile + filter prompt copy) or `GroupedSubmissions` (server-pre-grouped + sorted, one `<section>` per location with `Site Name (123) (N submissions) — Regional Manager: …` headers + per-form column registry tables). No row-level pagination — every row in scope renders in a single page load. `cap_reached` banner shown when the 2000-row safety cap was hit. Brief 110's per-page grouping + Brief 110's per-day date sub-headers are both gone (a "today only" default + accurate full-scope counts make sub-bucketing unnecessary; operators want "47 rewashes at Batavia II today" not 47 rows split across pages). FilterBar's Brief 111 client-side address-shape heuristic removed; consumes `loc.location_pretty` verbatim now. No schema / R2 / wrangler.toml / secret changes (asset proxy still uses Brief 107's `JOTFORM_API_KEY` + `JOTFORM_BASE_URL`). CLAUDE.md `JotForm submissions` + `jotform-worker` glossary entries get a Brief 115 paragraph; the Brief 110 + 111 sub-paragraphs about per-page grouping and the address heuristic are superseded. Self-correction log: Brief 110's per-page grouping was the wrong abstraction (counts were lies); Brief 111's client-side heuristic was patching a worker-side data bug; Brief 113's signature proxy needed one more iteration after deploy verification. Validation: root `pnpm typecheck` 18/18 green (16 cache hits, web + jotform-worker ran fresh); `pnpm --filter @splash/web build` succeeds (`/admin/jotform/[form_id]` 1.58 kB / 107 kB First-Load JS — ≈ unchanged vs Brief 113 1.61 kB despite a substantial rewrite, because the count-only and grouped branches share most code); `pnpm --filter @splash/jotform-worker exec wrangler deploy --dry-run` 763.42 KiB raw / 144.60 KiB gzipped (≈ +8 KiB vs Brief 114 baseline, attributable to the new helpers + grouping logic). Operator post-deploy smoke deferred — list page as super_admin should show count-only tile by default; signature `<img>` should load 200 with `Content-Type: image/png` on the detail page; Location dropdown should never show postal addresses. | Completed (2026-05-12) | none — strictly polish on Briefs 109–114 | [brief-115-jotform-viewer-comprehensive-ux-restructure.md](brief-115-jotform-viewer-comprehensive-ux-restructure.md) |

| 114 | jotform-worker — parse JotForm timestamps as America/New_York wall-clock, not UTC. One-file fix in `apps/jotform-worker/src/normalize.js` correcting Brief 107's `parseJotformDate`, which stamped EDT/EST wall-clocks as `Z` and produced a 4-hr (EDT) / 5-hr (EST) drift on the apps/web `formatEst()` display layer. JotForm Enterprise's API returns submission timestamps WITHOUT a tz offset in the account's local zone (`America/New_York` for Splash); the rewritten helper regex-extracts the six wall-clock components, probes `Intl.DateTimeFormat("en-US", { timeZone: "America/New_York", timeZoneName: "shortOffset" })` to read the DST-aware offset at that moment (`"GMT-4"` in EDT, `"GMT-5"` in EST), and converts to a true UTC ISO 8601 string. New private `easternOffsetMinutesForWallClock(y, mo, d, h, mi, s)` helper colocated. Brief 111's option (a) — display-only conversion — had been chosen under a misleading sample row whose `+00:00` suffix was actually Brief 107's PRIOR buggy output, not JotForm input; this brief corrects the storage path at ingest while leaving the display layer (`formatEst()`) unchanged. DST-ambiguous-hour edge case (2-3 AM on spring-forward/fall-back Sundays) inherits a 1-hour drift; accepted per the brief's Out-of-scope ("Few JotForm submissions land in that hour"). Already-ISO / malformed inputs pass through verbatim. Existing rows still carry the buggy timestamps until operator re-runs each form's backfill endpoint (`POST /admin/jotform/api/{form_id}/backfill?offset=...`) post-deploy — upsert is idempotent (`on_conflict=id` + `Prefer: resolution=merge-duplicates`), so existing rows update in-place. Phase 2 snippet documented in the brief's Outcome section. No schema / R2 / wrangler.toml / secret changes. CLAUDE.md `jotform-worker` glossary entry gains a Brief 114 paragraph. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, jotform-worker ran fresh); `pnpm --filter @splash/jotform-worker exec wrangler deploy --dry-run` 755.60 KiB raw / 143.11 KiB gzipped (≈ +1.2 KiB vs Brief 113 baseline); spot-check `parseJotformDate("2026-05-12 08:14:09") → "2026-05-12T12:14:09.000Z"` ✓ (operator sample), `parseJotformDate("2026-01-15 09:30:00") → "2026-01-15T14:30:00.000Z"` ✓ (winter EST case). | Completed (2026-05-12) | none — strictly a data-correctness fix on top of Brief 107 | [brief-114-jotform-parse-date-as-eastern-not-utc.md](brief-114-jotform-parse-date-as-eastern-not-utc.md) |

| 113 | JotForm viewer — absolute EST timestamps + signature/file asset proxy. Two visible bugs flagged in operator review 2026-05-12. (1) `submittedColumn()` in `apps/web/app/admin/jotform/[form_id]/_lib/form-columns.tsx` flipped to render `formatEst(iso).absolute` as the primary cell value; relative time moves to the `title` attr for hover. Brief 112's executor had left relative as the visible surface, which made the "Submitted (EST)" header label misleading. (2) Signature `<img>` + fileupload thumbnail grid in `apps/web/app/admin/jotform/[form_id]/[submission_id]/_lib/answer-renderer.tsx` retargeted from raw JotForm CDN URLs to a same-origin worker proxy. New worker endpoint `GET /admin/jotform/api/asset?url=<encoded>` (in `apps/jotform-worker/src/handlers/admin.js`) authenticates via the same `authenticateForAdminApi` gate as the per-form list/detail endpoints (any authenticated session), validates the target URL host against `JOTFORM_BASE_URL` and the path prefix against `/uploads/` (anti-SSRF), strips any pre-existing query params on the target, attaches `JOTFORM_API_KEY` as the `apikey` query param (mirrors `jotform.js`'s API read pattern), and streams the response back with `Cache-Control: private, max-age=300` + `X-Content-Type-Options: nosniff`. `redirect: "manual"` + 3xx-rejection guards against any future JotForm cross-host bounce. Returns 503 when `JOTFORM_API_KEY` / `JOTFORM_BASE_URL` is unbound; 400 on malformed url / wrong host / non-/uploads/ path; 502 on upstream error or thrown fetch; 404 when upstream returns 404; 10s `AbortSignal.timeout`. apps/web side: new `assetProxyUrl(jotformUrl)` helper in `apps/web/app/admin/jotform/_lib/worker-fetch.ts` builds `/admin/jotform/api/asset?url=<encoded>`. The signature renderer points its `<img src>` at the proxy; the fileupload renderer points both `<a href>` and `<img src>` at the proxy. No schema / R2 / wrangler.toml / secret changes (the worker already has `JOTFORM_API_KEY` + `JOTFORM_BASE_URL` bound from Brief 107). CLAUDE.md `JotForm submissions` + `jotform-worker` glossary entries gain a Brief 113 paragraph documenting the asset-proxy endpoint and SSRF guardrails. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/jotform/[form_id]` 1.61 kB / 107 kB First-Load JS — unchanged vs Brief 112; `/admin/jotform/[form_id]/[submission_id]` 172 B / 105 kB First-Load JS — unchanged, server-rendered); `pnpm --filter @splash/jotform-worker exec wrangler deploy --dry-run` 754.39 KiB raw / 142.70 KiB gzipped (≈ +2 KiB vs Brief 110 baseline). Operator post-deploy smoke (deferred): list "Submitted (EST)" cells render "May 12, 2026, 7:25 AM EDT" style strings, not "5 hr ago"; hover surfaces the relative; signature `<img>` loads inline on rewash + retention + time-card-edit detail pages; Network tab shows `/admin/jotform/api/asset?...` 200 not direct CDN; SSRF smoke `?url=https://evil.example.com/x.png` returns 400 `url host not allowed`. | Completed (2026-05-12) | none — strictly additive bug-fix polish on top of Briefs 111 + 112 | [brief-113-jotform-viewer-absolute-est-and-signature-proxy.md](brief-113-jotform-viewer-absolute-est-and-signature-proxy.md) |

| 112 | JotForm viewer — content polish (time-card-edit columns + detail page type-dispatched renderer). apps/web-only UX pass on top of Brief 111's per-form column registry + Brief 109's detail page. Phase 1: `apps/web/app/admin/jotform/[form_id]/_lib/form-columns.tsx` time-card-edit entry expanded from `[submittedColumn(), siteColumn()]` to include three operator-specified answer columns (key 4 "Employee Name" / key 5 "Reason For Edit" / key 28 "Manager Making Edits"). The `answerColumn` builder's `prettyFormat → answer → ""` accessor chain handles all three correctly — fullname keys render prettyFormat, radio renders the bare answer string. Phase 2: detail page (`apps/web/app/admin/jotform/[form_id]/[submission_id]/page.tsx`) renderer rewritten — replaced the Brief 109 alphabetical-key generic loop with a type-dispatched renderer that sorts by `answers[KEY].order` (JotForm builder display order, not alphabetical key) and filters out empty entries via `hasContent()`. New `apps/web/app/admin/jotform/[form_id]/[submission_id]/_lib/answer-renderer.tsx` exports `hasContent` / `orderKey` / `renderAnswerValue` + the `AnswerEntry` interface. `renderAnswerValue` dispatches on `entry.type`: `control_signature` → inline `<img>` (JotForm CDN URL); `control_fileupload` → wrapping `<a>` thumbnail grid (filtered to URL-shaped strings); `control_fullname` / `control_datetime` / `control_phone` / `control_checkbox` → prefer `prettyFormat` when present (falls through to generic answer rendering when missing); default branch renders the bare `answer` string for `control_textbox` / `control_textarea` / `control_radio` / `control_dropdown` / `control_email` / `control_number` / `control_widget`. Metadata block's "Submitted at" / "Updated at" rows + under-title timestamp swapped from browser-local `toLocaleString` to `formatEst()` matching the list page (Brief 111). Local `formatAbsolute` / `formatRelative` helpers in the page removed; `AnswerValue` component + `sortedAnswerKeys` list both replaced. Field count parenthetical dropped the ", alphabetical" suffix (renderer no longer sorts alphabetically). The `<details>` "Raw JSON (debug)" expandable stays as-is — useful debugging surface, intentionally collapsed by default. No worker change, no Supabase / R2 / wrangler.toml / secret change. CLAUDE.md "JotForm submissions" glossary entry gains a Brief 112 one-liner. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/jotform/[form_id]` 1.61 kB / 107 kB First-Load JS — unchanged vs Brief 111; `/admin/jotform/[form_id]/[submission_id]` 172 B / 105 kB First-Load JS — unchanged, server-rendered, no client island). | Completed (2026-05-12) | Brief 113 (per-form facet filters — worker-side filter param support deferred per Brief 112 out-of-scope) | [brief-112-jotform-viewer-content-polish.md](brief-112-jotform-viewer-content-polish.md) |

| 111 | JotForm viewer — per-form column packs + EST timestamps + location_pretty dropdown polish. apps/web-only refactor on top of Brief 109's flat list + Brief 110's filters/grouping. New `apps/web/app/admin/jotform/[form_id]/_lib/form-columns.tsx` registry maps each onboarded form_id to an ordered `FormColumn[]` (rewash → Submitted / Site / Rewash Reason; salt-log → Submitted / Site / three answer cols; retention → Submitted / Site / four answer cols; time-card-edit → generic Submitted / Site). `DEFAULT_COLUMNS` fallback means a future 5th / 6th form added via SQL INSERT + backfill gets `Submitted | Site` automatically — zero apps/web code needed unless non-generic columns are wanted. New `apps/web/app/admin/jotform/_lib/format-est.ts` formats `jotform_created_at` via `Intl.DateTimeFormat` + IANA zone `America/New_York` (auto-handles EDT/EST DST transitions); displays relative time inline with the absolute EST string in the `title` attr on hover. Column header for the timestamp is "Submitted (EST)" so operators don't have to mentally convert. `apps/web/app/admin/jotform/[form_id]/page.tsx` table rewritten: hard-coded "Submitted at / Site / Status / View →" replaced with `columnsFor(formId).map(...)` driving both `<thead>` and `<tbody>`; trailing `View →` link column appended unconditionally per form. Status column dropped — every onboarded form emits `"ACTIVE"` only (deleted submissions never ingest). Brief 110's location → date grouping retained; group headers gained the same `location_pretty` → `location_code` fallback the FilterBar uses (the roster worker's address-fallback for sites without `pricing_simple.location_pretty` no longer leaks into group headers either). `apps/web/app/admin/jotform/[form_id]/_components/FilterBar.tsx` Location dropdown gains a heuristic `locationDisplayLabel()` helper: if `location_pretty` contains a comma OR starts with a digit (postal-address shape — the roster worker's fallback when `pricing_simple.location_pretty` is null), use `location_code` instead. Defensive client-side override — Phase 5.3 explicitly forbade a worker change, so the roster's `row.location` fallback stays put. Per-form numeric-answer column LABELS are placeholder pending Supabase sample-data inspection (executor was headless; sample rows live only in operator's Supabase, not in git): rewash's "Rewash Reason" key is marked `REWASH_REASON_KEY = "TBD"`, salt-log keys 5/6/8 use "Answer (key 5)" etc., retention keys 28/29/30/31 same shape. Operator/next-executor grep `REWASH_REASON_KEY` + `"Answer (key "` and replaces with the JotForm builder's question text from `answers[KEY].text` of a representative sample row — no code-shape changes needed, only label-string edits. Accessor's `prettyFormat → answer → ""` fallback chain keeps the column rendering meaningful regardless. Timezone story: option (a) by design — display-only conversion. The worker's `parseJotformDate` (Brief 107) stamps UTC; this brief converts UTC → EST for display. Operator post-deploy verification step in brief (compare JotForm dashboard timestamp to apps/web EST display); if a 5-hour offset shows, follow-up brief to fix the ingest path. No worker change, no Supabase / R2 / wrangler.toml / secret change. CLAUDE.md "JotForm submissions" glossary entry gains a Brief 111 one-liner. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/jotform/[form_id]` 1.61 kB / 107 kB First-Load JS — up 60 B from Brief 110's 1.55 kB, well under budget). | Completed (2026-05-11) | none — strictly additive UX polish on top of Briefs 109 + 110 | [brief-111-jotform-viewer-per-form-columns-and-est-and-location-pretty.md](brief-111-jotform-viewer-per-form-columns-and-est-and-location-pretty.md) |

| 110 | JotForm viewer — RD / RM / Location filter dropdowns + grouped (location → date) rendering. Additive UX on top of Brief 109's flat-list viewer. Worker side (`apps/jotform-worker`): new `src/filters.js` (~140 LOC) is the single resolver for `am_email` / `rm_email` / `location_code` query params — each resolves to a `Set<string>` of `site_number` strings in BOTH zero-padded and unpadded forms (per the Brief 107 convention; JotForm's `typeA` widget returns "090" sometimes and "127" sometimes), and the result is the intersection of all provided filter sets AND the caller's `accessibleSiteNumbersForSession`. Empty intersection short-circuits to a 200 with `rows: []`. `handleListSubmissions` + `handleCsvExport` in `src/handlers/admin.js` now call `resolveLocationFilters` instead of reading scope directly — the existing `siteNumbersFilter` plumbing in `db.js` is unchanged. New `src/handlers/roster.js` (~250 LOC) serves `GET /admin/jotform/api/roster` (any-session gate) returning `{ regional_directors, regional_managers, locations, scope }` — derived from one `locations` read + one `pricing_simple` lookup for `location_pretty`, scoped to the caller's accessible set when not admin-tier. apps/web side: `worker-fetch.ts` extended with `getRoster()` + `JotformRoster` / `RosterAm` / `RosterRm` / `RosterLocation` types + new opts (`amEmail` / `rmEmail` / `locationCode`) on both `listSubmissions` and `csvExportUrl`. New client component `apps/web/app/admin/jotform/[form_id]/_components/FilterBar.tsx` — three URL-driven `<select>`s (RD / RM / Location); RM + Location options narrow client-side when RD is set (UX hint; worker re-validates on submit). Each `<select>` change pushes the URL and clears `?offset=` (narrow filters mean the prior offset might be past the new total). Empty-roster slots (e.g., GM with one site sees no RDs) are hidden entirely. `page.tsx` renders FilterBar above DateRangePicker and groups the current 50-row page by location → date with friendly headers ("Binghamton (127) — Regional Manager: Bob Smith" / "May 11, 2026 (3 submissions)"). Single-location pages render flat (no outer group chrome — the date sub-headers carry the structure). Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds (`/admin/jotform/[form_id]` 1.55 kB / 106 kB First Load — up from Brief 109's 924 B but well under the brief's targets); `pnpm --filter @splash/jotform-worker exec wrangler deploy --dry-run` 752.20 KiB raw / 142.28 KiB gzipped. No schema / R2 / wrangler.toml / secret changes. | Completed (2026-05-11) | none — additive UX extension on top of Brief 109 | [brief-110-jotform-viewer-filters-and-grouping.md](brief-110-jotform-viewer-filters-and-grouping.md) |

| 109 | apps/web JotForm viewer + dashboard tile. Paints `/admin/jotform/*` on top of Brief 107's `splash-jotform` worker — no worker change, no schema change, no env/secret. Six new files under `apps/web/app/admin/jotform/`: `_lib/worker-fetch.ts` (typed wrappers around the four admin endpoints + `csvExportUrl` builder via the `JOTFORM_WORKER` service binding, URL-fallback for `next dev` — mirrors fleet's Brief 83 shape), `_components/NoAccessCard.tsx` (admin-tier copy mirroring Brief 95's forms NoAccessCard with JotForm-specific subtext), `page.tsx` (index — admin-tier gate via getMe(); enabled forms render as cards; "JotForm worker not configured" empty state when the binding is unbound), `[form_id]/page.tsx` (per-form submissions list — any-session gate, DateRangePicker + CsvExportButton + meta-only table "Submitted at / Site / Status / View →" + Prev/Next pagination keyed off `total`+`offset`+`limit`; notFound() for unknown form_ids when admin-tier resolves via listForms()), and `[form_id]/[submission_id]/page.tsx` (detail — metadata grid + generic alphabetical-key `<dl>` over `row.answers` with type-aware `<AnswerValue>` renderer that prefers `prettyFormat` → `answer.text/value` → JSON pre-block; bottom-of-page `<details>` "Raw JSON (debug)" for super_admin debugging). Dashboard tile add: new "JotForm" tile (inline lucide FileText SVG, "Submissions" eyebrow, links to `/admin/jotform`) — first dashboard tile to be visibility-gated. `AdminDashboardPage` made async, calls getMe(), filters via a new optional `Tile.visibleTo === "adminTier"` field. RM/RD/GM users still see per-form URLs by bookmark and the worker scopes their rows automatically (Brief 107). Generic alphabetical-key renderer means new JotForm forms onboarded via SQL INSERT + backfill require zero apps/web code changes. Form-specific field ordering is a v2 candidate. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/web build` succeeds. New routes' bundle: `/admin/jotform` 172 B / 105 kB First Load; `/admin/jotform/[form_id]` 924 B / 106 kB; `/admin/jotform/[form_id]/[submission_id]` 172 B / 105 kB — all well under the 5 kB target the brief flagged. | Completed (2026-05-11) | none — strictly additive UI on top of Brief 107's worker | [brief-109-apps-web-jotform-viewer.md](brief-109-apps-web-jotform-viewer.md) |
| 136 | Damage — claim form localStorage autosave + resume banner. Ports the Brief 122 forms-public.js autosave pattern to the customer-facing `/claims/{site}` form in `apps/damage-worker/src/render/claim-form.ts`. Single-file change (+262 LOC): ~25 LOC CSS appended to `SHARED_STYLES` (amber `.resume-banner` palette mirroring Brief 122 with splash-navy Resume button); ~225 LOC inline JS inserted between the photo-widget setup and the submit handler so `syncDamageOther` / `syncEquipment` are already defined when `restoreForm` calls them; 7-LOC edit to the existing submit handler calling `clearDraft()` after `validateBeforeSubmit()` passes. Storage key `claims.draft.{location_code}` read from the existing `<input name="location">` hidden input; value shape `{values, savedAt}` with no `photoR2Keys` field — photos NOT persisted because the form uses a local File-in-closure pattern (not the Brief 92 OOB-upload + r2_key pattern the brief Phase 1 spec assumed). Customer re-adds photos on resume; the 10+ typed customer-section fields are preserved. PIN gate stays sealed on resume — staff-section fields rehydrate silently underneath the hidden section and surface pre-populated after PIN unlock. 500ms debounce on input/change via event delegation on the form root; 30-day staleness; banner copy "We saved your progress from {time-ago}. Pick up where you left off?" with Resume / Start over buttons. `__equipmentRelated` skipped per brief Phase 2.4 (side effect: `equipmentInvolved` wipes on resume because default-No radio fires syncEquipment's reset — acceptable single-dropdown re-pick). Clear-on-submit fires after validateBeforeSubmit passes but before the fetch — the form is JS-driven (preventDefault + outcome card), so Brief 122's "before browser navigates" semantics map to "after client-side validation passes." Validation: root `pnpm typecheck` 18/18 green (17 cache hits, damage-worker ran fresh); `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1740.19 KiB raw / 394.69 KiB gzipped (+10.58 KiB raw / +3.37 KiB gzip vs Brief 135 baseline). No worker / Supabase / R2 / D1 / wrangler.toml / secret changes. CLAUDE.md one-liner appended to the existing **claim summary PDF** glossary entry — the closest sibling describing customer-facing claim form behavior. Deviation chief: photo persistence dropped because the form's local-File-in-closure pattern can't survive JSON.stringify and base64 + a 4-photo claim exceeds localStorage's 5MB-per-origin quota; flagged as v2 if photo-on-resume becomes a real complaint. | Completed (2026-05-15) | none — additive UX hardening on the customer claim form | [brief-136-claim-form-localstorage-autosave-and-resume.md](brief-136-claim-form-localstorage-autosave-and-resume.md) |

| 135 | Claim form customer section — all fields required + gate Continue button. Small UX hardening on the customer-facing `/claims/{site}` form in `apps/damage-worker/src/render/claim-form.ts`. Phase 1: three previously-optional section-1 fields (`mailingAddress`, `licensePlate`, `vehicleColor`) now carry the `required` HTML attribute + red asterisk label marker — bringing all ten customer-section fields to required parity. Phase 2: the `#btnContinue` ("Continue to staff assessment →") click handler now validates the customer section before opening the PIN modal. Implementation reads every `[required]` element inside `#customerSection` and calls `checkValidity()` per field; the first invalid field gets `reportValidity()` + `.focus()` and the handler returns without opening the PIN modal. The existing `openPinModal()` call now sits behind that gate (wrapping rather than replacing — easy revert). The `customerSection` const reference was already declared in the FORM_SCRIPT IIFE (line ~601) for the unrelated unlockEmployeeSection flow, so no new id wiring was needed. Phase 3: `mailingAddress` hint text changed from "Required for payment if claim is approved" (now misleading — the field is required regardless of approval) to "Where we'll mail claim correspondence and any approved payment." per the brief's wording suggestion. Phase 4 latent (flagged, not addressed): worker-side enforcement on `POST /claims-api/submit-claim` does NOT currently re-validate the three newly-required fields. The Brief 32 customer-webhook path + D1 row schema both accept these as optional. HTML5 client-side gating + Turnstile/CSRF remains the v1 posture; widening server-side checks is a follow-up if the operator wants defense-in-depth (a crafted POST can still submit with the three new fields empty). No worker / Supabase / R2 / wrangler.toml / secret changes. CLAUDE.md damage-worker entry untouched — the existing description doesn't enumerate required fields. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, damage-worker ran fresh); `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1729.61 KiB raw / 391.32 KiB gzip (≈ unchanged vs prior baseline — ~30 LOC added to claim-form.ts only). Operator post-deploy smoke deferred per Phase 4.4: verify red asterisks on the three newly-required labels; verify Continue refuses to advance with any blank required customer field and surfaces the native validity bubble at the first invalid one; verify PIN overlay still opens normally when all customer fields are filled; verify the submitted D1 row carries the three new values and the Brief 32 customer-webhook payload includes them. | Completed (2026-05-15) | none — strictly additive UX hardening on the customer claim form | [brief-135-claim-form-customer-section-required-fields.md](brief-135-claim-form-customer-section-required-fields.md) |

| 137 | Claim form — drop mailing address hint, keep email hint. One-line follow-up to Brief 135's Phase 3. Removes the `<span class="hint">Where we'll mail claim correspondence and any approved payment.</span>` element under the Mailing Address label on the customer-facing `/claims/{site}` form (`apps/damage-worker/src/render/claim-form.ts`); the label keeps its red asterisk + required attribute (unchanged from Brief 135 Phase 1). The asterisk + the form's overall context (a damage-claim form) makes the mailing-address ask self-evident — no need to explain why Splash is collecting the field. The `customerEmail` hint ("We'll email you a copy of this claim.") is preserved verbatim because it explains a post-submit behavior the customer wouldn't otherwise expect (Brief 32 PDF email). License Plate + Vehicle Color labels remain hint-less (unchanged from Brief 135). Label collapsed from 3 lines back to a single line matching the surrounding hint-less labels (`<label for="mailingAddress">Mailing Address <span class="required">*</span></label>`). Validation: root `pnpm typecheck` 18/18 green (17 cache hits, damage-worker ran fresh); `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1740.07 KiB raw / 394.66 KiB gzip (essentially flat vs Brief 136 baseline — net -2 lines). No worker / Supabase / R2 / D1 / wrangler.toml / secret changes. No CLAUDE.md change. Operator post-deploy smoke deferred per Phase 3.4: visual check on `/claims/{site}` confirming Mailing Address label has the red asterisk but NO hint underneath, Email Address label retains both the asterisk AND the "We'll email you a copy of this claim." hint, License Plate + Vehicle Color labels unchanged. | Completed (2026-05-15) | none — strictly subtractive UX polish on the customer claim form | [brief-137-claim-form-mailing-address-hint-removal.md](brief-137-claim-form-mailing-address-hint-removal.md) |
| 138 | Claim form — submit resilience (defer `clearDraft`, offline banner, idempotency key, retry/backoff). Hardens the customer `/claims/{site}` submit path against transient network failures. Four sub-changes in `apps/damage-worker/src/render/claim-form.ts` + `apps/damage-worker/src/index.ts` + `packages/db-d1/src/claims.ts`: Phase 1 reverses Brief 136's optimistic-clear (option B → option A) so a failed submit leaves the localStorage draft intact for resume-on-next-page-load. Phase 2 adds a `navigator.onLine`-driven amber offline banner (`.banner-offline` CSS + `<div id="offlineBanner">` + `online`/`offline` listeners). Phase 3 generates a UUID v4 `submissionId` per form instance via `crypto.randomUUID()` (Math.random polyfill fallback), appends it as `idempotency_key` on every submit, and adds a worker-side dedup query (`getClaimByIdempotencyKey` in `@splash/db-d1`) that fires BEFORE any side effects; hit → re-emit the original success response (same shape) without duplicate webhooks / photos / PDF / PA POST. `ClaimInsert` widened with optional `idempotency_key`; INSERT column list + VALUES + bind extended. Worker tolerates missing column via try/catch detecting `no such column.*idempotency_key` and falls through with a logged fallthrough — backstop for the brief window between code push and operator-applied D1 migration. Phase 4 adds `submitWithRetry(fd, 3)`: retries on `fetch` reject + HTTP 408/502/503/504, 1s/2s/4s exponential backoff (3 retries, 4 total attempts), per-attempt 30s `AbortController` timeout, "Submitting (retry N of 3)..." overlay text, and a `pendingOnlineRetry` slot so offline-during-backoff holds the next attempt pending the `online` event for immediate firing on reconnect. Operator-applied D1 schema migration: `ALTER TABLE claims ADD COLUMN idempotency_key TEXT;` + `CREATE UNIQUE INDEX idx_claims_idempotency_key ON claims(idempotency_key) WHERE idempotency_key IS NOT NULL;` — documented in the brief Outcome; operator runs separately via `wrangler d1 execute splash-damage-claims --remote --command`. Validation: root `pnpm typecheck` 18/18 green; `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1749.47 KiB raw / 397.44 KiB gzip (+9.4 KiB raw / +2.78 KiB gzip vs Brief 137 baseline — the four new client-side helpers + worker dedup block). No Supabase / R2 / wrangler.toml / secret changes (D1 schema change is the only data-layer touch and is operator-applied). Service-worker offline page-load + Background-Sync-API queued submits + IndexedDB photo persistence all explicitly deferred per operator. | Completed (2026-05-15) | Service-worker / Background-Sync work (IndexedDB-backed photo persistence, true offline submit queue) deferred until this lands and is exercised against real-world drops | [brief-138-claim-form-submit-resilience.md](brief-138-claim-form-submit-resilience.md) |
| 139 | Claim form — persist idempotency_key in localStorage draft. Surgical follow-up to Brief 138 that closes the last duplicate-claim failure mode in the customer `/claims/{site}` submit path: server-side success → response lost on the way back (Wi-Fi blip, edge timeout, tab closed mid-response, mobile OS suspends the tab before paint) → customer reopens the page → Resume banner appears → customer clicks Resume + Submit → without persistence a fresh UUID is generated at IIFE init → worker has no record → duplicate row. Brief 138's `submissionId` was a closure variable that died on page unload; Brief 139 promotes it into the Brief 136 localStorage draft so it survives the tab boundary. Single-file change in `apps/damage-worker/src/render/claim-form.ts` (~14 LOC net): (1) `loadDraft()` extended to surface `idempotencyKey` alongside the existing `values` + `savedAt`, with a UUID v4 regex validator (same shape as Brief 138's worker-side validator) — pre-Brief-139 drafts without the field return `idempotencyKey: null` and the init path falls through to `generateSubmissionId()`. (2) `saveDraft(values)` writes the current `submissionId` into the persisted JSON alongside `values` + `savedAt`. (3) IIFE init replaces `var submissionId = generateSubmissionId()` with `var existingDraft = loadDraft(); var submissionId = (existingDraft && existingDraft.idempotencyKey) ? existingDraft.idempotencyKey : generateSubmissionId()`. (4) Resume banner's Start over handler regenerates `submissionId` after `clearDraft()` so a discarded draft's prior key can't carry into a genuinely new submission. The success-branch `submissionId = generateSubmissionId()` regen Brief 138 already shipped covers the success-then-form-redisplayed case. `loadDraft()` is a `function` declaration, so JavaScript hoisting makes it callable at the IIFE init line even though it's defined further down the file — no manual hoist required. No worker / Supabase / R2 / D1 / wrangler.toml / secret changes. CLAUDE.md `idempotency_key` glossary entry's last paragraph rewritten — Brief 139 now describes the realized behavior rather than the proposed future. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, damage-worker ran fresh); `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1751.20 KiB raw / 398.09 KiB gzip (+1.73 KiB raw / +0.65 KiB gzip vs Brief 138 baseline — the small loadDraft / saveDraft / init delta). | Completed (2026-05-21) | none — closes the final cross-session duplicate-claim hole flagged by Brief 138's Outcome | [brief-139-claim-form-persist-idempotency-key-in-draft.md](brief-139-claim-form-persist-idempotency-key-in-draft.md) |
| 140 | Claim submit — truthful D1-failure response + INSERT-time column tolerance. Closes the "success card shown but claim invisible to admin" failure class surfaced by real-world Brief 138 testing on 2026-05-21: when the D1 INSERT throws (the immediate trigger was `no such column: idempotency_key` during the brief window between code push and the operator-applied D1 migration, but the failure class is broader — any D1 outage / constraint violation reproduces), the prior worker silently returned `200 OK` with `d1Success: false` and the client trusted `ok: true` alone and painted the green check mark on a claim that was never persisted. Four-part fix: (a) `writeClaimBatch` in `packages/db-d1/src/claims.ts` wraps `db.batch([...])` in try/catch; on `no such column.*idempotency_key` (case-insensitive) it rebuilds a legacy INSERT shape that drops the column + bind + VALUES placeholder, rebuilds photo + activity statements (D1 prepared statements can't be rebound after a failed batch), and retries. Loud log `[claim.d1] idempotency_key column missing — fell back to legacy INSERT shape (apply schema migration)`. Any other D1 error rethrows. (b) `handleClaimSubmission` in `apps/damage-worker/src/index.ts` captures the D1 throw message (truncated to 500 chars at the helper boundary) and, when `d1Success === false`, returns 500 with `{ok: false, d1Success: false, claim_id, summary_pdf_url, error: "Claim was received but not persisted to admin storage. Please notify a manager and save your claim ID for reference."}` instead of 200. claim_id + summary_pdf_url ride along so the customer can still download the PDF copy and the operator has a recovery handle (the R2 submission JSON at `submissions/{claim_id}.json` is the canonical backfill source). Browser-mode direct submitters bounce back to `/claims/{slug}?error=...`. (c) Brief 138's `RETRYABLE_STATUS` map widened to include 500 — Brief 140 redefines 500 to mean "transient D1 failure" and the retry path collapses onto the original claim via the existing idempotency-key dedup. (d) Client success guard at `apps/damage-worker/src/render/claim-form.ts` line ~1338 changed from `out.ok && out.body && out.body.ok` to `out.ok && out.body && out.body.ok && out.body.d1Success !== false` — explicit-false blocks the green check mark; missing/undefined `d1Success` (older worker versions / future regressions) still succeeds. (e) New `fireD1FailureAlert` helper in `apps/damage-worker/src/notifications.ts` posts to the existing `INTERNAL_NEW_CLAIM_WEBHOOK_URL` with discriminator `alert_type: "d1_failed"` (the Brief 102 webhook URL is reused — PA branches on the field to choose template). Payload: `claim_id`, `location_code`, `customer_name`, `customer_email`, `r2_submission_url` (the R2 key path `submissions/{claim_id}.json` for one-off operator backfill), `summary_pdf_url` (when PDF generation also succeeded), `error_message`, `recipients` (INCIDENTS_EMAIL only — narrow scope, internal infra alert not a field notification). `ctx.waitUntil`-ed so it doesn't block the customer response; same fail-soft + 15s `AbortSignal` posture as Brief 101/102. `handleClaimSubmission` signature widened to accept `ctx: ExecutionContext`; fetch dispatcher passes through. PA build guide at PA_FLOWS_BRIEF_140.md. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, damage-worker + db-d1 ran fresh); `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run` 1757.37 KiB raw / 399.54 KiB gzip (+6.17 KiB raw / +1.45 KiB gzip vs Brief 139 baseline). No Supabase / R2 / wrangler.toml / secret changes. D1 schema untouched. CLAUDE.md `idempotency_key` glossary entry extended with the writeClaimBatch fallback + d1Success response contract. | Completed (2026-05-21) | Blocks Brief 141 (retry hardening) — 141's retry loop depends on the truthful success/failure contract this brief establishes | [brief-140-claim-submit-truthful-d1-failure-response.md](brief-140-claim-submit-truthful-d1-failure-response.md) |
| 141 | Claim form — retry hardening (drop `navigator.onLine` gate, longer backoff, post-exhaustion watchdog). Closes three real-world failure modes exposed by Brief 138 testing on 2026-05-21: (G5) `navigator.onLine` is unreliable on Windows — operator pulled Wi-Fi but `navigator.onLine` continued returning `true` because some interface was still "up" (VPN, sleeping Ethernet, virtual adapter), so the `if (!navigator.onLine)` branch in `scheduleNextAttempt` never fired and retries went through the setTimeout-with-backoff path instead of the held-pending-online path; (G6) Brief 138's 1s/2s/4s backoff totalled ~3s elapsed before final error banner — too aggressive for any realistic flaky-Wi-Fi recovery; (G7) once the bounded 3-attempt loop exhausted, nothing watched for connectivity to return, so a customer who reconnected Wi-Fi 30s later still saw a permanent error banner and had to manually click Submit. Three-phase fix landed in the inline FORM_SCRIPT template literal at `apps/damage-worker/src/render/claim-form.ts`: **Phase 1** — `scheduleNextAttempt` no longer references `navigator.onLine` or `pendingOnlineRetry`; it unconditionally calls `setTimeout(tryOnce, computeBackoffDelay(attempt))`. Empirical fetch failure IS the authoritative signal that the network is unreachable to our origin; the offline banner still uses `navigator.onLine` for the visual hint but the retry loop trusts the fetch result. **Phase 2** — new `computeBackoffDelay(attempt)` helper returns `Math.max(500, Math.round(base + jitter))` where `base = [2000, 5000, 15000][min(attempt-1, 2)]` and `jitter = base * 0.2 * (Math.random() * 2 - 1)`. Bounded retry's total elapsed across 3 attempts ≈ 2s + 5s = 7s (plus or minus ~1s jitter) vs Brief 138's ~3s. **Phase 3** — new `startWatchdog(fd)` function attaches a long-lived `online` event listener + 60s setInterval timer (max 30 attempts = 30-minute ceiling), firing one-shot fetches via a new `submitOnceForWatchdog(fd)` helper (no nested retry; worker idempotency-key dedup collapses any repeats). Watchdog reuses the same `submissionId` the bounded retry used, so the same key spans both phases. On watchdog success: `showOutcome` + `clearDraft` + regenerate `submissionId` + teardown. On 30-minute ceiling: banner copy switches to "Network unstable — please click Submit to retry manually." On manual submit during watchdog: submit handler's pre-validate block tears down the active watchdog via a new `activeWatchdogTeardown` closure var before running `submitWithRetry`. A second setInterval(1000) refreshes "(Last attempt: Ns ago)" suffix on the error banner. **Phase 4 wiring** — submit handler's `.then(out)` non-success branch routes to `startWatchdog(fd)` when `isRetryableStatus(out.status)` is true (5xx/408 exhausted) and falls through to the original `showError(... Please retry.)` otherwise (deterministic 4xx — customer must act). `.catch(err)` branch always routes to `startWatchdog(fd)`. `setSubmitting(false)` runs before watchdog start so overlay releases to user-interactive. Banner copy when watchdog is active: "Network unstable — we'll keep trying every minute. You can also click Submit manually. (Last attempt: Ns ago)". The Phase 2 `pendingOnlineRetry` closure variable + `online` event listener at ~lines 1107-1119 are now dead code (no consumer writes to `pendingOnlineRetry`) — left in place per brief; small cleanup candidate in a future brief. Validation: root `pnpm typecheck` 18/18 green (17 cache hits, damage-worker ran fresh; 2.089s wall); `pnpm --filter @splash/damage-worker exec wrangler deploy --dry-run --outdir .wrangler/dry-run` 1763.29 KiB raw / 401.15 KiB gzip (+5.92 KiB raw / +1.61 KiB gzip vs Brief 140 baseline). No worker / Supabase / R2 / D1 / wrangler.toml / secret / CLAUDE.md changes — strictly an inline FORM_SCRIPT template literal edit. | Completed (2026-05-21) | Service worker / Background Sync still deferred (next logical step but operator-gated) | [brief-141-claim-form-retry-hardening.md](brief-141-claim-form-retry-hardening.md) |

**Folded items** (work absorbed into earlier briefs):
- Item 3 (URL drift /admin/{loc} <-> /admin/pricing/{loc}) - folded into Brief 2.
- Item 10 (Logout flow) - folded into Brief 2.
- Item 14 (Auth middleware) - folded into Brief 1.

**Decisions** (no code, but tracked here for visibility):
- Item 8: Signup customer flow ownership - **Decided 2026-05-04: signup-worker owns** (option B). Executed in Brief 50 (2026-05-06 — placeholder retired).
- Item 9: Public claim form ownership - **Decided 2026-05-04: damage-worker owns** (option B). Same reasoning as 8.

---

## Archive backfill

Briefs 1, 2, and 4 were completed before this BRIEFS/ system was set up.
Their full text + outcomes live in BUILD_STATE.md's Findings & decisions
log. Backfilling them as standalone files in this directory is a small
follow-up task (not blocking - the source of truth is BUILD_STATE.md).
