{
  "name": "@splash/xlsx",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free XLSX writer for the Cloudflare Workers runtime. Extracted from damage-worker's claims export (Brief 178) and generalised to multiple worksheets.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint yet'",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@splash/config": "workspace:*",
    "typescript": "^5.6.0"
  }
}
